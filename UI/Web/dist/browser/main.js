import {
  LibraryAccessGuard
} from "./chunk-HY4ACD33.js";
import {
  AuthGuard
} from "./chunk-3KO4NMBD.js";
import {
  CollectionOwnerComponent
} from "./chunk-TULHW2MT.js";
import "./chunk-66ED26VX.js";
import {
  PromotedIconComponent
} from "./chunk-LOI3GAWB.js";
import "./chunk-33CWXKBP.js";
import {
  UpdateNotificationModalComponent
} from "./chunk-S5EIZF7M.js";
import {
  ChangelogUpdateItemComponent
} from "./chunk-ZT67ZH4B.js";
import {
  PreferenceNavComponent,
  SettingsTabId,
  SideNavItemComponent
} from "./chunk-HRPGFRNB.js";
import "./chunk-D3OMWBOU.js";
import {
  ServerService
} from "./chunk-KKWGV4SA.js";
import {
  SideNavStreamType
} from "./chunk-EJFLUGZ7.js";
import {
  NavService
} from "./chunk-CUVDZVWA.js";
import "./chunk-E4IH7S3P.js";
import {
  NgCircleProgressModule
} from "./chunk-454HUQZW.js";
import {
  ScrollService
} from "./chunk-7GKAFCLA.js";
import {
  SeriesFormatComponent
} from "./chunk-FAULLNLJ.js";
import {
  FilterUtilitiesService
} from "./chunk-SBBWMOEE.js";
import {
  CardActionablesComponent
} from "./chunk-W4DBALQX.js";
import {
  SearchResultGroup,
  SearchService
} from "./chunk-5VCWX7WV.js";
import {
  ActionService
} from "./chunk-HTPJQXUV.js";
import "./chunk-HG4LWHDT.js";
import "./chunk-6M2JHRYX.js";
import "./chunk-D2FQ65JS.js";
import {
  provideTranslocoLocale
} from "./chunk-HSFLUHPU.js";
import "./chunk-4AQVNEIP.js";
import "./chunk-2XUUWUDC.js";
import "./chunk-AOHQX7B2.js";
import "./chunk-ECSSQE2U.js";
import "./chunk-JGJJUNHA.js";
import "./chunk-BJ7U7OXN.js";
import "./chunk-DMXJBIB5.js";
import {
  WikiLink
} from "./chunk-GX7EGMKV.js";
import "./chunk-M6XH7OA7.js";
import {
  SentenceCasePipe
} from "./chunk-IQIEDCQT.js";
import {
  DefaultModalOptions
} from "./chunk-ZLESQ73K.js";
import "./chunk-RMZ6EY72.js";
import {
  DownloadService,
  SAVER,
  getSaver
} from "./chunk-CVI764VQ.js";
import "./chunk-C423WP7Y.js";
import {
  FilterPipe
} from "./chunk-NGXWDHM5.js";
import "./chunk-XZ5MKEYT.js";
import {
  LibraryService
} from "./chunk-PBM62GK4.js";
import "./chunk-JP4GUTMJ.js";
import "./chunk-HXDVBBHR.js";
import "./chunk-65GP3B4U.js";
import {
  ImageComponent,
  LazyLoadImageModule
} from "./chunk-2JLAQ3MZ.js";
import {
  ImageService
} from "./chunk-TPURWX2Y.js";
import {
  FilterComparison,
  FilterField
} from "./chunk-IBO6ECG5.js";
import "./chunk-BX5PUGWH.js";
import "./chunk-KJWRNOWO.js";
import "./chunk-D7CIAEAO.js";
import {
  Select2Module
} from "./chunk-LGLLZBOI.js";
import "./chunk-EA72DBFY.js";
import "./chunk-HN36WWH3.js";
import {
  ScrobbleProvider
} from "./chunk-R5PSFMI2.js";
import {
  Breakpoint,
  KEY_CODES,
  LibraryType,
  UtilityService
} from "./chunk-A6JEQ5GB.js";
import "./chunk-RSMGJKYW.js";
import {
  AccountService,
  Action,
  ActionFactoryService,
  LicenseService,
  LocalizationService,
  NavigationEnd,
  NavigationStart,
  PreloadAllModules,
  Router,
  RouterLink,
  RouterLinkActive,
  RouterModule,
  RouterOutlet,
  ThemeService
} from "./chunk-U2DWQJI2.js";
import {
  ConfirmConfig,
  ConfirmService,
  NgbActiveModal,
  NgbDropdown,
  NgbDropdownItem,
  NgbDropdownMenu,
  NgbDropdownToggle,
  NgbModal,
  NgbModalConfig,
  NgbOffcanvas,
  NgbPopover,
  NgbRatingConfig,
  NgbTooltip
} from "./chunk-2R2RCU4Y.js";
import {
  AUTO_STYLE,
  AnimationGroupPlayer,
  AnimationMetadataType,
  NoopAnimationPlayer,
  ToastrModule,
  ToastrService,
  sequence,
  style,
  ɵPRE_STYLE
} from "./chunk-5J5IKBMT.js";
import {
  EVENTS,
  MessageHubService
} from "./chunk-PUXELTB2.js";
import {
  BrowserModule,
  DomRendererFactory2,
  SafeHtmlPipe,
  Title,
  bootstrapApplication
} from "./chunk-FLGYJHAG.js";
import {
  CheckboxControlValueAccessor,
  DefaultValueAccessor,
  FormControl,
  FormControlName,
  FormGroup,
  FormGroupDirective,
  FormsModule,
  NgControlStatus,
  NgControlStatusGroup,
  NgModel,
  ReactiveFormsModule,
  ɵNgNoValidate
} from "./chunk-BPOUBDX2.js";
import {
  HTTP_INTERCEPTORS,
  HttpClient,
  environment,
  provideHttpClient,
  withInterceptorsFromDi
} from "./chunk-CQADFT7F.js";
import {
  ANIMATION_MODULE_TYPE,
  APP_BASE_HREF,
  APP_INITIALIZER,
  AsyncPipe,
  BehaviorSubject,
  ChangeDetectorRef,
  DOCUMENT,
  DestroyRef,
  EventEmitter,
  Inject,
  Injectable,
  InjectionToken,
  NgClass,
  NgModule,
  NgStyle,
  NgTemplateOutlet,
  NgZone,
  PlatformLocation,
  RendererFactory2,
  ReplaySubject,
  RuntimeError,
  TRANSLOCO_LOADER,
  TranslocoDirective,
  TranslocoService,
  __async,
  __objRest,
  __spreadValues,
  catchError,
  debounceTime,
  distinctUntilChanged,
  filter,
  from,
  fromEvent,
  getBrowserCultureLang,
  getBrowserLang,
  importProvidersFrom,
  inject,
  interval,
  isBrowser,
  isFunction,
  isObject,
  isObservable,
  isString,
  makeEnvironmentProviders,
  map,
  merge,
  of,
  provideTransloco,
  setClassMetadata,
  shareReplay,
  skip,
  startWith,
  switchMap,
  take,
  takeUntilDestroyed,
  tap,
  throwError,
  translate,
  ɵsetClassDebugInfo,
  ɵɵStandaloneFeature,
  ɵɵadvance,
  ɵɵattribute,
  ɵɵconditional,
  ɵɵcontentQuery,
  ɵɵdefineComponent,
  ɵɵdefineInjectable,
  ɵɵdefineInjector,
  ɵɵdefineNgModule,
  ɵɵdirectiveInject,
  ɵɵelement,
  ɵɵelementContainer,
  ɵɵelementContainerEnd,
  ɵɵelementContainerStart,
  ɵɵelementEnd,
  ɵɵelementStart,
  ɵɵgetCurrentView,
  ɵɵinject,
  ɵɵlistener,
  ɵɵloadQuery,
  ɵɵnextContext,
  ɵɵpipe,
  ɵɵpipeBind1,
  ɵɵpipeBind2,
  ɵɵproperty,
  ɵɵpureFunction0,
  ɵɵpureFunction1,
  ɵɵpureFunction2,
  ɵɵpureFunction3,
  ɵɵqueryRefresh,
  ɵɵreference,
  ɵɵrepeater,
  ɵɵrepeaterCreate,
  ɵɵrepeaterTrackByIdentity,
  ɵɵresetView,
  ɵɵresolveWindow,
  ɵɵrestoreView,
  ɵɵsanitizeHtml,
  ɵɵsanitizeUrl,
  ɵɵtemplate,
  ɵɵtemplateRefExtractor,
  ɵɵtext,
  ɵɵtextInterpolate,
  ɵɵtextInterpolate1,
  ɵɵtwoWayBindingSet,
  ɵɵtwoWayListener,
  ɵɵtwoWayProperty,
  ɵɵviewQuery
} from "./chunk-PYHIUVNJ.js";

// src/app/sidenav/_components/side-nav/side-nav.component.ts
var _forTrack0 = ($index, $item) => $item.name + $item.order;
var _c0 = (a0, a1) => ({ "closed": a0, "hidden": a1 });
var _c1 = (a0, a1, a2) => ({ "closed": a0, "hidden": a1, "no-donate": a2 });
var _c2 = (a0) => ({ "closed": a0 });
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 8);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("title", t_r3("customize"))("fragment", ctx_r1.SettingsTabId.Customize);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 9)(1, "label", 10);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 11)(4, "input", 12);
    \u0275\u0275twoWayListener("ngModelChange", function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Conditional_2_Template_input_ngModelChange_4_listener($event) {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(5);
      \u0275\u0275twoWayBindingSet(ctx_r1.filterQuery, $event) || (ctx_r1.filterQuery = $event);
      return \u0275\u0275resetView($event);
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "button", 13);
    \u0275\u0275listener("click", function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Conditional_2_Template_button_click_5_listener() {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r1.filterQuery = "");
    });
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("filter-label"));
    \u0275\u0275advance(2);
    \u0275\u0275twoWayProperty("ngModel", ctx_r1.filterQuery);
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-label", t_r3("clear"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-side-nav-item", 7);
    \u0275\u0275listener("click", function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Template_app_side_nav_item_click_0_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.showLess());
    });
    \u0275\u0275elementEnd();
    \u0275\u0275template(1, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Conditional_1_Template, 1, 2, "app-side-nav-item", 8)(2, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Conditional_2_Template, 6, 3, "div", 9);
    \u0275\u0275pipe(3, "async");
  }
  if (rf & 2) {
    const streams_r5 = \u0275\u0275nextContext();
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("title", t_r3("back"));
    \u0275\u0275advance();
    \u0275\u0275conditional(!ctx_r1.isReadOnly ? 1 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(streams_r5.length > ctx_r1.ItemLimit && \u0275\u0275pipeBind1(3, 3, ctx_r1.navService.sideNavCollapsed$) === false ? 2 : -1);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-side-nav-item", 14);
    \u0275\u0275elementContainerStart(1, 23);
    \u0275\u0275elementStart(2, "app-card-actionables", 24);
    \u0275\u0275listener("actionHandler", function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_0_Template_app_card_actionables_actionHandler_2_listener($event) {
      \u0275\u0275restoreView(_r6);
      const navStream_r7 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.performAction($event, navStream_r7.library));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementContainerEnd();
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const navStream_r7 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275property("link", "/library/" + navStream_r7.libraryId + "/")("icon", ctx_r1.getLibraryTypeIcon(navStream_r7.library.type))("imageUrl", ctx_r1.getLibraryImage(navStream_r7.library))("title", navStream_r7.library.name)("comparisonMethod", "startsWith");
    \u0275\u0275advance(2);
    \u0275\u0275property("actions", ctx_r1.actions)("labelBy", navStream_r7.name);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 15);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r3("all-series"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 16);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r3("bookmarks"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 17);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r3("reading-lists"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 18);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r3("collections"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 19);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r3("want-to-read"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 20);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r3("browse-authors"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 21);
  }
  if (rf & 2) {
    const navStream_r7 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275property("title", navStream_r7.name)("queryParams", navStream_r7.smartFilterEncoded);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 22);
  }
  if (rf & 2) {
    const navStream_r7 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275property("title", navStream_r7.name)("link", navStream_r7.externalSource.host + "login?apiKey=" + navStream_r7.externalSource.apiKey)("external", true);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_0_Template, 3, 7, "app-side-nav-item", 14)(1, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_1_Template, 1, 1, "app-side-nav-item", 15)(2, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_2_Template, 1, 1, "app-side-nav-item", 16)(3, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_3_Template, 1, 1, "app-side-nav-item", 17)(4, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_4_Template, 1, 1, "app-side-nav-item", 18)(5, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_5_Template, 1, 1, "app-side-nav-item", 19)(6, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_6_Template, 1, 1, "app-side-nav-item", 20)(7, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_7_Template, 1, 2, "app-side-nav-item", 21)(8, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Case_8_Template, 1, 3, "app-side-nav-item", 22);
  }
  if (rf & 2) {
    let tmp_16_0;
    const navStream_r7 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275conditional((tmp_16_0 = navStream_r7.streamType) === ctx_r1.SideNavStreamType.Library ? 0 : tmp_16_0 === ctx_r1.SideNavStreamType.AllSeries ? 1 : tmp_16_0 === ctx_r1.SideNavStreamType.Bookmarks ? 2 : tmp_16_0 === ctx_r1.SideNavStreamType.ReadingLists ? 3 : tmp_16_0 === ctx_r1.SideNavStreamType.Collections ? 4 : tmp_16_0 === ctx_r1.SideNavStreamType.WantToRead ? 5 : tmp_16_0 === ctx_r1.SideNavStreamType.BrowseAuthors ? 6 : tmp_16_0 === ctx_r1.SideNavStreamType.SmartFilter ? 7 : tmp_16_0 === ctx_r1.SideNavStreamType.ExternalSource ? 8 : -1);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "app-side-nav-item", 25);
    \u0275\u0275listener("click", function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_4_Template_app_side_nav_item_click_0_listener() {
      \u0275\u0275restoreView(_r8);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.showMore());
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(3).$implicit;
    \u0275\u0275property("title", t_r3("more"));
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_0_Template, 4, 5);
    \u0275\u0275repeaterCreate(1, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_For_2_Template, 9, 1, null, null, _forTrack0);
    \u0275\u0275pipe(3, "filter");
    \u0275\u0275template(4, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Conditional_4_Template, 1, 1, "app-side-nav-item", 6);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275conditional(ctx_r1.showAll ? 0 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(\u0275\u0275pipeBind2(3, 2, ctx, ctx_r1.filterLibrary));
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.totalSize > ctx_r1.ItemLimit && !ctx_r1.showAll ? 4 : -1);
  }
}
function SideNavComponent_ng_container_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 1);
    \u0275\u0275pipe(1, "async");
    \u0275\u0275pipe(2, "async");
    \u0275\u0275pipe(3, "async");
    \u0275\u0275elementStart(4, "div", 4);
    \u0275\u0275element(5, "app-side-nav-item", 5);
    \u0275\u0275template(6, SideNavComponent_ng_container_0_Conditional_1_Conditional_6_Template, 5, 5);
    \u0275\u0275pipe(7, "async");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_6_0;
    const t_r3 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction3(11, _c1, \u0275\u0275pipeBind1(1, 3, ctx_r1.navService.sideNavCollapsed$), \u0275\u0275pipeBind1(2, 5, ctx_r1.navService.sideNavVisibility$) === false, \u0275\u0275pipeBind1(3, 7, ctx_r1.licenseService.hasValidLicense$) === true));
    \u0275\u0275advance(5);
    \u0275\u0275property("title", t_r3("home"));
    \u0275\u0275advance();
    \u0275\u0275conditional((tmp_6_0 = \u0275\u0275pipeBind1(7, 9, ctx_r1.navStreams$)) ? 6 : -1, tmp_6_0);
  }
}
function SideNavComponent_ng_container_0_Conditional_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275pipe(1, "async");
    \u0275\u0275listener("click", function SideNavComponent_ng_container_0_Conditional_3_Conditional_0_Template_div_click_0_listener() {
      \u0275\u0275restoreView(_r9);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.toggleNavBar());
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(3, _c2, \u0275\u0275pipeBind1(1, 1, ctx_r1.navService.sideNavCollapsed$)));
  }
}
function SideNavComponent_ng_container_0_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, SideNavComponent_ng_container_0_Conditional_3_Conditional_0_Template, 2, 5, "div", 26);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275conditional(ctx < ctx_r1.Breakpoint.Desktop ? 0 : -1);
  }
}
function SideNavComponent_ng_container_0_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav-item", 3);
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("ngClass", "donate")("ngbTooltip", t_r3("donate-tooltip"))("link", ctx_r1.WikiLink.Donation)("external", true);
  }
}
function SideNavComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275template(1, SideNavComponent_ng_container_0_Conditional_1_Template, 8, 15, "div", 1);
    \u0275\u0275pipe(2, "async");
    \u0275\u0275template(3, SideNavComponent_ng_container_0_Conditional_3_Template, 1, 1);
    \u0275\u0275pipe(4, "async");
    \u0275\u0275elementStart(5, "div", 2);
    \u0275\u0275pipe(6, "async");
    \u0275\u0275pipe(7, "async");
    \u0275\u0275pipe(8, "async");
    \u0275\u0275template(9, SideNavComponent_ng_container_0_Conditional_9_Template, 1, 4, "app-side-nav-item", 3);
    \u0275\u0275pipe(10, "async");
    \u0275\u0275elementEnd();
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    let tmp_2_0;
    let tmp_3_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275conditional((tmp_2_0 = \u0275\u0275pipeBind1(2, 4, ctx_r1.accountService.currentUser$)) ? 1 : -1, tmp_2_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_3_0 = \u0275\u0275pipeBind1(4, 6, ctx_r1.utilityService.activeBreakpoint$)) ? 3 : -1, tmp_3_0);
    \u0275\u0275advance(2);
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction2(16, _c0, \u0275\u0275pipeBind1(6, 8, ctx_r1.navService.sideNavCollapsed$), \u0275\u0275pipeBind1(7, 10, ctx_r1.navService.sideNavVisibility$) === false || \u0275\u0275pipeBind1(8, 12, ctx_r1.licenseService.hasValidLicense$) === true));
    \u0275\u0275advance(4);
    \u0275\u0275conditional(\u0275\u0275pipeBind1(10, 14, ctx_r1.licenseService.hasValidLicense$) === false ? 9 : -1);
  }
}
var SideNavComponent = class _SideNavComponent {
  constructor() {
    this.router = inject(Router);
    this.utilityService = inject(UtilityService);
    this.messageHub = inject(MessageHubService);
    this.actionService = inject(ActionService);
    this.navService = inject(NavService);
    this.cdRef = inject(ChangeDetectorRef);
    this.imageService = inject(ImageService);
    this.accountService = inject(AccountService);
    this.licenseService = inject(LicenseService);
    this.destroyRef = inject(DestroyRef);
    this.actionFactoryService = inject(ActionFactoryService);
    this.WikiLink = WikiLink;
    this.ItemLimit = 10;
    this.SideNavStreamType = SideNavStreamType;
    this.SettingsTabId = SettingsTabId;
    this.cachedData = null;
    this.actions = this.actionFactoryService.getLibraryActions(this.handleAction.bind(this));
    this.filterQuery = "";
    this.filterLibrary = (stream) => {
      return stream.name.toLowerCase().indexOf((this.filterQuery || "").toLowerCase()) >= 0;
    };
    this.showAll = false;
    this.totalSize = 0;
    this.isReadOnly = false;
    this.showAllSubject = new BehaviorSubject(false);
    this.showAll$ = this.showAllSubject.asObservable();
    this.loadDataSubject = new ReplaySubject();
    this.loadData$ = this.loadDataSubject.asObservable();
    this.loadDataOnInit$ = this.loadData$.pipe(switchMap(() => {
      if (this.cachedData != null) {
        return of(this.cachedData);
      }
      return this.navService.getSideNavStreams().pipe(map((data) => {
        this.cachedData = data;
        return data;
      }));
    }));
    this.navStreams$ = merge(this.showAll$.pipe(startWith(false), distinctUntilChanged(), tap((showAll) => this.showAll = showAll), switchMap((showAll) => showAll ? this.loadDataOnInit$.pipe(tap((d) => this.totalSize = d.length)) : this.loadDataOnInit$.pipe(tap((d) => this.totalSize = d.length), map((d) => d.slice(0, this.ItemLimit)))), takeUntilDestroyed(this.destroyRef)), this.messageHub.messages$.pipe(filter((event) => event.event === EVENTS.LibraryModified || event.event === EVENTS.SideNavUpdate), tap(() => {
      this.cachedData = null;
    }), switchMap(() => {
      if (this.showAll)
        return this.loadDataOnInit$;
      else
        return this.loadDataOnInit$.pipe(map((d) => d.slice(0, this.ItemLimit)));
    }), takeUntilDestroyed(this.destroyRef))).pipe(startWith(null), filter((data) => data !== null), takeUntilDestroyed(this.destroyRef));
    this.collapseSideNavOnMobileNav$ = this.router.events.pipe(filter((event) => event instanceof NavigationEnd), takeUntilDestroyed(this.destroyRef), map((evt) => evt), filter(() => this.utilityService.getActiveBreakpoint() < Breakpoint.Tablet), switchMap(() => this.navService.sideNavCollapsed$), take(1), filter((collapsed) => !collapsed));
    this.Breakpoint = Breakpoint;
    if (this.utilityService.getActiveBreakpoint() < Breakpoint.Tablet) {
      this.navService.collapseSideNav(true);
    }
    this.collapseSideNavOnMobileNav$.subscribe(() => {
      this.navService.collapseSideNav(false);
      this.cdRef.markForCheck();
    });
  }
  ngOnInit() {
    this.accountService.currentUser$.pipe(take(1)).subscribe((user) => {
      if (!user)
        return;
      this.isReadOnly = this.accountService.hasReadOnlyRole(user);
      this.cdRef.markForCheck();
      this.loadDataSubject.next();
    });
  }
  handleAction(action, library) {
    return __async(this, null, function* () {
      switch (action.action) {
        case Action.Scan:
          yield this.actionService.scanLibrary(library);
          break;
        case Action.RefreshMetadata:
          yield this.actionService.refreshLibraryMetadata(library);
          break;
        case Action.GenerateColorScape:
          yield this.actionService.refreshLibraryMetadata(library, void 0, false);
          break;
        case Action.AnalyzeFiles:
          yield this.actionService.analyzeFiles(library);
          break;
        case Action.Delete:
          yield this.actionService.deleteLibrary(library);
          break;
        case Action.Edit:
          this.actionService.editLibrary(library, () => window.scrollTo(0, 0));
          break;
        default:
          break;
      }
    });
  }
  performAction(action, library) {
    if (typeof action.callback === "function") {
      action.callback(action, library);
    }
  }
  getLibraryTypeIcon(format) {
    switch (format) {
      case LibraryType.Book:
      case LibraryType.LightNovel:
        return "fa-book";
      case LibraryType.Comic:
      case LibraryType.ComicVine:
      case LibraryType.Manga:
        return "fa-book-open";
      case LibraryType.Images:
        return "fa-images";
    }
  }
  getLibraryImage(library) {
    if (library.coverImage)
      return this.imageService.getLibraryCoverImage(library.id);
    return null;
  }
  toggleNavBar() {
    this.navService.toggleSideNav();
  }
  showMore() {
    this.showAllSubject.next(true);
  }
  showLess() {
    this.filterQuery = "";
    this.cdRef.markForCheck();
    this.showAllSubject.next(false);
  }
  static {
    this.\u0275fac = function SideNavComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SideNavComponent)();
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _SideNavComponent, selectors: [["app-side-nav"]], standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [[4, "transloco", "translocoRead"], [1, "side-nav-container", 3, "ngClass"], [1, "sidenav-bottom", 3, "ngClass"], ["icon", "fa-heart", 3, "ngClass", "ngbTooltip", "link", "external"], [1, "side-nav"], ["icon", "fa-home", "link", "/home/", 3, "title"], ["icon", "fa fa-chevron-right", 3, "title"], ["icon", "fa fa-chevron-left", 3, "click", "title"], ["icon", "fa-cogs", "link", "/settings", 3, "title", "fragment"], [1, "mb-2", "mt-3", "ms-2", "me-2"], ["for", "filter", 1, "form-label", "visually-hidden"], [1, "form-group", "position-relative"], ["id", "filter", "autocomplete", "off", "type", "text", "aria-describedby", "reset-input", 1, "form-control", 3, "ngModelChange", "ngModel"], ["type", "button", "id", "reset-input", 1, "btn-close", 3, "click"], [3, "link", "icon", "imageUrl", "title", "comparisonMethod"], ["icon", "fa-regular fa-rectangle-list", "link", "/all-series/", 3, "title"], ["icon", "fa-bookmark", "link", "/bookmarks/", 3, "title"], ["icon", "fa-list-ol", "link", "/lists/", 3, "title"], ["icon", "fa-list", "link", "/collections/", 3, "title"], ["icon", "fa-star", "link", "/want-to-read/", 3, "title"], ["icon", "fa-users", "link", "/browse/authors/", 3, "title"], ["icon", "fa-bars-staggered", "link", "/all-series", 3, "title", "queryParams"], ["icon", "fa-server", 3, "title", "link", "external"], ["actions", ""], ["iconClass", "fa-ellipsis-v", 3, "actionHandler", "actions", "labelBy"], ["icon", "fa fa-chevron-right", 3, "click", "title"], [1, "side-nav-overlay", 3, "ngClass"], [1, "side-nav-overlay", 3, "click", "ngClass"]], template: function SideNavComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, SideNavComponent_ng_container_0_Template, 11, 19, "ng-container", 0);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "side-nav");
      }
    }, dependencies: [SideNavItemComponent, CardActionablesComponent, FilterPipe, FormsModule, DefaultValueAccessor, NgControlStatus, NgModel, TranslocoDirective, NgbTooltip, NgClass, AsyncPipe], changeDetection: 0 });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(SideNavComponent, { className: "SideNavComponent", filePath: "src/app/sidenav/_components/side-nav/side-nav.component.ts", lineNumber: 43 });
})();

// src/app/nav/_components/events-widget/events-widget.component.ts
var _c02 = (a0) => ({ "colored": a0 });
var _c12 = (a0) => ({ leftToProcess: a0 });
var _c22 = (a0) => ({ "width": a0 });
var _c3 = (a0) => ({ item: a0 });
var _c4 = (a0) => ({ num: a0 });
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 3);
    \u0275\u0275pipe(1, "async");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const onlineUsers_r1 = \u0275\u0275nextContext();
    const activeDownloads_r2 = \u0275\u0275nextContext(3);
    const ctx_r2 = \u0275\u0275nextContext(3);
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(4, _c02, (\u0275\u0275pipeBind1(1, 2, ctx_r2.activeEvents$) || 0) > 0 || activeDownloads_r2.length > 0 || ctx_r2.updateAvailable));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(onlineUsers_r1.length);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "i", 5);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "i", 6);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "div", 7);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "i", 8);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "button", 2);
    \u0275\u0275template(1, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_1_Template, 3, 6, "span", 3);
    \u0275\u0275element(2, "i", 4);
    \u0275\u0275pipe(3, "async");
    \u0275\u0275template(4, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_4_Template, 1, 0, "i", 5);
    \u0275\u0275pipe(5, "async");
    \u0275\u0275template(6, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_6_Template, 1, 0, "i", 6)(7, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_7_Template, 1, 0, "div", 7)(8, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Conditional_8_Template, 1, 0, "i", 8);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const infos_r4 = \u0275\u0275nextContext();
    const errors_r5 = \u0275\u0275nextContext();
    const activeDownloads_r2 = \u0275\u0275nextContext();
    \u0275\u0275nextContext();
    const popContent_r6 = \u0275\u0275reference(3);
    const t_r7 = \u0275\u0275nextContext().$implicit;
    const ctx_r2 = \u0275\u0275nextContext();
    \u0275\u0275property("ngbPopover", popContent_r6)("title", t_r7("title-alt"))("popoverClass", "nav-events")("autoClose", "outside");
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx.length > 1 ? 1 : -1);
    \u0275\u0275advance();
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(11, _c02, (\u0275\u0275pipeBind1(3, 7, ctx_r2.activeEvents$) || 0) > 0 || activeDownloads_r2.length > 0 || ctx_r2.updateAvailable));
    \u0275\u0275advance(2);
    \u0275\u0275conditional(errors_r5.length > 0 ? 4 : infos_r4.length > 0 ? 6 : (\u0275\u0275pipeBind1(5, 9, ctx_r2.activeEvents$) || 0) > 0 || activeDownloads_r2.length > 0 ? 7 : ctx_r2.updateAvailable ? 8 : -1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Conditional_0_Template, 9, 13, "button", 2);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    let tmp_10_0;
    const ctx_r2 = \u0275\u0275nextContext(5);
    \u0275\u0275conditional((tmp_10_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.messageHub.onlineUsers$)) ? 0 : -1, tmp_10_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Conditional_0_Template, 2, 3);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    let tmp_8_0;
    const ctx_r2 = \u0275\u0275nextContext(4);
    \u0275\u0275conditional((tmp_8_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.infos$)) ? 0 : -1, tmp_8_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Conditional_0_Template, 2, 3);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r2 = \u0275\u0275nextContext(3);
    \u0275\u0275conditional((tmp_6_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.errors$)) ? 0 : -1, tmp_6_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 11);
    \u0275\u0275listener("click", function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Conditional_0_Conditional_0_Template_li_click_0_listener() {
      \u0275\u0275restoreView(_r8);
      const ctx_r2 = \u0275\u0275nextContext(6);
      return \u0275\u0275resetView(ctx_r2.clearAllErrorOrInfos());
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r7 = \u0275\u0275nextContext(5).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", t_r7("dismiss-all"), " ");
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Conditional_0_Conditional_0_Template, 2, 1, "li", 10);
  }
  if (rf & 2) {
    const errors_r9 = \u0275\u0275nextContext();
    \u0275\u0275conditional(errors_r9.length > 0 || ctx.length > 0 ? 0 : -1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Conditional_0_Template, 1, 1);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r2 = \u0275\u0275nextContext(4);
    \u0275\u0275conditional((tmp_7_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.infos$)) ? 0 : -1, tmp_7_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 14);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const message_r10 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275property("title", message_r10.subTitle);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(message_r10.subTitle);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 14);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const message_r10 = \u0275\u0275nextContext(2).$implicit;
    const t_r7 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275property("title", t_r7("left-to-process", \u0275\u0275pureFunction1(2, _c12, message_r10.body.leftToProcess)));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", t_r7("left-to-process", \u0275\u0275pureFunction1(4, _c12, message_r10.body.leftToProcess)), " ");
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 16);
    \u0275\u0275element(1, "div", 17);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-valuenow", 100);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 12)(1, "div", 13);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275template(3, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Conditional_3_Template, 2, 2, "div", 14)(4, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Conditional_4_Template, 2, 6, "div", 14);
    \u0275\u0275elementStart(5, "div", 15);
    \u0275\u0275template(6, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Conditional_6_Template, 2, 1, "div", 16);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const message_r10 = \u0275\u0275nextContext().$implicit;
    const ctx_r2 = \u0275\u0275nextContext(5);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(message_r10.title);
    \u0275\u0275advance();
    \u0275\u0275conditional(message_r10.subTitle !== "" ? 3 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(message_r10.name === ctx_r2.EVENTS.ScanProgress && message_r10.body.leftToProcess > 0 ? 4 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(message_r10.progress === "indeterminate" ? 6 : -1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 14);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const message_r10 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275property("title", message_r10.subTitle);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(message_r10.subTitle);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 12)(1, "div", 13);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275template(3, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_1_Conditional_3_Template, 2, 2, "div", 14);
    \u0275\u0275elementStart(4, "div", 15)(5, "div", 18);
    \u0275\u0275text(6);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(7, "div", 19);
    \u0275\u0275element(8, "div", 20);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const message_r10 = \u0275\u0275nextContext().$implicit;
    const ctx_r2 = \u0275\u0275nextContext(5);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(message_r10.title);
    \u0275\u0275advance();
    \u0275\u0275conditional(message_r10.subTitle !== "" ? 3 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(ctx_r2.prettyPrintProgress(message_r10.body.progress) + "%");
    \u0275\u0275advance(2);
    \u0275\u0275property("ngStyle", \u0275\u0275pureFunction1(5, _c22, message_r10.body.progress * 100 + "%"));
    \u0275\u0275attribute("aria-valuenow", message_r10.body.progress * 100);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_0_Template, 7, 4, "li", 12)(1, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Conditional_1_Template, 9, 7, "li", 12);
  }
  if (rf & 2) {
    const message_r10 = ctx.$implicit;
    \u0275\u0275conditional(message_r10.progress === "indeterminate" || message_r10.progress === "none" ? 0 : 1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_For_1_Template, 2, 1, null, null, \u0275\u0275repeaterTrackByIdentity);
  }
  if (rf & 2) {
    \u0275\u0275repeater(ctx);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_0_Template_li_click_0_listener() {
      \u0275\u0275restoreView(_r11);
      const singleUpdate_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r2 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r2.handleUpdateAvailableClick(singleUpdate_r12));
    });
    \u0275\u0275element(1, "i", 23);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r7 = \u0275\u0275nextContext(5).$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("", t_r7("update-available"), " ");
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 24);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const singleUpdate_r12 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(singleUpdate_r12.subTitle);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 21)(1, "div");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275template(3, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_1_Conditional_3_Template, 2, 1, "div", 24);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const singleUpdate_r12 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(singleUpdate_r12.title);
    \u0275\u0275advance();
    \u0275\u0275conditional(singleUpdate_r12.subTitle !== "" ? 3 : -1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_0_Template, 3, 1, "li", 21)(1, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Conditional_1_Template, 4, 2, "li", 21);
  }
  if (rf & 2) {
    const singleUpdate_r12 = ctx.$implicit;
    const ctx_r2 = \u0275\u0275nextContext(5);
    \u0275\u0275conditional(singleUpdate_r12.name === ctx_r2.EVENTS.UpdateAvailable ? 0 : 1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_For_1_Template, 2, 1, null, null, \u0275\u0275repeaterTrackByIdentity);
  }
  if (rf & 2) {
    \u0275\u0275repeater(ctx);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_For_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 14);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const download_r13 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275property("title", download_r13.subTitle);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(download_r13.subTitle);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_For_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 12)(1, "div", 13);
    \u0275\u0275text(2);
    \u0275\u0275pipe(3, "sentenceCase");
    \u0275\u0275elementEnd();
    \u0275\u0275template(4, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_For_1_Conditional_4_Template, 2, 2, "div", 14);
    \u0275\u0275elementStart(5, "div", 15)(6, "div", 18);
    \u0275\u0275text(7);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "div", 19);
    \u0275\u0275element(9, "div", 20);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const download_r13 = ctx.$implicit;
    const t_r7 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r7("downloading-item", \u0275\u0275pureFunction1(7, _c3, \u0275\u0275pipeBind1(3, 5, download_r13.entityType))));
    \u0275\u0275advance(2);
    \u0275\u0275conditional(download_r13.subTitle !== "" ? 4 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1("", download_r13.progress, "%");
    \u0275\u0275advance(2);
    \u0275\u0275property("ngStyle", \u0275\u0275pureFunction1(9, _c22, download_r13.progress + "%"));
    \u0275\u0275attribute("aria-valuenow", download_r13.progress);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 12);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const activeDownloads_r14 = \u0275\u0275nextContext();
    const t_r7 = \u0275\u0275nextContext(3).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(t_r7("download-in-queue", \u0275\u0275pureFunction1(1, _c4, activeDownloads_r14.length)));
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_For_1_Template, 10, 11, "li", 12, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275template(2, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_Conditional_2_Template, 2, 3, "li", 12);
  }
  if (rf & 2) {
    const activeDownloads_r14 = ctx;
    \u0275\u0275repeater(activeDownloads_r14);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(activeDownloads_r14.length > 1 ? 2 : -1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_9_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 26);
    \u0275\u0275listener("click", function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_9_For_1_Template_li_click_0_listener() {
      const error_r16 = \u0275\u0275restoreView(_r15).$implicit;
      const ctx_r2 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r2.seeMore(error_r16));
    });
    \u0275\u0275elementStart(1, "div")(2, "div", 13);
    \u0275\u0275element(3, "i", 27);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "div", 28);
    \u0275\u0275text(6);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(7, "button", 29);
    \u0275\u0275listener("click", function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_9_For_1_Template_button_click_7_listener($event) {
      const error_r16 = \u0275\u0275restoreView(_r15).$implicit;
      const ctx_r2 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r2.removeErrorOrInfo(error_r16, $event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const error_r16 = ctx.$implicit;
    const t_r7 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(error_r16.title);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r7("more-info"));
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-label", t_r7("close"));
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_9_For_1_Template, 8, 3, "li", 25, \u0275\u0275repeaterTrackByIdentity);
  }
  if (rf & 2) {
    \u0275\u0275repeater(ctx);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_11_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 31);
    \u0275\u0275listener("click", function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_11_For_1_Template_li_click_0_listener() {
      const info_r18 = \u0275\u0275restoreView(_r17).$implicit;
      const ctx_r2 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r2.seeMore(info_r18));
    });
    \u0275\u0275elementStart(1, "div")(2, "div", 13);
    \u0275\u0275element(3, "i", 32);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "div", 28);
    \u0275\u0275text(6);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(7, "button", 29);
    \u0275\u0275listener("click", function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_11_For_1_Template_button_click_7_listener($event) {
      const info_r18 = \u0275\u0275restoreView(_r17).$implicit;
      const ctx_r2 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r2.removeErrorOrInfo(info_r18, $event));
    });
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const info_r18 = ctx.$implicit;
    const t_r7 = \u0275\u0275nextContext(4).$implicit;
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(info_r18.title);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r7("more-info"));
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-label", t_r7("close"));
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_11_For_1_Template, 8, 3, "li", 30, \u0275\u0275repeaterTrackByIdentity);
  }
  if (rf & 2) {
    \u0275\u0275repeater(ctx);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Conditional_0_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 12);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r7 = \u0275\u0275nextContext(6).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(t_r7("no-data"));
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Conditional_0_Conditional_0_Conditional_0_Template, 2, 1, "li", 12);
  }
  if (rf & 2) {
    const errors_r19 = \u0275\u0275nextContext();
    const activeDownloads_r20 = \u0275\u0275nextContext();
    const ctx_r2 = \u0275\u0275nextContext(4);
    \u0275\u0275conditional(ctx.length === 0 && errors_r19.length === 0 && activeDownloads_r20.length === 0 && ctx_r2.activeEvents === 0 ? 0 : -1);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Conditional_0_Conditional_0_Template, 1, 1);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    let tmp_9_0;
    const ctx_r2 = \u0275\u0275nextContext(5);
    \u0275\u0275conditional((tmp_9_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.infos$)) ? 0 : -1, tmp_9_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Conditional_0_Template, 2, 3);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r2 = \u0275\u0275nextContext(4);
    \u0275\u0275conditional((tmp_7_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.errors$)) ? 0 : -1, tmp_7_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "ul", 9);
    \u0275\u0275template(1, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_1_Template, 2, 3);
    \u0275\u0275pipe(2, "async");
    \u0275\u0275template(3, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_3_Template, 2, 0);
    \u0275\u0275pipe(4, "async");
    \u0275\u0275template(5, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_5_Template, 2, 0);
    \u0275\u0275pipe(6, "async");
    \u0275\u0275template(7, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_7_Template, 3, 1);
    \u0275\u0275pipe(8, "async");
    \u0275\u0275template(9, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_9_Template, 2, 0);
    \u0275\u0275pipe(10, "async");
    \u0275\u0275template(11, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_11_Template, 2, 0);
    \u0275\u0275pipe(12, "async");
    \u0275\u0275template(13, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Conditional_13_Template, 2, 3);
    \u0275\u0275pipe(14, "async");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    let tmp_5_0;
    let tmp_6_0;
    let tmp_7_0;
    let tmp_8_0;
    let tmp_9_0;
    let tmp_10_0;
    let tmp_11_0;
    const ctx_r2 = \u0275\u0275nextContext(3);
    \u0275\u0275advance();
    \u0275\u0275conditional((tmp_5_0 = \u0275\u0275pipeBind1(2, 7, ctx_r2.errors$)) ? 1 : -1, tmp_5_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_6_0 = \u0275\u0275pipeBind1(4, 9, ctx_r2.progressEvents$)) ? 3 : -1, tmp_6_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_7_0 = \u0275\u0275pipeBind1(6, 11, ctx_r2.singleUpdates$)) ? 5 : -1, tmp_7_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_8_0 = \u0275\u0275pipeBind1(8, 13, ctx_r2.downloadService.activeDownloads$)) ? 7 : -1, tmp_8_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_9_0 = \u0275\u0275pipeBind1(10, 15, ctx_r2.errors$)) ? 9 : -1, tmp_9_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_10_0 = \u0275\u0275pipeBind1(12, 17, ctx_r2.infos$)) ? 11 : -1, tmp_10_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_11_0 = \u0275\u0275pipeBind1(14, 19, ctx_r2.downloadService.activeDownloads$)) ? 13 : -1, tmp_11_0);
  }
}
function EventsWidgetComponent_ng_container_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Conditional_1_Conditional_0_Template, 2, 3);
    \u0275\u0275pipe(1, "async");
    \u0275\u0275template(2, EventsWidgetComponent_ng_container_0_Conditional_1_ng_template_2_Template, 15, 21, "ng-template", null, 0, \u0275\u0275templateRefExtractor);
  }
  if (rf & 2) {
    let tmp_4_0;
    const ctx_r2 = \u0275\u0275nextContext(2);
    \u0275\u0275conditional((tmp_4_0 = \u0275\u0275pipeBind1(1, 1, ctx_r2.downloadService.activeDownloads$)) ? 0 : -1, tmp_4_0);
  }
}
function EventsWidgetComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275template(1, EventsWidgetComponent_ng_container_0_Conditional_1_Template, 4, 3);
    \u0275\u0275pipe(2, "async");
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275conditional(\u0275\u0275pipeBind1(2, 1, ctx_r2.accountService.isAdmin$) ? 1 : -1);
  }
}
var EventsWidgetComponent = class _EventsWidgetComponent {
  constructor() {
    this.downloadService = inject(DownloadService);
    this.messageHub = inject(MessageHubService);
    this.modalService = inject(NgbModal);
    this.accountService = inject(AccountService);
    this.confirmService = inject(ConfirmService);
    this.cdRef = inject(ChangeDetectorRef);
    this.destroyRef = inject(DestroyRef);
    this.progressEventsSource = new BehaviorSubject([]);
    this.progressEvents$ = this.progressEventsSource.asObservable();
    this.singleUpdateSource = new BehaviorSubject([]);
    this.singleUpdates$ = this.singleUpdateSource.asObservable();
    this.errorSource = new BehaviorSubject([]);
    this.errors$ = this.errorSource.asObservable();
    this.infoSource = new BehaviorSubject([]);
    this.infos$ = this.infoSource.asObservable();
    this.updateNotificationModalRef = null;
    this.activeEventsSource = new BehaviorSubject(0);
    this.activeEvents$ = this.activeEventsSource.asObservable().pipe(startWith(0), takeUntilDestroyed(this.destroyRef), debounceTime(100));
    this.activeEvents = 0;
    this.updateAvailable = false;
    this.EVENTS = EVENTS;
  }
  ngOnDestroy() {
    this.progressEventsSource.complete();
    this.singleUpdateSource.complete();
    this.errorSource.complete();
  }
  ngOnInit() {
    this.messageHub.messages$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe((event) => {
      if (event.event === EVENTS.NotificationProgress) {
        this.processNotificationProgressEvent(event);
      } else if (event.event === EVENTS.Error) {
        const values = this.errorSource.getValue();
        values.push(event.payload);
        this.errorSource.next(values);
        this.activeEvents += 1;
        this.activeEventsSource.next(this.activeEvents);
        this.cdRef.markForCheck();
      } else if (event.event === EVENTS.Info) {
        const values = this.infoSource.getValue();
        values.push(event.payload);
        this.infoSource.next(values);
        this.activeEvents += 1;
        this.activeEventsSource.next(this.activeEvents);
        this.cdRef.markForCheck();
      } else if (event.event === EVENTS.UpdateAvailable) {
        this.handleUpdateAvailableClick(event.payload);
      }
    });
  }
  processNotificationProgressEvent(event) {
    const message = event.payload;
    let data;
    switch (event.payload.eventType) {
      case "single":
        const values = this.singleUpdateSource.getValue();
        values.push(message);
        this.singleUpdateSource.next(values);
        this.activeEvents += 1;
        this.activeEventsSource.next(this.activeEvents);
        if (event.payload.name === EVENTS.UpdateAvailable) {
          this.updateAvailable = true;
        }
        this.cdRef.markForCheck();
        break;
      case "started":
        data = this.mergeOrUpdate(this.progressEventsSource.getValue(), message);
        this.progressEventsSource.next(data);
        break;
      case "updated":
        data = this.mergeOrUpdate(this.progressEventsSource.getValue(), message);
        this.progressEventsSource.next(data);
        break;
      case "ended":
        data = this.progressEventsSource.getValue();
        data = data.filter((m) => m.name !== message.name);
        this.progressEventsSource.next(data);
        this.activeEvents = Math.max(this.activeEvents - 1, 0);
        this.activeEventsSource.next(this.activeEvents);
        this.cdRef.markForCheck();
        break;
      default:
        break;
    }
  }
  mergeOrUpdate(data, message) {
    const index = data.findIndex((m) => m.name === message.name);
    if (index < 0) {
      data.push(message);
      this.activeEvents += 1;
      this.activeEventsSource.next(this.activeEvents);
      this.cdRef.markForCheck();
    } else {
      data[index] = message;
    }
    return data;
  }
  handleUpdateAvailableClick(message) {
    if (this.updateNotificationModalRef != null) {
      return;
    }
    this.updateNotificationModalRef = this.modalService.open(UpdateNotificationModalComponent, DefaultModalOptions);
    if (message.hasOwnProperty("body")) {
      this.updateNotificationModalRef.componentInstance.updateData = message.body;
    } else {
      this.updateNotificationModalRef.componentInstance.updateData = message;
    }
    this.updateNotificationModalRef.closed.subscribe(() => {
      this.updateNotificationModalRef = null;
    });
    this.updateNotificationModalRef.dismissed.subscribe(() => {
      this.updateNotificationModalRef = null;
    });
  }
  seeMore(event) {
    return __async(this, null, function* () {
      const config = new ConfirmConfig();
      if (event.name === EVENTS.Error) {
        config.buttons = [
          { text: "Ok", type: "secondary" },
          { text: "Dismiss", type: "primary" }
        ];
      } else {
        config.buttons = [
          { text: "Ok", type: "primary" }
        ];
      }
      config.header = event.title;
      config.content = event.subTitle;
      const result = yield this.confirmService.alert(event.subTitle || event.title, config);
      if (result) {
        this.removeErrorOrInfo(event);
      }
    });
  }
  clearAllErrorOrInfos() {
    const infoCount = this.infoSource.getValue().length;
    const errorCount = this.errorSource.getValue().length;
    this.infoSource.next([]);
    this.errorSource.next([]);
    this.activeEvents -= Math.max(infoCount + errorCount, 0);
    this.activeEventsSource.next(this.activeEvents);
    this.cdRef.markForCheck();
  }
  removeErrorOrInfo(messageEvent, event) {
    if (event) {
      event.stopPropagation();
      event.preventDefault();
    }
    let data = [];
    if (messageEvent.name === EVENTS.Info) {
      data = this.infoSource.getValue();
      data = data.filter((m) => m !== messageEvent);
      this.infoSource.next(data);
    } else {
      data = this.errorSource.getValue();
      data = data.filter((m) => m !== messageEvent);
      this.errorSource.next(data);
    }
    this.activeEvents = Math.max(this.activeEvents - 1, 0);
    this.activeEventsSource.next(this.activeEvents);
    this.cdRef.markForCheck();
  }
  prettyPrintProgress(progress) {
    return Math.trunc(progress * 100);
  }
  static {
    this.\u0275fac = function EventsWidgetComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _EventsWidgetComponent)();
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _EventsWidgetComponent, selectors: [["app-nav-events-toggle"]], inputs: { user: "user" }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [["popContent", ""], [4, "transloco", "translocoRead"], ["type", "button", "placement", "bottom", 1, "btn", "btn-icon", 3, "ngbPopover", "title", "popoverClass", "autoClose"], [1, "me-2", 3, "ngClass"], ["aria-hidden", "true", 1, "fa", "fa-wave-square", "nav", 3, "ngClass"], ["aria-hidden", "true", 1, "fa", "fa-circle-exclamation", "nav", "widget-button--indicator", "error"], ["aria-hidden", "true", 1, "fa", "fa-circle-info", "nav", "widget-button--indicator", "info"], [1, "nav", "widget-button--indicator", "spinner-border", "spinner-border-sm"], ["aria-hidden", "true", 1, "fa", "fa-circle-arrow-up", "nav", "widget-button--indicator", "update"], [1, "list-group", "list-group-flush", "dark-menu"], [1, "list-group-item", "dark-menu-item", "clickable"], [1, "list-group-item", "dark-menu-item", "clickable", 3, "click"], [1, "list-group-item", "dark-menu-item"], [1, "h6", "mb-1"], [1, "accent-text", "mb-1", 3, "title"], [1, "progress-container", "row", "g-0", "align-items-center"], [1, "progress", 2, "height", "5px"], ["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", 1, "progress-bar", "progress-bar-striped", "progress-bar-animated", 2, "width", "100%"], [1, "col-2"], [1, "col-10", "progress", 2, "height", "5px"], ["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", 1, "progress-bar", 3, "ngStyle"], [1, "list-group-item", "dark-menu-item", "update-available"], [1, "list-group-item", "dark-menu-item", "update-available", 3, "click"], ["aria-hidden", "true", 1, "fa", "fa-chevron-circle-up", "me-1"], [1, "accent-text"], ["role", "alert", 1, "list-group-item", "dark-menu-item", "error"], ["role", "alert", 1, "list-group-item", "dark-menu-item", "error", 3, "click"], ["aria-hidden", "true", 1, "fa-solid", "fa-triangle-exclamation", "me-2"], [1, "accent-text", "mb-1"], ["type", "button", 1, "btn-close", "float-end", 3, "click"], ["role", "alert", 1, "list-group-item", "dark-menu-item", "info"], ["role", "alert", 1, "list-group-item", "dark-menu-item", "info", 3, "click"], ["aria-hidden", "true", 1, "fa-solid", "fa-circle-info", "me-2"]], template: function EventsWidgetComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, EventsWidgetComponent_ng_container_0_Template, 3, 3, "ng-container", 1);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "events-widget");
      }
    }, dependencies: [NgClass, NgbPopover, NgStyle, AsyncPipe, SentenceCasePipe, TranslocoDirective], styles: ["\n\n.dark-menu[_ngcontent-%COMP%] {\n  background-color: var(--navbar-bg-color);\n  border-color: var(--event-widget-border-color);\n}\n.dark-menu-item[_ngcontent-%COMP%] {\n  color: var(--event-widget-text-color);\n  background-color: var(--event-widget-item-bg-color);\n  border-color: var(--event-widget-item-border-color);\n}\n  .bs-popover-bottom > .popover-arrow::after, \n.bs-popover-bottom[_ngcontent-%COMP%]    > .popover-arrow[_ngcontent-%COMP%]::before {\n  border-bottom-color: transparent;\n}\n.colored[_ngcontent-%COMP%] {\n  color: var(--event-widget-activity-bg-color) !important;\n}\n.widget-button--indicator[_ngcontent-%COMP%] {\n  position: absolute;\n  top: 24px;\n  color: var(--event-widget-activity-bg-color);\n}\n.widget-button--indicator.error[_ngcontent-%COMP%] {\n  color: var(--event-widget-error-bg-color) !important;\n}\n.widget-button--indicator.info[_ngcontent-%COMP%] {\n  color: var(--event-widget-info-bg-color) !important;\n}\n.widget-button--indicator.update[_ngcontent-%COMP%] {\n  color: var(--event-widget-update-bg-color) !important;\n}\n  .nav-events .popover-body {\n  min-width: 300px;\n  max-width: 300px;\n  padding: 0px;\n  box-shadow: 0px 0px 12px rgba(0, 0, 0, 0.75);\n  max-height: calc(100vh - 60px);\n  overflow-y: auto;\n}\n  .nav-events .popover {\n  min-width: 300px;\n}\n.progress-container[_ngcontent-%COMP%] {\n  width: 100%;\n}\n.progress[_ngcontent-%COMP%] {\n  padding: 0;\n}\n.accent-text[_ngcontent-%COMP%] {\n  width: 100%;\n  text-overflow: ellipsis;\n  overflow: hidden;\n  white-space: nowrap;\n}\n.small-spinner[_ngcontent-%COMP%] {\n  width: 1rem;\n  height: 1rem;\n}\n.btn-icon[_ngcontent-%COMP%] {\n  color: var(--event-widget-text-color);\n}\n.dark-menu-item.update-available[_ngcontent-%COMP%] {\n  cursor: pointer;\n  opacity: 1;\n  color: var(--primary-color);\n}\n.dark-menu-item.update-available[_ngcontent-%COMP%]   i.fa[_ngcontent-%COMP%] {\n  color: var(--primary-color) !important;\n}\n.dark-menu-item.error[_ngcontent-%COMP%] {\n  cursor: pointer;\n  position: relative;\n}\n.dark-menu-item.error[_ngcontent-%COMP%]   .h6[_ngcontent-%COMP%] {\n  color: var(--event-widget-error-bg-color);\n}\n.dark-menu-item.error[_ngcontent-%COMP%]   i.fa[_ngcontent-%COMP%] {\n  color: var(--primary-color) !important;\n}\n.dark-menu-item.error[_ngcontent-%COMP%]   .btn-close[_ngcontent-%COMP%] {\n  top: 5px;\n  right: 10px;\n  font-size: 11px;\n  position: absolute;\n}\n.dark-menu-item.info[_ngcontent-%COMP%] {\n  cursor: pointer;\n  position: relative;\n}\n.dark-menu-item.info[_ngcontent-%COMP%]   .h6[_ngcontent-%COMP%] {\n  color: var(--event-widget-info-bg-color);\n}\n.dark-menu-item.info[_ngcontent-%COMP%]   i.fa[_ngcontent-%COMP%] {\n  color: var(--primary-color) !important;\n}\n.dark-menu-item.info[_ngcontent-%COMP%]   .btn-close[_ngcontent-%COMP%] {\n  top: 35px;\n  right: 10px;\n  font-size: 11px;\n  position: absolute;\n}\n/*# sourceMappingURL=events-widget.component.css.map */"], changeDetection: 0 });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(EventsWidgetComponent, { className: "EventsWidgetComponent", filePath: "src/app/nav/_components/events-widget/events-widget.component.ts", lineNumber: 39 });
})();

// src/app/nav/_components/grouped-typeahead/grouped-typeahead.component.ts
var _c03 = ["itemTemplate"];
var _c13 = ["seriesTemplate"];
var _c23 = ["collectionTemplate"];
var _c32 = ["tagTemplate"];
var _c42 = ["personTemplate"];
var _c5 = ["genreTemplate"];
var _c6 = ["noResultsTemplate"];
var _c7 = ["extraTemplate"];
var _c8 = ["libraryTemplate"];
var _c9 = ["readingListTemplate"];
var _c10 = ["fileTemplate"];
var _c11 = ["chapterTemplate"];
var _c122 = ["bookmarkTemplate"];
var _c132 = ["input"];
var _c14 = (a0) => ({ "focused": a0 });
var _c15 = (a0, a1) => ({ $implicit: a0, idx: a1 });
function GroupedTypeaheadComponent_ng_container_0_Conditional_6_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 7)(1, "span", 9);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("loading"));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_6_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 10);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_6_Conditional_1_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.resetField());
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275attribute("aria-label", t_r3("close"));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, GroupedTypeaheadComponent_ng_container_0_Conditional_6_Conditional_0_Template, 3, 1, "div", 7)(1, GroupedTypeaheadComponent_ng_container_0_Conditional_6_Conditional_1_Template, 1, 1, "button", 8);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275conditional(ctx_r1.isLoading ? 0 : 1);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_3_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 19);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_3_For_5_Template_li_click_0_listener() {
      const option_r6 = \u0275\u0275restoreView(_r5).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r6));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r6 = ctx.$implicit;
    const \u0275$index_38_r7 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.seriesTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r6, \u0275$index_38_r7));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5", 16);
    \u0275\u0275text(2, "Series");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 17);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_3_For_5_Template, 2, 5, "li", 18, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275advance(4);
    \u0275\u0275repeater(ctx_r1.groupedData.series);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_4_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_4_For_5_Template_li_click_0_listener() {
      const option_r9 = \u0275\u0275restoreView(_r8).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r9));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r9 = ctx.$implicit;
    const \u0275$index_51_r10 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.collectionTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r9, \u0275$index_51_r10));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_4_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("collections"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.collections);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_5_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_5_For_5_Template_li_click_0_listener() {
      const option_r12 = \u0275\u0275restoreView(_r11).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r12));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r12 = ctx.$implicit;
    const \u0275$index_64_r13 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.readingListTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r12, \u0275$index_64_r13));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_5_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("reading-lists"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.readingLists);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_6_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_6_For_5_Template_li_click_0_listener() {
      const option_r15 = \u0275\u0275restoreView(_r14).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r15));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r15 = ctx.$implicit;
    const \u0275$index_77_r16 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.bookmarkTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r15, \u0275$index_77_r16));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_6_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("bookmarks"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.bookmarks);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_7_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 26);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_7_For_5_Template_li_click_0_listener() {
      const option_r18 = \u0275\u0275restoreView(_r17).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r18));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r18 = ctx.$implicit;
    const \u0275$index_90_r19 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.libraryTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r18, \u0275$index_90_r19));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5", 23);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 24);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_7_For_5_Template, 2, 5, "li", 25, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("libraries"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.libraries);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_8_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r20 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_8_For_5_Template_li_click_0_listener() {
      const option_r21 = \u0275\u0275restoreView(_r20).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r21));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r21 = ctx.$implicit;
    const \u0275$index_103_r22 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.genreTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r21, \u0275$index_103_r22));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_8_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("genres"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.genres);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_9_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r23 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_9_For_5_Template_li_click_0_listener() {
      const option_r24 = \u0275\u0275restoreView(_r23).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r24));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r24 = ctx.$implicit;
    const \u0275$index_116_r25 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.tagTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r24, \u0275$index_116_r25));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_9_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("tags"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.tags);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_10_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r26 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_10_For_5_Template_li_click_0_listener() {
      const option_r27 = \u0275\u0275restoreView(_r26).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r27));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r27 = ctx.$implicit;
    const \u0275$index_129_r28 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.personTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r27, \u0275$index_129_r28));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_10_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("people"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.persons);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_11_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r29 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_11_For_5_Template_li_click_0_listener() {
      const option_r30 = \u0275\u0275restoreView(_r29).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r30));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r30 = ctx.$implicit;
    const \u0275$index_142_r31 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.chapterTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r30, \u0275$index_142_r31));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_11_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("chapters"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.chapters);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_12_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r32 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 22);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_12_For_5_Template_li_click_0_listener() {
      const option_r33 = \u0275\u0275restoreView(_r32).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleResultClick(option_r33));
    });
    \u0275\u0275elementContainer(1, 20);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const option_r33 = ctx.$implicit;
    const \u0275$index_155_r34 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.fileTemplate)("ngTemplateOutletContext", \u0275\u0275pureFunction2(2, _c15, option_r33, \u0275$index_155_r34));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "li", 15)(1, "h5");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "ul", 13);
    \u0275\u0275repeaterCreate(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_12_For_5_Template, 2, 5, "li", 21, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("files"));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupedData.files);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "ul", 13)(1, "li", 27);
    \u0275\u0275elementContainer(2, 28);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275advance(2);
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.noResultsTemplate);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r35 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "li", 29);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_14_Template_li_click_0_listener($event) {
      \u0275\u0275restoreView(_r35);
      return \u0275\u0275resetView($event.stopPropagation());
    });
    \u0275\u0275elementContainer(1, 28);
    \u0275\u0275elementStart(2, "form", 30)(3, "div", 31);
    \u0275\u0275element(4, "input", 32);
    \u0275\u0275elementStart(5, "label", 33);
    \u0275\u0275text(6);
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const t_r3 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("ngTemplateOutlet", ctx_r1.extraTemplate);
    \u0275\u0275advance();
    \u0275\u0275property("formGroup", ctx_r1.searchSettingsForm);
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(t_r3("include-extras"));
  }
}
function GroupedTypeaheadComponent_ng_container_0_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 6)(1, "div", 11)(2, "ul", 12);
    \u0275\u0275template(3, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_3_Template, 6, 0)(4, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_4_Template, 6, 1)(5, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_5_Template, 6, 1)(6, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_6_Template, 6, 1)(7, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_7_Template, 6, 1)(8, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_8_Template, 6, 1)(9, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_9_Template, 6, 1)(10, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_10_Template, 6, 1)(11, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_11_Template, 6, 1)(12, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_12_Template, 6, 1)(13, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_13_Template, 3, 1, "ul", 13)(14, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Conditional_14_Template, 7, 3, "li", 14);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.seriesTemplate !== void 0 && ctx_r1.groupedData.series.length > 0 ? 3 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.collectionTemplate !== void 0 && ctx_r1.groupedData.collections.length > 0 ? 4 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.readingListTemplate !== void 0 && ctx_r1.groupedData.readingLists.length > 0 ? 5 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.bookmarkTemplate !== void 0 && ctx_r1.groupedData.bookmarks.length > 0 ? 6 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.libraryTemplate !== void 0 && ctx_r1.groupedData.libraries.length > 0 ? 7 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.genreTemplate !== void 0 && ctx_r1.groupedData.genres.length > 0 ? 8 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.tagTemplate !== void 0 && ctx_r1.groupedData.tags.length > 0 ? 9 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.personTemplate !== void 0 && ctx_r1.groupedData.persons.length > 0 ? 10 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.chapterTemplate !== void 0 && ctx_r1.groupedData.chapters.length > 0 ? 11 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.fileTemplate !== void 0 && ctx_r1.groupedData.files.length > 0 ? 12 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(!ctx_r1.hasData && ctx_r1.searchTerm.length > 0 && !ctx_r1.isLoading ? 13 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.searchTerm.length > 0 && !ctx_r1.isLoading ? 14 : -1);
  }
}
function GroupedTypeaheadComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275elementStart(1, "form", 2)(2, "div", 3);
    \u0275\u0275listener("click", function GroupedTypeaheadComponent_ng_container_0_Template_div_click_2_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onInputFocus($event));
    });
    \u0275\u0275elementStart(3, "div", 4)(4, "input", 5, 0);
    \u0275\u0275listener("focusout", function GroupedTypeaheadComponent_ng_container_0_Template_input_focusout_4_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.close($event));
    })("focus", function GroupedTypeaheadComponent_ng_container_0_Template_input_focus_4_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.open($event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275template(6, GroupedTypeaheadComponent_ng_container_0_Conditional_6_Template, 2, 1);
    \u0275\u0275elementEnd()();
    \u0275\u0275template(7, GroupedTypeaheadComponent_ng_container_0_Conditional_7_Template, 15, 12, "div", 6);
    \u0275\u0275elementEnd();
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("formGroup", ctx_r1.typeaheadForm);
    \u0275\u0275advance();
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(7, _c14, ctx_r1.hasFocus));
    \u0275\u0275advance(2);
    \u0275\u0275property("id", ctx_r1.id)("placeholder", ctx_r1.placeholder);
    \u0275\u0275attribute("aria-expanded", ctx_r1.hasFocus && ctx_r1.hasData);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.searchTerm.length > 0 ? 6 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.hasFocus ? 7 : -1);
  }
}
var GroupedTypeaheadComponent = class _GroupedTypeaheadComponent {
  constructor() {
    this.destroyRef = inject(DestroyRef);
    this.cdRef = inject(ChangeDetectorRef);
    this.accountService = inject(AccountService);
    this.id = "grouped-typeahead";
    this.minQueryLength = 0;
    this.initialValue = "";
    this.groupedData = new SearchResultGroup();
    this.placeholder = "";
    this.isLoading = false;
    this.debounceTime = 200;
    this.inputChanged = new EventEmitter();
    this.selected = new EventEmitter();
    this.clearField = new EventEmitter();
    this.focusChanged = new EventEmitter();
    this.hasFocus = false;
    this.typeaheadForm = new FormGroup({});
    this.includeChapterAndFiles = false;
    this.prevSearchTerm = "";
    this.searchSettingsForm = new FormGroup({ "includeExtras": new FormControl(false) });
  }
  get searchTerm() {
    return this.typeaheadForm.get("typeahead")?.value || "";
  }
  get hasData() {
    return !(this.noResultsTemplate != void 0 && !this.groupedData.persons.length && !this.groupedData.collections.length && !this.groupedData.series.length && !this.groupedData.persons.length && !this.groupedData.tags.length && !this.groupedData.genres.length && !this.groupedData.libraries.length && !this.groupedData.files.length && !this.groupedData.chapters.length && !this.groupedData.bookmarks.length);
  }
  handleDocumentClick(event) {
    this.close();
  }
  handleKeyPress(event) {
    if (!this.hasFocus) {
      return;
    }
    switch (event.key) {
      case KEY_CODES.ESC_KEY:
        this.close();
        event.stopPropagation();
        break;
      default:
        break;
    }
  }
  ngOnInit() {
    this.typeaheadForm.addControl("typeahead", new FormControl(this.initialValue, []));
    this.cdRef.markForCheck();
    this.searchSettingsForm.get("includeExtras").valueChanges.pipe(startWith(false), map((val) => {
      if (val === null)
        return false;
      return val;
    }), distinctUntilChanged(), tap((val) => this.toggleIncludeFiles(val)), takeUntilDestroyed(this.destroyRef)).subscribe();
    this.typeaheadForm.valueChanges.pipe(debounceTime(this.debounceTime), takeUntilDestroyed(this.destroyRef)).subscribe((change) => {
      const value = this.typeaheadForm.get("typeahead")?.value;
      if (value != void 0 && value != "" && !this.hasFocus) {
        this.hasFocus = true;
        this.cdRef.markForCheck();
      }
      if (value != void 0 && value.length >= this.minQueryLength) {
        if (this.prevSearchTerm === value)
          return;
        this.inputChanged.emit({ value, includeFiles: this.includeChapterAndFiles });
        this.prevSearchTerm = value;
        this.cdRef.markForCheck();
      }
    });
  }
  onInputFocus(event) {
    if (event) {
      event.stopPropagation();
      event.preventDefault();
    }
    this.openDropdown();
    return this.hasFocus;
  }
  openDropdown() {
    setTimeout(() => {
      const model = this.typeaheadForm.get("typeahead");
      if (model) {
        model.setValue(model.value);
      }
    });
  }
  handleResultClick(item) {
    this.selected.emit(item);
  }
  toggleIncludeFiles(val) {
    const firstRun = !val && val === this.includeChapterAndFiles;
    this.includeChapterAndFiles = val;
    this.inputChanged.emit({ value: this.searchTerm, includeFiles: this.includeChapterAndFiles });
    if (!firstRun) {
      this.hasFocus = true;
      if (this.inputElem && this.inputElem.nativeElement) {
        this.inputElem.nativeElement.focus();
      }
      this.openDropdown();
    }
    this.cdRef.markForCheck();
  }
  resetField() {
    this.prevSearchTerm = "";
    this.typeaheadForm.get("typeahead")?.setValue(this.initialValue);
    this.clearField.emit();
    this.cdRef.markForCheck();
  }
  close(event) {
    if (event) {
      if (this.hasData || this.searchTerm) {
        return;
      }
    }
    if (this.searchTerm === "") {
      this.resetField();
    }
    this.hasFocus = false;
    this.cdRef.markForCheck();
    this.focusChanged.emit(this.hasFocus);
  }
  open(event) {
    this.hasFocus = true;
    this.focusChanged.emit(this.hasFocus);
    this.cdRef.markForCheck();
  }
  clear() {
    this.prevSearchTerm = "";
    this.typeaheadForm.get("typeahead")?.setValue(this.initialValue);
    this.cdRef.markForCheck();
  }
  static {
    this.\u0275fac = function GroupedTypeaheadComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GroupedTypeaheadComponent)();
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _GroupedTypeaheadComponent, selectors: [["app-grouped-typeahead"]], contentQueries: function GroupedTypeaheadComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        \u0275\u0275contentQuery(dirIndex, _c03, 5);
        \u0275\u0275contentQuery(dirIndex, _c13, 5);
        \u0275\u0275contentQuery(dirIndex, _c23, 5);
        \u0275\u0275contentQuery(dirIndex, _c32, 5);
        \u0275\u0275contentQuery(dirIndex, _c42, 5);
        \u0275\u0275contentQuery(dirIndex, _c5, 5);
        \u0275\u0275contentQuery(dirIndex, _c6, 5);
        \u0275\u0275contentQuery(dirIndex, _c7, 5);
        \u0275\u0275contentQuery(dirIndex, _c8, 5);
        \u0275\u0275contentQuery(dirIndex, _c9, 5);
        \u0275\u0275contentQuery(dirIndex, _c10, 5);
        \u0275\u0275contentQuery(dirIndex, _c11, 5);
        \u0275\u0275contentQuery(dirIndex, _c122, 5);
      }
      if (rf & 2) {
        let _t;
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.itemTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.seriesTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.collectionTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.tagTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.personTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.genreTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.noResultsTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.extraTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.libraryTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.readingListTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.fileTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.chapterTemplate = _t.first);
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.bookmarkTemplate = _t.first);
      }
    }, viewQuery: function GroupedTypeaheadComponent_Query(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275viewQuery(_c132, 5);
      }
      if (rf & 2) {
        let _t;
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.inputElem = _t.first);
      }
    }, hostBindings: function GroupedTypeaheadComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("click", function GroupedTypeaheadComponent_click_HostBindingHandler($event) {
          return ctx.handleDocumentClick($event);
        }, false, \u0275\u0275resolveWindow)("keydown", function GroupedTypeaheadComponent_keydown_HostBindingHandler($event) {
          return ctx.handleKeyPress($event);
        }, false, \u0275\u0275resolveWindow);
      }
    }, inputs: { id: "id", minQueryLength: "minQueryLength", initialValue: "initialValue", groupedData: "groupedData", placeholder: "placeholder", isLoading: "isLoading", debounceTime: "debounceTime" }, outputs: { inputChanged: "inputChanged", selected: "selected", clearField: "clearField", focusChanged: "focusChanged" }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [["input", ""], [4, "transloco", "translocoRead"], [1, "grouped-typeahead", 3, "formGroup"], [1, "typeahead-input", 3, "click", "ngClass"], [1, "search"], ["type", "text", "inputmode", "search", "autocomplete", "off", "formControlName", "typeahead", "aria-haspopup", "listbox", "aria-owns", "dropdown", "aria-autocomplete", "list", "role", "searchbox", 3, "focusout", "focus", "id", "placeholder"], [1, "overlay"], ["role", "status", 1, "spinner-border", "spinner-border-sm"], ["type", "button", 1, "btn-close"], [1, "visually-hidden"], ["type", "button", 1, "btn-close", 3, "click"], [1, "dropdown"], ["role", "listbox", "id", "dropdown", 1, "list-group"], [1, "list-group", "results"], [1, "list-group-item", 2, "min-height", "34px"], [1, "list-group-item", "section-header"], ["id", "series-group"], ["role", "group", "aria-describedby", "series-group", 1, "list-group", "results"], ["tabindex", "0", "aria-labelledby", "series-group", "role", "option", 1, "list-group-item"], ["tabindex", "0", "aria-labelledby", "series-group", "role", "option", 1, "list-group-item", 3, "click"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], ["tabindex", "0", "role", "option", 1, "list-group-item"], ["tabindex", "0", "role", "option", 1, "list-group-item", 3, "click"], ["id", "libraries-group"], ["role", "group", "aria-describedby", "libraries-group", 1, "list-group", "results"], ["tabindex", "0", "aria-labelledby", "libraries-group", "role", "option", 1, "list-group-item"], ["tabindex", "0", "aria-labelledby", "libraries-group", "role", "option", 1, "list-group-item", 3, "click"], [1, "list-group-item"], [3, "ngTemplateOutlet"], [1, "list-group-item", 2, "min-height", "34px", 3, "click"], [3, "formGroup"], [1, "form-check", "form-switch"], ["type", "checkbox", "id", "search-include-extras", "role", "switch", "formControlName", "includeExtras", "aria-labelledby", "auto-close-label", "aria-describedby", "tag-promoted-help", 1, "form-check-input"], ["for", "search-include-extras", 1, "form-check-label"]], template: function GroupedTypeaheadComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, GroupedTypeaheadComponent_ng_container_0_Template, 8, 9, "ng-container", 1);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "grouped-typeahead");
      }
    }, dependencies: [ReactiveFormsModule, \u0275NgNoValidate, DefaultValueAccessor, CheckboxControlValueAccessor, NgControlStatus, NgControlStatusGroup, FormGroupDirective, FormControlName, NgClass, NgTemplateOutlet, TranslocoDirective], styles: ["\n\n.search-result[_ngcontent-%COMP%]   img[_ngcontent-%COMP%] {\n  width: 100% !important;\n}\n.btn-close[_ngcontent-%COMP%] {\n  margin-top: 8px;\n  font-size: 0.8rem;\n  position: absolute;\n  right: 5px;\n}\n.typeahead-input[_ngcontent-%COMP%] {\n  border: 1px solid transparent;\n  border-radius: 4px;\n  overflow: hidden;\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  box-shadow: none;\n  cursor: text;\n  background-color: var(--searchbar-bg-color);\n  color: var(--body-text-color);\n  min-height: 32px;\n  transition-property: all;\n  transition-duration: 0.3s;\n  display: block;\n  width: 50%;\n}\n.typeahead-input[_ngcontent-%COMP%]   .search[_ngcontent-%COMP%] {\n  display: flex;\n  position: relative;\n  min-height: 32px;\n}\n.typeahead-input[_ngcontent-%COMP%]   .search[_ngcontent-%COMP%]   .btn-close[_ngcontent-%COMP%] {\n  position: unset;\n  margin-top: unset;\n  align-self: center;\n  margin: 0 8px;\n}\n.typeahead-input[_ngcontent-%COMP%]   input[_ngcontent-%COMP%] {\n  width: 100%;\n  padding: 0px 6px;\n  outline: 0 !important;\n  border-radius: 0.28571429rem;\n  margin: 0px !important;\n  text-indent: 0 !important;\n  line-height: inherit !important;\n  box-shadow: none !important;\n  transition-property: all;\n  transition-duration: 0.3s;\n  display: block;\n  opacity: 1;\n  position: relative;\n  left: 4px;\n  border: none;\n  background-color: transparent;\n}\n.typeahead-input[_ngcontent-%COMP%]   input[_ngcontent-%COMP%]:focus-visible {\n  width: 100%;\n}\n.typeahead-input.focused[_ngcontent-%COMP%] {\n  width: 100%;\n  border-color: var(--primary-color);\n}\n.typeahead-input.focused[_ngcontent-%COMP%]   input[_ngcontent-%COMP%] {\n  color: #fff;\n}\n@media only screen and (max-width: 980px) {\n  .typeahead-input[_ngcontent-%COMP%] {\n    width: 65%;\n  }\n}\n@media only screen and (max-width: 650px) {\n  .typeahead-input[_ngcontent-%COMP%] {\n    width: 75%;\n    margin: 0 auto;\n  }\n}\n@media only screen and (max-width: 500px) {\n  .typeahead-input[_ngcontent-%COMP%] {\n    width: 100%;\n  }\n}\n.section-header[_ngcontent-%COMP%] {\n  color: var(--body-text-color);\n}\n.section-header[_ngcontent-%COMP%]:hover {\n  background-color: var(--search-list-group-item-bg-color, black) !important;\n  cursor: default;\n}\n.list-group-item[_ngcontent-%COMP%] {\n  background-color: var(--search-list-group-item-bg-color, black);\n}\n.dropdown[_ngcontent-%COMP%] {\n  width: 100vw;\n  top: 55px;\n  position: fixed;\n  justify-content: center;\n  left: 0;\n  overflow-y: auto;\n  display: flex;\n  flex-wrap: wrap;\n  margin-top: 9px;\n}\n.list-group[_ngcontent-%COMP%] {\n  max-width: 600px;\n  z-index: 1000;\n  overflow-y: auto;\n  overflow-x: hidden;\n  display: block;\n  flex: auto;\n  max-height: calc(var(--vh) * 100 - var(--nav-offset));\n  height: fit-content;\n}\n.list-group.results[_ngcontent-%COMP%] {\n  max-height: unset;\n}\n@media only screen and (max-width: 600px) {\n  .list-group[_ngcontent-%COMP%] {\n    max-width: unset;\n  }\n}\n.list-group-item[_ngcontent-%COMP%] {\n  padding: 5px 10px;\n}\nli[_ngcontent-%COMP%] {\n  list-style: none;\n  border-radius: 0px !important;\n  margin: 0 !important;\n}\nul[_ngcontent-%COMP%]   ul[_ngcontent-%COMP%] {\n  border-radius: 0px !important;\n}\n.spinner-border[_ngcontent-%COMP%] {\n  position: absolute;\n  right: 10px;\n  margin: auto;\n  cursor: pointer;\n  top: 30%;\n}\n.overlay[_ngcontent-%COMP%] {\n  position: fixed;\n  top: 0;\n  left: 0;\n  background: rgba(0, 0, 0, 0.4);\n  width: 100vw;\n  height: 100vh;\n}\n/*# sourceMappingURL=grouped-typeahead.component.css.map */"], changeDetection: 0 });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(GroupedTypeaheadComponent, { className: "GroupedTypeaheadComponent", filePath: "src/app/nav/_components/grouped-typeahead/grouped-typeahead.component.ts", lineNumber: 40 });
})();

// src/app/nav/_components/nav-link-modal/nav-link-modal.component.ts
function NavLinkModalComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275elementStart(1, "div", 1)(2, "h4", 2);
    \u0275\u0275text(3);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "button", 3);
    \u0275\u0275listener("click", function NavLinkModalComponent_ng_container_0_Template_button_click_4_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.close());
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(5, "div", 4)(6, "div", 5)(7, "a", 6);
    \u0275\u0275listener("click", function NavLinkModalComponent_ng_container_0_Template_a_click_7_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.closeIfOnSettings());
    });
    \u0275\u0275text(8);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(9, "div", 5)(10, "a", 7);
    \u0275\u0275text(11);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(12, "div", 5)(13, "a", 8);
    \u0275\u0275text(14);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(15, "div", 5)(16, "a", 9);
    \u0275\u0275text(17);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(18, "div", 5)(19, "a", 10);
    \u0275\u0275listener("click", function NavLinkModalComponent_ng_container_0_Template_a_click_19_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.logout());
    });
    \u0275\u0275text(20);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(21, "div", 11)(22, "button", 12);
    \u0275\u0275listener("click", function NavLinkModalComponent_ng_container_0_Template_button_click_22_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.close());
    });
    \u0275\u0275text(23);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    const t_r3 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("nav-link-header"));
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-label", t_r3("close"));
    \u0275\u0275advance(3);
    \u0275\u0275property("fragment", ctx_r1.SettingsTabId.Preferences)("title", t_r3("settings"));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(t_r3("settings"));
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("all-filters"));
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("announcements"));
    \u0275\u0275advance(2);
    \u0275\u0275property("href", ctx_r1.WikiLink.Guides, \u0275\u0275sanitizeUrl);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(t_r3("help"));
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("logout"));
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("close"));
  }
}
var NavLinkModalComponent = class _NavLinkModalComponent {
  constructor() {
    this.modal = inject(NgbActiveModal);
    this.router = inject(Router);
    this.WikiLink = WikiLink;
    this.SettingsTabId = SettingsTabId;
  }
  close() {
    this.modal.close();
  }
  logout() {
    this.logoutFn();
  }
  closeIfOnSettings() {
    setTimeout(() => {
      const currentUrl = this.router.url;
      if (currentUrl.startsWith("/settings")) {
        this.close();
      }
    }, 10);
  }
  static {
    this.\u0275fac = function NavLinkModalComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavLinkModalComponent)();
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _NavLinkModalComponent, selectors: [["app-nav-link-modal"]], inputs: { logoutFn: "logoutFn" }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [[4, "transloco", "translocoRead"], [1, "modal-header"], ["id", "modal-basic-title", 1, "modal-title"], ["type", "button", 1, "btn-close", 3, "click"], [1, "modal-body"], [1, "mb-3"], ["routerLink", "/settings", 3, "click", "fragment", "title"], ["routerLink", "/all-filters/"], ["routerLink", "/announcements/"], ["rel", "noopener noreferrer", "target", "_blank", 3, "href"], ["href", "javascript:void(0);", 3, "click"], [1, "modal-footer"], ["type", "button", 1, "btn", "btn-primary", 3, "click"]], template: function NavLinkModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, NavLinkModalComponent_ng_container_0_Template, 24, 11, "ng-container", 0);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "nav-header");
      }
    }, dependencies: [
      RouterLink,
      ReactiveFormsModule,
      Select2Module,
      TranslocoDirective
    ] });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(NavLinkModalComponent, { className: "NavLinkModalComponent", filePath: "src/app/nav/_components/nav-link-modal/nav-link-modal.component.ts", lineNumber: 25 });
})();

// src/app/nav/_components/nav-header/nav-header.component.ts
var _c04 = ["search"];
var _c16 = () => ({ "background": "none", "max-height": "24px", "height": "24px", "width": "24px", "border-radius": "50%" });
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "a", 22);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_4_Template_a_click_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.toggleSideNav($event));
    });
    \u0275\u0275element(1, "i", 23);
    \u0275\u0275elementEnd();
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_6_Template_div_click_0_listener() {
      const item_r6 = \u0275\u0275restoreView(_r5).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickLibraryResult(item_r6));
    });
    \u0275\u0275elementStart(1, "div", 28)(2, "span");
    \u0275\u0275text(3);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const item_r6 = ctx.$implicit;
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(item_r6.name);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Conditional_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span");
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const item_r8 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(item_r8.name);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Conditional_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "span", 33);
  }
  if (rf & 2) {
    const item_r8 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275property("innerHTML", item_r8.localizedName, \u0275\u0275sanitizeHtml);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Conditional_5_Conditional_0_Template, 2, 1, "span")(1, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Conditional_5_Conditional_1_Template, 1, 1, "span", 33);
  }
  if (rf & 2) {
    const item_r8 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275conditional(item_r8.name.toLowerCase().trim().indexOf(ctx) >= 0 ? 0 : 1);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Template_div_click_0_listener() {
      const item_r8 = \u0275\u0275restoreView(_r7).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickSeriesSearchResult(item_r8));
    });
    \u0275\u0275elementStart(1, "div", 29);
    \u0275\u0275element(2, "app-image", 30);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 28);
    \u0275\u0275element(4, "app-series-format", 31);
    \u0275\u0275template(5, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Conditional_5_Template, 2, 1);
    \u0275\u0275elementStart(6, "div", 32);
    \u0275\u0275text(7);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    let tmp_21_0;
    const item_r8 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance(2);
    \u0275\u0275property("imageUrl", ctx_r1.imageService.getSeriesCoverImage(item_r8.seriesId));
    \u0275\u0275advance(2);
    \u0275\u0275property("format", item_r8.format);
    \u0275\u0275advance();
    \u0275\u0275conditional((tmp_21_0 = ctx_r1.searchTerm.toLowerCase().trim()) ? 5 : -1, tmp_21_0);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("in ", item_r8.libraryName, "");
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Conditional_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span");
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const item_r10 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(item_r10.seriesName);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Conditional_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "span", 33);
  }
  if (rf & 2) {
    const item_r10 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275property("innerHTML", item_r10.localizedSeriesName, \u0275\u0275sanitizeHtml);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Conditional_5_Conditional_0_Template, 2, 1, "span")(1, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Conditional_5_Conditional_1_Template, 1, 1, "span", 33);
  }
  if (rf & 2) {
    const item_r10 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275conditional(item_r10.seriesName.toLowerCase().trim().indexOf(ctx) >= 0 ? 0 : 1);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Template_div_click_0_listener() {
      const item_r10 = \u0275\u0275restoreView(_r9).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickBookmarkSearchResult(item_r10));
    });
    \u0275\u0275elementStart(1, "div", 29);
    \u0275\u0275element(2, "app-image", 30);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 28);
    \u0275\u0275element(4, "app-series-format", 31);
    \u0275\u0275template(5, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Conditional_5_Template, 2, 1);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_21_0;
    const item_r10 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance(2);
    \u0275\u0275property("imageUrl", ctx_r1.imageService.getSeriesCoverImage(item_r10.seriesId));
    \u0275\u0275advance(2);
    \u0275\u0275property("format", item_r10.format);
    \u0275\u0275advance();
    \u0275\u0275conditional((tmp_21_0 = ctx_r1.searchTerm.toLowerCase().trim()) ? 5 : -1, tmp_21_0);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_12_Template_div_click_0_listener() {
      const item_r12 = \u0275\u0275restoreView(_r11).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickCollectionSearchResult(item_r12));
    });
    \u0275\u0275elementStart(1, "div", 29);
    \u0275\u0275element(2, "app-image", 30);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 28)(4, "div")(5, "span");
    \u0275\u0275text(6);
    \u0275\u0275elementEnd();
    \u0275\u0275element(7, "app-promoted-icon", 34);
    \u0275\u0275elementEnd();
    \u0275\u0275element(8, "app-collection-owner", 35);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const item_r12 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance(2);
    \u0275\u0275property("imageUrl", ctx_r1.imageService.getCollectionCoverImage(item_r12.id));
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(item_r12.title);
    \u0275\u0275advance();
    \u0275\u0275property("promoted", item_r12.promoted);
    \u0275\u0275advance();
    \u0275\u0275property("collection", item_r12);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_14_Template_div_click_0_listener() {
      const item_r14 = \u0275\u0275restoreView(_r13).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickReadingListSearchResult(item_r14));
    });
    \u0275\u0275elementStart(1, "div", 28)(2, "span");
    \u0275\u0275text(3);
    \u0275\u0275elementEnd();
    \u0275\u0275element(4, "app-promoted-icon", 34);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const item_r14 = ctx.$implicit;
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(item_r14.title);
    \u0275\u0275advance();
    \u0275\u0275property("promoted", item_r14.promoted);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_16_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_16_Template_div_click_0_listener() {
      const item_r16 = \u0275\u0275restoreView(_r15).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.goToOther(ctx_r1.FilterField.Tags, item_r16.id));
    });
    \u0275\u0275elementStart(1, "div", 28)(2, "span");
    \u0275\u0275text(3);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const item_r16 = ctx.$implicit;
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(item_r16.title);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_18_Template_div_click_0_listener() {
      const item_r18 = \u0275\u0275restoreView(_r17).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.goToPerson(item_r18));
    });
    \u0275\u0275elementStart(1, "div", 29);
    \u0275\u0275element(2, "app-image", 36);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 28)(4, "div");
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const item_r18 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance(2);
    \u0275\u0275property("styles", \u0275\u0275pureFunction0(4, _c16))("imageUrl", ctx_r1.imageService.getPersonImage(item_r18.id))("errorImage", ctx_r1.imageService.noPersonImage);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(item_r18.name);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_20_Template(rf, ctx) {
  if (rf & 1) {
    const _r19 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_20_Template_div_click_0_listener() {
      const item_r20 = \u0275\u0275restoreView(_r19).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.goToOther(ctx_r1.FilterField.Genres, item_r20.id));
    });
    \u0275\u0275elementStart(1, "div", 28);
    \u0275\u0275element(2, "div", 33);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const item_r20 = ctx.$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275property("innerHTML", item_r20.title, \u0275\u0275sanitizeHtml);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_22_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-series-format", 31);
  }
  if (rf & 2) {
    const item_r22 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275property("format", item_r22.files == null ? null : item_r22.files[0].format);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_22_Template(rf, ctx) {
  if (rf & 1) {
    const _r21 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_22_Template_div_click_0_listener() {
      const item_r22 = \u0275\u0275restoreView(_r21).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickChapterSearchResult(item_r22));
    });
    \u0275\u0275elementStart(1, "div", 28);
    \u0275\u0275template(2, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_22_Conditional_2_Template, 1, 1, "app-series-format", 31);
    \u0275\u0275elementStart(3, "span");
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const item_r22 = ctx.$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275conditional(item_r22.files.length > 0 ? 2 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(item_r22.titleName || item_r22.range);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_24_Template(rf, ctx) {
  if (rf & 1) {
    const _r23 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 27);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_24_Template_div_click_0_listener() {
      const item_r24 = \u0275\u0275restoreView(_r23).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clickFileSearchResult(item_r24));
    });
    \u0275\u0275elementStart(1, "div", 28);
    \u0275\u0275element(2, "app-series-format", 31);
    \u0275\u0275elementStart(3, "span");
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const item_r24 = ctx.$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275property("format", item_r24.format);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(item_r24.filePath);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_26_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275text(0);
  }
  if (rf & 2) {
    const t_r25 = \u0275\u0275nextContext(3).$implicit;
    \u0275\u0275textInterpolate1(" ", t_r25("no-data"), " ");
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 21)(1, "label", 24);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 25)(4, "app-grouped-typeahead", 26, 0);
    \u0275\u0275listener("inputChanged", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_Template_app_grouped_typeahead_inputChanged_4_listener($event) {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.onChangeSearch($event));
    })("clearField", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_Template_app_grouped_typeahead_clearField_4_listener() {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.clearSearch());
    })("focusChanged", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_Template_app_grouped_typeahead_focusChanged_4_listener($event) {
      \u0275\u0275restoreView(_r4);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.focusUpdate($event));
    });
    \u0275\u0275template(6, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_6_Template, 4, 1, "ng-template", null, 1, \u0275\u0275templateRefExtractor)(8, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_8_Template, 8, 4, "ng-template", null, 2, \u0275\u0275templateRefExtractor)(10, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_10_Template, 6, 3, "ng-template", null, 3, \u0275\u0275templateRefExtractor)(12, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_12_Template, 9, 4, "ng-template", null, 4, \u0275\u0275templateRefExtractor)(14, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_14_Template, 5, 2, "ng-template", null, 5, \u0275\u0275templateRefExtractor)(16, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_16_Template, 4, 1, "ng-template", null, 6, \u0275\u0275templateRefExtractor)(18, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_18_Template, 6, 5, "ng-template", null, 7, \u0275\u0275templateRefExtractor)(20, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_20_Template, 3, 1, "ng-template", null, 8, \u0275\u0275templateRefExtractor)(22, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_22_Template, 5, 2, "ng-template", null, 9, \u0275\u0275templateRefExtractor)(24, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_24_Template, 5, 2, "ng-template", null, 10, \u0275\u0275templateRefExtractor)(26, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_ng_template_26_Template, 1, 1, "ng-template", null, 11, \u0275\u0275templateRefExtractor);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const t_r25 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r25("search-series-alt"));
    \u0275\u0275advance(2);
    \u0275\u0275property("minQueryLength", 2)("isLoading", ctx_r1.isLoading)("placeholder", t_r25("search-alt"))("groupedData", ctx_r1.searchResults);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_14_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r26 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 37)(1, "button", 38);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_14_Conditional_0_Template_button_click_1_listener() {
      \u0275\u0275restoreView(_r26);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.scrollToTop());
    });
    \u0275\u0275element(2, "i", 39);
    \u0275\u0275elementStart(3, "span", 40);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const t_r25 = \u0275\u0275nextContext(3).$implicit;
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(t_r25("scroll-to-top-alt"));
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_14_Conditional_0_Template, 5, 1, "div", 37);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275conditional(ctx_r1.backToTopNeeded ? 0 : -1);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r27 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 44);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Conditional_2_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r27);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.openLinkSelectionMenu());
    });
    \u0275\u0275element(1, "i", 45);
    \u0275\u0275elementStart(2, "span", 46);
    \u0275\u0275text(3);
    \u0275\u0275pipe(4, "sentenceCase");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const user_r28 = \u0275\u0275nextContext();
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(\u0275\u0275pipeBind1(4, 1, user_r28.username));
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r29 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 47)(1, "a", 48);
    \u0275\u0275element(2, "i", 49);
    \u0275\u0275elementStart(3, "span", 40);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(5, "div", 50)(6, "button", 51);
    \u0275\u0275element(7, "i", 45);
    \u0275\u0275elementStart(8, "span", 46);
    \u0275\u0275text(9);
    \u0275\u0275pipe(10, "sentenceCase");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(11, "div", 52)(12, "a", 53);
    \u0275\u0275text(13);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "a", 54);
    \u0275\u0275text(15);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(16, "a", 55);
    \u0275\u0275text(17);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "a", 56);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Conditional_4_Template_a_click_18_listener() {
      \u0275\u0275restoreView(_r29);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.logout());
    });
    \u0275\u0275text(19);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const user_r28 = \u0275\u0275nextContext();
    const t_r25 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("fragment", ctx_r1.SettingsTabId.Preferences)("title", t_r25("settings"));
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r25("settings"));
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate(\u0275\u0275pipeBind1(10, 9, user_r28.username));
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(t_r25("all-filters"));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r25("announcements"));
    \u0275\u0275advance();
    \u0275\u0275property("href", ctx_r1.WikiLink.Guides, \u0275\u0275sanitizeUrl);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(t_r25("help"));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r25("logout"));
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 41);
    \u0275\u0275element(1, "app-nav-events-toggle", 42);
    \u0275\u0275elementEnd();
    \u0275\u0275template(2, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Conditional_2_Template, 5, 3, "button", 43);
    \u0275\u0275pipe(3, "async");
    \u0275\u0275template(4, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Conditional_4_Template, 20, 11);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275advance();
    \u0275\u0275property("user", ctx);
    \u0275\u0275advance();
    \u0275\u0275conditional(\u0275\u0275pipeBind1(3, 2, ctx_r1.breakpoint$) <= ctx_r1.Breakpoint.Mobile ? 2 : 4);
  }
}
function NavHeaderComponent_ng_container_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "nav", 13)(1, "div", 14)(2, "a", 15);
    \u0275\u0275listener("click", function NavHeaderComponent_ng_container_0_Conditional_1_Template_a_click_2_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.moveFocus());
    });
    \u0275\u0275text(3);
    \u0275\u0275elementEnd();
    \u0275\u0275template(4, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_4_Template, 2, 0, "a", 16);
    \u0275\u0275pipe(5, "async");
    \u0275\u0275elementStart(6, "a", 17);
    \u0275\u0275element(7, "app-image", 18);
    \u0275\u0275elementStart(8, "div", 19)(9, "span");
    \u0275\u0275text(10, "Kavita");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(11, "ul", 20);
    \u0275\u0275template(12, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_12_Template, 28, 5, "div", 21);
    \u0275\u0275pipe(13, "async");
    \u0275\u0275elementEnd();
    \u0275\u0275template(14, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_14_Template, 1, 1);
    \u0275\u0275pipe(15, "async");
    \u0275\u0275template(16, NavHeaderComponent_ng_container_0_Conditional_1_Conditional_16_Template, 5, 4);
    \u0275\u0275pipe(17, "async");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_5_0;
    let tmp_7_0;
    const t_r25 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r25("skip-alt"));
    \u0275\u0275advance();
    \u0275\u0275conditional(\u0275\u0275pipeBind1(5, 5, ctx_r1.navService.sideNavVisibility$) ? 4 : -1);
    \u0275\u0275advance(8);
    \u0275\u0275conditional((tmp_5_0 = \u0275\u0275pipeBind1(13, 7, ctx_r1.accountService.currentUser$)) ? 12 : -1, tmp_5_0);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(\u0275\u0275pipeBind1(15, 9, ctx_r1.breakpoint$) > ctx_r1.Breakpoint.Tablet ? 14 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_7_0 = \u0275\u0275pipeBind1(17, 11, ctx_r1.accountService.currentUser$)) ? 16 : -1, tmp_7_0);
  }
}
function NavHeaderComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275template(1, NavHeaderComponent_ng_container_0_Conditional_1_Template, 18, 13, "nav", 13);
    \u0275\u0275pipe(2, "async");
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275conditional(\u0275\u0275pipeBind1(2, 1, ctx_r1.navService.navbarVisible$) ? 1 : -1);
  }
}
var NavHeaderComponent = class _NavHeaderComponent {
  onResize() {
    this.breakpointSource.next(this.utilityService.getActiveBreakpoint());
  }
  constructor(document2) {
    this.document = document2;
    this.router = inject(Router);
    this.scrollService = inject(ScrollService);
    this.searchService = inject(SearchService);
    this.filterUtilityService = inject(FilterUtilitiesService);
    this.accountService = inject(AccountService);
    this.cdRef = inject(ChangeDetectorRef);
    this.destroyRef = inject(DestroyRef);
    this.navService = inject(NavService);
    this.imageService = inject(ImageService);
    this.utilityService = inject(UtilityService);
    this.modalService = inject(NgbModal);
    this.FilterField = FilterField;
    this.WikiLink = WikiLink;
    this.ScrobbleProvider = ScrobbleProvider;
    this.SettingsTabId = SettingsTabId;
    this.Breakpoint = Breakpoint;
    this.isLoading = false;
    this.debounceTime = 300;
    this.searchResults = new SearchResultGroup();
    this.searchTerm = "";
    this.backToTopNeeded = false;
    this.searchFocused = false;
    this.breakpointSource = new BehaviorSubject(this.utilityService.getActiveBreakpoint());
    this.breakpoint$ = this.breakpointSource.asObservable();
    this.scrollElem = this.document.body;
  }
  ngOnInit() {
    this.scrollService.scrollContainer$.pipe(distinctUntilChanged(), takeUntilDestroyed(this.destroyRef), tap((scrollContainer) => {
      if (scrollContainer === "body" || scrollContainer === void 0) {
        this.scrollElem = this.document.body;
      } else {
        const elem = scrollContainer;
        this.scrollElem = elem.nativeElement;
      }
      fromEvent(this.scrollElem, "scroll").pipe(debounceTime(20)).subscribe(() => this.checkBackToTopNeeded(this.scrollElem));
    })).subscribe();
    this.router.events.pipe(filter((event) => event instanceof NavigationEnd)).subscribe(() => {
      this.checkBackToTopNeeded(this.scrollElem);
    });
  }
  checkBackToTopNeeded(elem) {
    const offset = elem.scrollTop || 0;
    if (offset > 100) {
      this.backToTopNeeded = true;
    } else if (offset < 40) {
      this.backToTopNeeded = false;
    }
    this.cdRef.markForCheck();
  }
  logout() {
    this.accountService.logout();
    this.navService.hideNavBar();
    this.navService.hideSideNav();
    this.router.navigateByUrl("/login");
  }
  moveFocus() {
    this.document.getElementById("content")?.focus();
  }
  onChangeSearch(evt) {
    this.isLoading = true;
    this.searchTerm = evt.value.trim();
    this.cdRef.markForCheck();
    this.searchService.search(this.searchTerm, evt.includeFiles).pipe(takeUntilDestroyed(this.destroyRef)).subscribe((results) => {
      this.searchResults = results;
      this.isLoading = false;
      this.cdRef.markForCheck();
    }, () => {
      this.searchResults.reset();
      this.isLoading = false;
      this.searchTerm = "";
      this.cdRef.markForCheck();
    });
  }
  goTo(statement) {
    let params = {};
    const filter2 = this.filterUtilityService.createSeriesV2Filter();
    filter2.statements = [statement];
    params["page"] = 1;
    this.clearSearch();
    this.filterUtilityService.applyFilterWithParams(["all-series"], filter2, params).subscribe();
  }
  goToOther(field, value) {
    this.goTo({ field, comparison: FilterComparison.Equal, value: value + "" });
  }
  goToPerson(person) {
    this.clearSearch();
    this.router.navigate(["person", person.name]);
  }
  clearSearch() {
    this.searchViewRef.clear();
    this.searchTerm = "";
    this.searchResults = new SearchResultGroup();
    this.cdRef.markForCheck();
  }
  clickSeriesSearchResult(item) {
    this.clearSearch();
    const libraryId = item.libraryId;
    const seriesId = item.seriesId;
    this.router.navigate(["library", libraryId, "series", seriesId]);
  }
  clickBookmarkSearchResult(item) {
    this.clearSearch();
    const libraryId = item.libraryId;
    const seriesId = item.seriesId;
    this.router.navigate(["library", libraryId, "series", seriesId, "manga", item.chapterId], { queryParams: {
      incognitoMode: false,
      bookmarkMode: true
    } });
  }
  clickFileSearchResult(item) {
    this.clearSearch();
    this.searchService.getSeriesForMangaFile(item.id).subscribe((series) => {
      if (series !== void 0 && series !== null) {
        this.router.navigate(["library", series.libraryId, "series", series.id]);
      }
    });
  }
  clickChapterSearchResult(item) {
    this.clearSearch();
    this.searchService.getSeriesForChapter(item.id).subscribe((series) => {
      if (series !== void 0 && series !== null) {
        this.router.navigate(["library", series.libraryId, "series", series.id]);
      }
    });
  }
  clickLibraryResult(item) {
    this.clearSearch();
    this.router.navigate(["library", item.id]);
  }
  clickCollectionSearchResult(item) {
    this.clearSearch();
    this.router.navigate(["collections", item.id]);
  }
  clickReadingListSearchResult(item) {
    this.clearSearch();
    this.router.navigate(["lists", item.id]);
  }
  scrollToTop() {
    this.scrollService.scrollTo(0, this.scrollElem);
  }
  focusUpdate(searchFocused) {
    this.searchFocused = searchFocused;
    this.cdRef.markForCheck();
  }
  toggleSideNav(event) {
    event.stopPropagation();
    this.navService.toggleSideNav();
  }
  openLinkSelectionMenu() {
    const ref = this.modalService.open(NavLinkModalComponent, { fullscreen: "sm" });
    ref.componentInstance.logoutFn = this.logout.bind(this);
  }
  static {
    this.\u0275fac = function NavHeaderComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavHeaderComponent)(\u0275\u0275directiveInject(DOCUMENT));
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _NavHeaderComponent, selectors: [["app-nav-header"]], viewQuery: function NavHeaderComponent_Query(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275viewQuery(_c04, 5);
      }
      if (rf & 2) {
        let _t;
        \u0275\u0275queryRefresh(_t = \u0275\u0275loadQuery()) && (ctx.searchViewRef = _t.first);
      }
    }, hostBindings: function NavHeaderComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("resize", function NavHeaderComponent_resize_HostBindingHandler($event) {
          return ctx.onResize($event);
        }, false, \u0275\u0275resolveWindow)("orientationchange", function NavHeaderComponent_orientationchange_HostBindingHandler($event) {
          return ctx.onResize($event);
        }, false, \u0275\u0275resolveWindow);
      }
    }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [["search", ""], ["libraryTemplate", ""], ["seriesTemplate", ""], ["bookmarkTemplate", ""], ["collectionTemplate", ""], ["readingListTemplate", ""], ["tagTemplate", ""], ["personTemplate", ""], ["genreTemplate", ""], ["chapterTemplate", ""], ["fileTemplate", ""], ["noResultsTemplate", ""], [4, "transloco", "translocoRead"], [1, "navbar", "navbar-expand-md", "navbar-dark", "fixed-top"], [1, "container-fluid"], ["href", "javascript:void(0);", 1, "visually-hidden-focusable", "focus-visible", 3, "click"], [1, "side-nav-toggle"], ["routerLink", "/home", "routerLinkActive", "active", 1, "navbar-brand", "dark-exempt"], ["width", "24px", "height", "24px", "imageUrl", "assets/images/logo-32.png", "classes", "logo-icon"], [1, "d-none", "d-md-inline", "logo"], [1, "navbar-nav", "col", "me-auto"], [1, "nav-item", "search"], [1, "side-nav-toggle", 3, "click"], ["aria-hidden", "true", 1, "fas", "fa-bars"], ["for", "nav-search", 1, "form-label", "visually-hidden"], [1, "ng-autocomplete"], ["id", "nav-search", "initialValue", "", 3, "inputChanged", "clearField", "focusChanged", "minQueryLength", "isLoading", "placeholder", "groupedData"], [1, "clickable", 2, "display", "flex", "padding", "5px", 3, "click"], [1, "ms-1"], [1, "me-1", 2, "width", "24px"], ["width", "24px", 1, "me-3", "search-result", 3, "imageUrl"], [3, "format"], [1, "text-light", "fst-italic", 2, "font-size", "0.8rem"], [3, "innerHTML"], [3, "promoted"], [3, "collection"], ["width", "24px", 1, "me-3", "search-result", 3, "styles", "imageUrl", "errorImage"], [1, "back-to-top"], [1, "btn", "btn-icon", "scroll-to-top", 3, "click"], ["aria-hidden", "true", 1, "fa", "fa-angle-double-up", "nav"], [1, "visually-hidden"], [1, "nav-item", "me-2"], [3, "user"], [1, "btn", "btn-outline-secondary", "primary-text"], [1, "btn", "btn-outline-secondary", "primary-text", 3, "click"], [1, "fa-solid", "fa-user-circle", "align-self-center", "phone-hidden", "d-xs-inline-block", "d-sm-inline-block", "d-md-none"], [1, "d-none", "d-xs-none", "d-sm-none", "d-md-inline-block", "fw-bold"], [1, "nav-item", "not-xs-only"], ["routerLink", "/settings", 1, "dark-exempt", "btn", "btn-icon", 3, "fragment", "title"], ["aria-hidden", "true", 1, "fa", "fa-cogs", "nav"], ["ngbDropdown", "", "display", "dynamic", "placement", "bottom-right", 1, "nav-item", "dropdown", "d-sm-none", "d-md-block"], ["ngbDropdownToggle", "", 1, "btn", "btn-outline-secondary", "primary-text"], ["ngbDropdownMenu", ""], ["ngbDropdownItem", "", "routerLink", "/all-filters/"], ["ngbDropdownItem", "", "routerLink", "/announcements/"], ["ngbDropdownItem", "", "rel", "noopener noreferrer", "target", "_blank", 3, "href"], ["ngbDropdownItem", "", 3, "click"]], template: function NavHeaderComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, NavHeaderComponent_ng_container_0_Template, 3, 3, "ng-container", 12);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "nav-header");
      }
    }, dependencies: [
      RouterLink,
      RouterLinkActive,
      GroupedTypeaheadComponent,
      ImageComponent,
      SeriesFormatComponent,
      EventsWidgetComponent,
      NgbDropdown,
      NgbDropdownToggle,
      NgbDropdownMenu,
      NgbDropdownItem,
      AsyncPipe,
      SentenceCasePipe,
      TranslocoDirective,
      CollectionOwnerComponent,
      PromotedIconComponent
    ], styles: ["\n\n.btn[_ngcontent-%COMP%]:focus {\n  box-shadow: 0 0 0 0.1rem var(--navbar-btn-hover-outline-color);\n}\n.navbar[_ngcontent-%COMP%] {\n  background-color: var(--navbar-bg-color);\n}\n.navbar[_ngcontent-%COMP%]   .navbar-nav[_ngcontent-%COMP%]   .nav-item.search[_ngcontent-%COMP%] {\n  width: 100%;\n}\n.navbar[_ngcontent-%COMP%]   .side-nav-toggle[_ngcontent-%COMP%] {\n  cursor: pointer;\n  margin-left: 0.8125rem;\n  font-size: 1.2rem;\n}\n.navbar[_ngcontent-%COMP%]   .side-nav-toggle[_ngcontent-%COMP%]   i[_ngcontent-%COMP%] {\n  color: var(--navbar-fa-icon-color);\n  opacity: 0.8;\n}\n.navbar[_ngcontent-%COMP%]   .side-nav-toggle[_ngcontent-%COMP%]:hover   i[_ngcontent-%COMP%] {\n  opacity: 1;\n}\n.navbar[_ngcontent-%COMP%]   .nav-item[_ngcontent-%COMP%]:not(.search) {\n  display: unset;\n  opacity: 0.8;\n}\n.navbar[_ngcontent-%COMP%]   .nav-item[_ngcontent-%COMP%]:not(.search):hover {\n  opacity: 1;\n}\n.xs-only[_ngcontent-%COMP%] {\n  display: none;\n}\n.not-xs-only[_ngcontent-%COMP%] {\n  display: inherit;\n}\n@media only screen and (max-width: 300px) {\n  .xs-only[_ngcontent-%COMP%] {\n    display: inherit;\n  }\n  .not-xs-only[_ngcontent-%COMP%] {\n    display: none;\n  }\n}\n.nav-item.dropdown[_ngcontent-%COMP%] {\n  position: unset;\n}\n.navbar-brand[_ngcontent-%COMP%] {\n  font-family: var(--brand-font-family);\n  font-weight: bold;\n  margin: 0 2rem;\n  display: flex;\n  align-items: center;\n}\n.navbar-brand[_ngcontent-%COMP%]:hover {\n  text-decoration: none;\n}\n.navbar-brand[_ngcontent-%COMP%]   .logo-icon[_ngcontent-%COMP%] {\n  align-items: center;\n  align-self: center;\n  display: flex;\n  flex-direction: column;\n  height: 1.5rem;\n  width: 1.5rem;\n  margin-right: 0.5rem;\n}\n.navbar-brand[_ngcontent-%COMP%]   .logo[_ngcontent-%COMP%] {\n  vertical-align: text-top;\n  max-height: 1.75rem;\n  line-height: 2.0625rem;\n  margin-left: 0.3125rem;\n}\n.navbar-brand[_ngcontent-%COMP%]   .phone-hidden[_ngcontent-%COMP%] {\n  vertical-align: middle;\n}\n.focus-visible[_ngcontent-%COMP%]:focus {\n  visibility: visible;\n  color: var(--nav-header-text-color);\n}\n.ng-autocomplete[_ngcontent-%COMP%] {\n  margin-bottom: 0px;\n}\n.primary-text[_ngcontent-%COMP%] {\n  color: var(--nav-header-text-color);\n  border: none;\n}\n.search-result[_ngcontent-%COMP%] {\n  width: 1.5rem;\n  margin-top: 0.3125rem;\n}\n.scroll-to-top[_ngcontent-%COMP%]:hover {\n  animation: MoveUpDown 1s linear infinite;\n}\n.text-light[_ngcontent-%COMP%] {\n  color: var(--search-result-text-lite-color) !important;\n}\n@media only screen and (max-width: 650px) {\n  .navbar-nav[_ngcontent-%COMP%] {\n    width: 0;\n  }\n  .navbar-brand[_ngcontent-%COMP%] {\n    margin: 0 1rem 0 1rem;\n  }\n  .navbar-brand[_ngcontent-%COMP%]   .logo-icon[_ngcontent-%COMP%] {\n    margin: unset;\n  }\n}\n/*# sourceMappingURL=nav-header.component.css.map */"], changeDetection: 0 });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(NavHeaderComponent, { className: "NavHeaderComponent", filePath: "src/app/nav/_components/nav-header/nav-header.component.ts", lineNumber: 66 });
})();

// src/app/announcements/_components/new-update-modal/new-update-modal.component.ts
function NewUpdateModalComponent_ng_container_0_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-changelog-update-item", 4);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("update", ctx_r1.update)("showExtras", false);
  }
}
function NewUpdateModalComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275elementStart(1, "div", 1)(2, "h4", 2);
    \u0275\u0275text(3);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(4, "div", 3);
    \u0275\u0275template(5, NewUpdateModalComponent_ng_container_0_Conditional_5_Template, 1, 2, "app-changelog-update-item", 4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "div", 5)(7, "button", 6);
    \u0275\u0275listener("click", function NewUpdateModalComponent_ng_container_0_Template_button_click_7_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.refresh());
    });
    \u0275\u0275text(8);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    const t_r3 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("title"));
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.update ? 5 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("refresh"));
  }
}
var NewUpdateModalComponent = class _NewUpdateModalComponent {
  constructor() {
    this.ngbModal = inject(NgbActiveModal);
    this.translocoService = inject(TranslocoService);
    this.version = "";
    this.update = null;
  }
  close() {
    this.ngbModal.dismiss();
  }
  refresh() {
    this.bustLocaleCache();
    this.applyUpdate(this.version);
    location.reload();
  }
  applyUpdate(version) {
    this.bustLocaleCache();
    location.reload();
  }
  bustLocaleCache() {
    localStorage.removeItem("@transloco/translations/timestamp");
    localStorage.removeItem("@transloco/translations");
    localStorage.removeItem("translocoLang");
    const locale = localStorage.getItem("kavita-locale") || "en";
    this.translocoService.cache.delete(locale);
    this.translocoService.cache.clear();
    setTimeout(() => {
      this.translocoService.setActiveLang(locale);
    }, 10);
  }
  static {
    this.\u0275fac = function NewUpdateModalComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NewUpdateModalComponent)();
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _NewUpdateModalComponent, selectors: [["app-new-update-modal"]], inputs: { version: "version", update: "update" }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [[4, "transloco", "translocoRead"], [1, "modal-header"], ["id", "modal-basic-title", 1, "modal-title"], [1, "modal-body", "scrollable-modal"], [3, "update", "showExtras"], [1, "modal-footer"], ["type", "button", 1, "btn", "btn-primary", 3, "click"]], template: function NewUpdateModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, NewUpdateModalComponent_ng_container_0_Template, 9, 3, "ng-container", 0);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "new-version-modal");
      }
    }, dependencies: [
      TranslocoDirective,
      ChangelogUpdateItemComponent
    ], changeDetection: 0 });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(NewUpdateModalComponent, { className: "NewUpdateModalComponent", filePath: "src/app/announcements/_components/new-update-modal/new-update-modal.component.ts", lineNumber: 24 });
})();

// src/app/announcements/_components/out-of-date-modal/out-of-date-modal.component.ts
var _c05 = (a0) => ({ count: a0 });
function OutOfDateModalComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementContainerStart(0);
    \u0275\u0275elementStart(1, "div", 1)(2, "h4", 2);
    \u0275\u0275text(3);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "button", 3);
    \u0275\u0275listener("click", function OutOfDateModalComponent_ng_container_0_Template_button_click_4_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.close());
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(5, "div", 4)(6, "p")(7, "strong");
    \u0275\u0275text(8);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(9, "p");
    \u0275\u0275text(10);
    \u0275\u0275elementEnd();
    \u0275\u0275element(11, "p", 5);
    \u0275\u0275pipe(12, "safeHtml");
    \u0275\u0275elementStart(13, "p");
    \u0275\u0275text(14);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(15, "div", 6)(16, "a", 7);
    \u0275\u0275text(17, "Discord");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "button", 8);
    \u0275\u0275listener("click", function OutOfDateModalComponent_ng_container_0_Template_button_click_18_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.close());
    });
    \u0275\u0275text(19);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementContainerEnd();
  }
  if (rf & 2) {
    const t_r3 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("title"));
    \u0275\u0275advance();
    \u0275\u0275attribute("aria-label", t_r3("close"));
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(t_r3("subtitle", \u0275\u0275pureFunction1(9, _c05, ctx_r1.versionsOutOfDate)));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(t_r3("description-1"));
    \u0275\u0275advance();
    \u0275\u0275property("innerHTML", \u0275\u0275pipeBind1(12, 7, t_r3("description-2")), \u0275\u0275sanitizeHtml);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(t_r3("description-3"));
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate(t_r3("close"));
  }
}
var OutOfDateModalComponent = class _OutOfDateModalComponent {
  constructor() {
    this.ngbModal = inject(NgbActiveModal);
    this.versionsOutOfDate = 0;
  }
  close() {
    this.ngbModal.close();
  }
  static {
    this.\u0275fac = function OutOfDateModalComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutOfDateModalComponent)();
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _OutOfDateModalComponent, selectors: [["app-out-of-date-modal"]], inputs: { versionsOutOfDate: "versionsOutOfDate" }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 1, vars: 1, consts: [[4, "transloco", "translocoRead"], [1, "modal-header"], ["id", "modal-basic-title", 1, "modal-title"], ["type", "button", 1, "btn-close", 3, "click"], [1, "modal-body"], [3, "innerHTML"], [1, "modal-footer"], ["href", "https://discord.gg/b52wT37kt7", "target", "_blank", "rel", "noreferrer noopener", 1, "btn", "btn-link", "me-1"], ["type", "button", 1, "btn", "btn-primary", 3, "click"]], template: function OutOfDateModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275template(0, OutOfDateModalComponent_ng_container_0_Template, 20, 11, "ng-container", 0);
      }
      if (rf & 2) {
        \u0275\u0275property("translocoRead", "out-of-date-modal");
      }
    }, dependencies: [
      FormsModule,
      TranslocoDirective,
      SafeHtmlPipe
    ] });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(OutOfDateModalComponent, { className: "OutOfDateModalComponent", filePath: "src/app/announcements/_components/out-of-date-modal/out-of-date-modal.component.ts", lineNumber: 29 });
})();

// src/app/_services/version.service.ts
var VersionService = class _VersionService {
  static {
    this.SERVER_VERSION_KEY = "kavita--version";
  }
  static {
    this.CLIENT_REFRESH_KEY = "kavita--client-refresh-last-shown";
  }
  static {
    this.NEW_UPDATE_KEY = "kavita--new-update-last-shown";
  }
  static {
    this.OUT_OF_BAND_KEY = "kavita--out-of-band-last-shown";
  }
  constructor() {
    this.serverService = inject(ServerService);
    this.accountService = inject(AccountService);
    this.modalService = inject(NgbModal);
    this.CLIENT_REFRESH_INTERVAL = 0;
    this.NEW_UPDATE_INTERVAL = 7 * 24 * 60 * 60 * 1e3;
    this.OUT_OF_BAND_INTERVAL = 30 * 24 * 60 * 60 * 1e3;
    this.VERSION_CHECK_INTERVAL = 30 * 60 * 1e3;
    this.OUT_OF_DATE_CHECK_INTERVAL = this.VERSION_CHECK_INTERVAL;
    this.OUT_Of_BAND_AMOUNT = 2;
    this.modalOpen = false;
    this.startVersionCheck();
    this.startOutOfDateCheck();
  }
  ngOnDestroy() {
    this.versionCheckSubscription?.unsubscribe();
    this.outOfDateCheckSubscription?.unsubscribe();
  }
  startVersionCheck() {
    console.log("Starting version checker");
    this.versionCheckSubscription = interval(this.VERSION_CHECK_INTERVAL).pipe(switchMap(() => this.accountService.currentUser$), filter((user) => !!user && !this.modalOpen), switchMap((user) => this.serverService.getVersion(user.apiKey)), filter((update) => !!update)).subscribe((version) => this.handleVersionUpdate(version));
  }
  startOutOfDateCheck() {
    console.log("Starting out-of-date checker");
    this.outOfDateCheckSubscription = interval(this.OUT_OF_DATE_CHECK_INTERVAL).pipe(switchMap(() => this.accountService.currentUser$), filter((u) => u !== void 0 && this.accountService.hasAdminRole(u) && !this.modalOpen), switchMap((_) => this.serverService.checkHowOutOfDate(true)), filter((versionsOutOfDate) => !isNaN(versionsOutOfDate) && versionsOutOfDate > this.OUT_Of_BAND_AMOUNT)).subscribe((versionsOutOfDate) => this.handleOutOfDateNotification(versionsOutOfDate));
  }
  handleVersionUpdate(serverVersion) {
    if (this.modalOpen)
      return;
    const cachedVersion = localStorage.getItem(_VersionService.SERVER_VERSION_KEY);
    console.log("Server version:", serverVersion, "Cached version:", cachedVersion);
    const isNewServerVersion = cachedVersion !== null && cachedVersion !== serverVersion;
    if (isNewServerVersion) {
      this.showClientRefreshNotification(serverVersion);
    } else {
      this.checkForNewUpdates();
    }
    localStorage.setItem(_VersionService.SERVER_VERSION_KEY, serverVersion);
  }
  showClientRefreshNotification(newVersion) {
    this.pauseChecks();
    this.modalOpen = true;
    this.serverService.getChangelog(1).subscribe((changelog) => {
      const ref = this.modalService.open(NewUpdateModalComponent, {
        size: "lg",
        keyboard: false,
        backdrop: "static"
      });
      ref.componentInstance.version = newVersion;
      ref.componentInstance.update = changelog[0];
      ref.componentInstance.requiresRefresh = true;
      localStorage.setItem(_VersionService.CLIENT_REFRESH_KEY, Date.now().toString());
      ref.closed.subscribe((_) => this.onModalClosed());
      ref.dismissed.subscribe((_) => this.onModalClosed());
    });
  }
  checkForNewUpdates() {
    this.accountService.currentUser$.pipe(take(1), filter((user) => user !== void 0 && this.accountService.hasAdminRole(user)), switchMap((_) => this.serverService.checkHowOutOfDate()), filter((versionsOutOfDate) => !isNaN(versionsOutOfDate) && versionsOutOfDate > 0 && versionsOutOfDate <= this.OUT_Of_BAND_AMOUNT)).subscribe((versionsOutOfDate) => {
      const lastShown = Number(localStorage.getItem(_VersionService.NEW_UPDATE_KEY) || "0");
      const currentTime = Date.now();
      if (currentTime - lastShown >= this.NEW_UPDATE_INTERVAL) {
        this.pauseChecks();
        this.modalOpen = true;
        this.serverService.getChangelog(1).subscribe((changelog) => {
          const ref = this.modalService.open(NewUpdateModalComponent, { size: "lg" });
          ref.componentInstance.versionsOutOfDate = versionsOutOfDate;
          ref.componentInstance.update = changelog[0];
          ref.componentInstance.requiresRefresh = false;
          localStorage.setItem(_VersionService.NEW_UPDATE_KEY, currentTime.toString());
          ref.closed.subscribe((_) => this.onModalClosed());
          ref.dismissed.subscribe((_) => this.onModalClosed());
        });
      }
    });
  }
  handleOutOfDateNotification(versionsOutOfDate) {
    const lastShown = Number(localStorage.getItem(_VersionService.OUT_OF_BAND_KEY) || "0");
    const currentTime = Date.now();
    if (currentTime - lastShown >= this.OUT_OF_BAND_INTERVAL) {
      this.pauseChecks();
      this.modalOpen = true;
      const ref = this.modalService.open(OutOfDateModalComponent, { size: "xl", fullscreen: "md" });
      ref.componentInstance.versionsOutOfDate = versionsOutOfDate;
      localStorage.setItem(_VersionService.OUT_OF_BAND_KEY, currentTime.toString());
      ref.closed.subscribe((_) => this.onModalClosed());
      ref.dismissed.subscribe((_) => this.onModalClosed());
    }
  }
  pauseChecks() {
    this.versionCheckSubscription?.unsubscribe();
    this.outOfDateCheckSubscription?.unsubscribe();
  }
  onModalClosed() {
    this.modalOpen = false;
    this.startVersionCheck();
    this.startOutOfDateCheck();
  }
  static {
    this.\u0275fac = function VersionService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _VersionService)();
    };
  }
  static {
    this.\u0275prov = \u0275\u0275defineInjectable({ token: _VersionService, factory: _VersionService.\u0275fac, providedIn: "root" });
  }
};

// src/app/app.component.ts
var _c06 = (a0) => ({ "no-transitions": a0 });
var _c17 = (a0, a1) => ({ "closed": a0, "content-wrapper": a1 });
var _c24 = (a0) => ({ "g-0": a0 });
var _c33 = (a0, a1) => ({ "companion-bar-content": a0, "companion-bar-collapsed": a1 });
function AppComponent_Conditional_2_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 3)(1, "div", 4);
    \u0275\u0275element(2, "canvas", 5);
    \u0275\u0275elementEnd()();
  }
}
function AppComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, AppComponent_Conditional_2_Conditional_0_Template, 3, 0, "div", 3);
    \u0275\u0275pipe(1, "async");
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275conditional(ctx && \u0275\u0275pipeBind1(1, 1, ctx_r0.navService.navbarVisible$) === true ? 0 : -1);
  }
}
function AppComponent_Conditional_8_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-preference-nav");
  }
}
function AppComponent_Conditional_8_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "app-side-nav");
  }
}
function AppComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275template(0, AppComponent_Conditional_8_Conditional_0_Template, 1, 0, "app-preference-nav");
    \u0275\u0275pipe(1, "async");
    \u0275\u0275template(2, AppComponent_Conditional_8_Conditional_2_Template, 1, 0, "app-side-nav");
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275conditional(\u0275\u0275pipeBind1(1, 1, ctx_r0.navService.usePreferenceSideNav$) ? 0 : 2);
  }
}
function AppComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div")(1, "div", 6);
    \u0275\u0275pipe(2, "async");
    \u0275\u0275pipe(3, "async");
    \u0275\u0275pipe(4, "async");
    \u0275\u0275pipe(5, "async");
    \u0275\u0275element(6, "router-outlet");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("ngClass", \u0275\u0275pureFunction2(9, _c33, \u0275\u0275pipeBind1(2, 1, ctx_r0.navService.sideNavCollapsed$) === false || \u0275\u0275pipeBind1(3, 3, ctx_r0.navService.usePreferenceSideNav$), \u0275\u0275pipeBind1(4, 5, ctx_r0.navService.sideNavCollapsed$) && \u0275\u0275pipeBind1(5, 7, ctx_r0.navService.usePreferenceSideNav$)));
  }
}
function AppComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "router-outlet");
  }
}
var AppComponent = class _AppComponent {
  constructor(ratingConfig, modalConfig) {
    this.Breakpoint = Breakpoint;
    this.destroyRef = inject(DestroyRef);
    this.offcanvas = inject(NgbOffcanvas);
    this.navService = inject(NavService);
    this.utilityService = inject(UtilityService);
    this.serverService = inject(ServerService);
    this.accountService = inject(AccountService);
    this.libraryService = inject(LibraryService);
    this.ngbModal = inject(NgbModal);
    this.router = inject(Router);
    this.themeService = inject(ThemeService);
    this.document = inject(DOCUMENT);
    this.translocoService = inject(TranslocoService);
    this.versionService = inject(VersionService);
    this.licenseService = inject(LicenseService);
    this.localizationService = inject(LocalizationService);
    modalConfig.fullscreen = "lg";
    ratingConfig.max = 5;
    ratingConfig.resettable = true;
    this.router.events.pipe(filter((event) => event instanceof NavigationStart), takeUntilDestroyed(this.destroyRef)).subscribe((event) => __async(this, null, function* () {
      if (!this.ngbModal.hasOpenModals() && !this.offcanvas.hasOpenOffcanvas())
        return;
      if (this.ngbModal.hasOpenModals()) {
        this.ngbModal.dismissAll();
      }
      if (this.offcanvas.hasOpenOffcanvas()) {
        this.offcanvas.dismiss();
      }
      if (event.navigationTrigger === "popstate") {
        const currentRoute = this.router.routerState;
        yield this.router.navigateByUrl(currentRoute.snapshot.url, { skipLocationChange: true });
      }
    }));
    this.transitionState$ = this.accountService.currentUser$.pipe(map((user) => {
      if (!user)
        return false;
      return user.preferences.noTransitions;
    }), takeUntilDestroyed(this.destroyRef));
    this.localizationService.getLocales().subscribe();
  }
  setDocHeight() {
    const vh = window.innerHeight * 0.01;
    this.document.documentElement.style.setProperty("--vh", `${vh}px`);
    this.utilityService.activeBreakpointSource.next(this.utilityService.getActiveBreakpoint());
  }
  ngOnInit() {
    this.setDocHeight();
    this.setCurrentUser();
    this.themeService.setColorScape("");
  }
  setCurrentUser() {
    const user = this.accountService.getUserFromLocalStorage();
    this.accountService.setCurrentUser(user);
    if (!user)
      return;
    this.themeService.getThemes().subscribe();
    this.libraryService.getLibraryNames().pipe(take(1), shareReplay({ refCount: true, bufferSize: 1 })).subscribe();
    if (this.accountService.hasAdminRole(user)) {
      this.licenseService.licenseInfo().subscribe();
    }
  }
  static {
    this.\u0275fac = function AppComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AppComponent)(\u0275\u0275directiveInject(NgbRatingConfig), \u0275\u0275directiveInject(NgbModalConfig));
    };
  }
  static {
    this.\u0275cmp = \u0275\u0275defineComponent({ type: _AppComponent, selectors: [["app-root"]], hostBindings: function AppComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("resize", function AppComponent_resize_HostBindingHandler($event) {
          return ctx.setDocHeight($event);
        }, false, \u0275\u0275resolveWindow)("orientationchange", function AppComponent_orientationchange_HostBindingHandler($event) {
          return ctx.setDocHeight($event);
        }, false, \u0275\u0275resolveWindow);
      }
    }, standalone: true, features: [\u0275\u0275StandaloneFeature], decls: 16, vars: 27, consts: [[3, "ngClass"], [1, "", 3, "ngClass"], ["id", "content"], [1, "fullpage-background"], [1, "background-area"], ["id", "backgroundCanvas", 1, "default-background", 2, "width", "100%", "height", "calc(var(--vh) * 100)"], [1, "companion-bar", 3, "ngClass"]], template: function AppComponent_Template(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275elementStart(0, "div", 0);
        \u0275\u0275pipe(1, "async");
        \u0275\u0275template(2, AppComponent_Conditional_2_Template, 2, 3);
        \u0275\u0275pipe(3, "async");
        \u0275\u0275element(4, "app-nav-header");
        \u0275\u0275elementStart(5, "div", 0);
        \u0275\u0275pipe(6, "async");
        \u0275\u0275pipe(7, "async");
        \u0275\u0275template(8, AppComponent_Conditional_8_Template, 3, 3);
        \u0275\u0275pipe(9, "async");
        \u0275\u0275elementStart(10, "div", 1);
        \u0275\u0275pipe(11, "async");
        \u0275\u0275element(12, "a", 2);
        \u0275\u0275template(13, AppComponent_Conditional_13_Template, 7, 12, "div");
        \u0275\u0275pipe(14, "async");
        \u0275\u0275template(15, AppComponent_Conditional_15_Template, 1, 0, "router-outlet");
        \u0275\u0275elementEnd()()();
      }
      if (rf & 2) {
        let tmp_1_0;
        \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(20, _c06, \u0275\u0275pipeBind1(1, 6, ctx.transitionState$)));
        \u0275\u0275advance(2);
        \u0275\u0275conditional((tmp_1_0 = \u0275\u0275pipeBind1(3, 8, ctx.accountService.currentUser$)) ? 2 : -1, tmp_1_0);
        \u0275\u0275advance(3);
        \u0275\u0275property("ngClass", \u0275\u0275pureFunction2(22, _c17, \u0275\u0275pipeBind1(6, 10, ctx.navService.sideNavCollapsed$), \u0275\u0275pipeBind1(7, 12, ctx.navService.sideNavVisibility$)));
        \u0275\u0275advance(3);
        \u0275\u0275conditional(\u0275\u0275pipeBind1(9, 14, ctx.navService.sideNavVisibility$) ? 8 : -1);
        \u0275\u0275advance(2);
        \u0275\u0275property("ngClass", \u0275\u0275pureFunction1(25, _c24, \u0275\u0275pipeBind1(11, 16, ctx.navService.sideNavVisibility$) === false));
        \u0275\u0275advance(3);
        \u0275\u0275conditional(\u0275\u0275pipeBind1(14, 18, ctx.navService.sideNavVisibility$) ? 13 : 15);
      }
    }, dependencies: [NgClass, SideNavComponent, RouterOutlet, AsyncPipe, NavHeaderComponent, PreferenceNavComponent], styles: ["\n\n.content-wrapper[_ngcontent-%COMP%] {\n  padding: 0 0 0 10px;\n  height: calc(var(--vh) * 100 - var(--nav-offset));\n}\n.companion-bar[_ngcontent-%COMP%] {\n  transition: all var(--side-nav-companion-bar-transistion);\n  margin-left: 45px;\n  overflow-y: hidden;\n  overflow-x: hidden;\n  height: calc(var(--vh) * 100 - var(--nav-mobile-offset));\n  scrollbar-gutter: stable;\n  scrollbar-width: thin;\n  mask-image:\n    linear-gradient(\n      to bottom,\n      transparent,\n      black 0%,\n      black 95%,\n      transparent 100%);\n  -webkit-mask-image:\n    linear-gradient(\n      to bottom,\n      transparent,\n      black 0%,\n      black 95%,\n      transparent 100%);\n}\n@supports (-moz-appearance: none) {\n  .companion-bar[_ngcontent-%COMP%] {\n    scrollbar-color: transparent transparent;\n    scrollbar-width: thin;\n  }\n}\n.companion-bar[_ngcontent-%COMP%]::-webkit-scrollbar {\n  background-color: transparent;\n  width: inherit;\n  display: none;\n  visibility: hidden;\n  background: transparent;\n}\n.companion-bar[_ngcontent-%COMP%]::-webkit-scrollbar-thumb {\n  background: transparent;\n}\n.companion-bar[_ngcontent-%COMP%]:hover {\n  scrollbar-width: thin;\n  overflow-y: auto;\n}\n@supports (-moz-appearance: none) {\n  .companion-bar[_ngcontent-%COMP%]:hover {\n    scrollbar-color: rgba(255, 255, 255, 0.3) rgba(0, 0, 0, 0);\n  }\n}\n.companion-bar[_ngcontent-%COMP%]:hover::-webkit-scrollbar-thumb {\n  visibility: visible;\n  background-color: rgba(255, 255, 255, 0.3);\n}\n.companion-bar-collapsed[_ngcontent-%COMP%] {\n  margin-left: 0 !important;\n}\n.companion-bar-content[_ngcontent-%COMP%] {\n  margin-left: var(--side-nav-width);\n}\n@media (max-width: 992px) {\n    html {\n    height: 100dvh !important;\n  }\n  .container-fluid[_ngcontent-%COMP%] {\n    padding: 0;\n  }\n  .content-wrapper[_ngcontent-%COMP%] {\n    overflow: hidden;\n    height: calc(var(--vh) * 100);\n    padding: 0;\n  }\n  .content-wrapper.closed[_ngcontent-%COMP%] {\n    overflow: auto;\n    height: calc(var(--vh) * 100);\n  }\n  .companion-bar[_ngcontent-%COMP%] {\n    margin-left: 0;\n    padding-left: 0;\n    width: 100%;\n    height: calc(var(--vh) * 100 - var(--nav-mobile-offset));\n    scrollbar-color: rgba(255, 255, 255, 0.3) rgba(0, 0, 0, 0.1);\n    scrollbar-width: thin;\n    margin-bottom: 20px;\n    overflow-y: auto;\n  }\n  .companion-bar-content[_ngcontent-%COMP%] {\n    margin-left: 0;\n  }\n}\n.default-background[_ngcontent-%COMP%] {\n  background:\n    radial-gradient(\n      circle farthest-side at 0% 100%,\n      var(--colorscape-darker-color) 0%,\n      var(--colorscape-darker-no-alpha-color) 100%),\n    radial-gradient(\n      circle farthest-side at 100% 100%,\n      var(--colorscape-primary-color) 0%,\n      var(--colorscape-primary-no-alpha-color) 100%),\n    radial-gradient(\n      circle farthest-side at 100% 0%,\n      var(--colorscape-lighter-color) 0%,\n      var(--colorscape-lighter-no-alpha-color) 100%),\n    radial-gradient(\n      circle farthest-side at 0% 0%,\n      var(--colorscape-complementary-color) 0%,\n      var(--colorscape-complementary-no-alpha-color) 100%),\n    var(--bs-body-bg);\n}\n.fullpage-background[_ngcontent-%COMP%] {\n  position: fixed;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100vh;\n  z-index: -1;\n  pointer-events: none;\n  background-color: var(--bs-body-bg);\n  filter: blur(20px);\n  object-fit: contain;\n  transform: scale(1.1);\n  mix-blend-mode: color;\n}\n.fullpage-background[_ngcontent-%COMP%]   .background-area[_ngcontent-%COMP%] {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 113vh;\n}\n/*# sourceMappingURL=app.component.css.map */"], changeDetection: 0 });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(AppComponent, { className: "AppComponent", filePath: "src/app/app.component.ts", lineNumber: 37 });
})();

// node_modules/@angular/animations/fesm2022/browser.mjs
var LINE_START = "\n - ";
function invalidTimingValue(exp) {
  return new RuntimeError(3e3, ngDevMode && `The provided timing value "${exp}" is invalid.`);
}
function negativeStepValue() {
  return new RuntimeError(3100, ngDevMode && "Duration values below 0 are not allowed for this animation step.");
}
function negativeDelayValue() {
  return new RuntimeError(3101, ngDevMode && "Delay values below 0 are not allowed for this animation step.");
}
function invalidStyleParams(varName) {
  return new RuntimeError(3001, ngDevMode && `Unable to resolve the local animation param ${varName} in the given list of values`);
}
function invalidParamValue(varName) {
  return new RuntimeError(3003, ngDevMode && `Please provide a value for the animation param ${varName}`);
}
function invalidNodeType(nodeType) {
  return new RuntimeError(3004, ngDevMode && `Unable to resolve animation metadata node #${nodeType}`);
}
function invalidCssUnitValue(userProvidedProperty, value) {
  return new RuntimeError(3005, ngDevMode && `Please provide a CSS unit value for ${userProvidedProperty}:${value}`);
}
function invalidTrigger() {
  return new RuntimeError(3006, ngDevMode && "animation triggers cannot be prefixed with an `@` sign (e.g. trigger('@foo', [...]))");
}
function invalidDefinition() {
  return new RuntimeError(3007, ngDevMode && "only state() and transition() definitions can sit inside of a trigger()");
}
function invalidState(metadataName, missingSubs) {
  return new RuntimeError(3008, ngDevMode && `state("${metadataName}", ...) must define default values for all the following style substitutions: ${missingSubs.join(", ")}`);
}
function invalidStyleValue(value) {
  return new RuntimeError(3002, ngDevMode && `The provided style string value ${value} is not allowed.`);
}
function invalidParallelAnimation(prop, firstStart, firstEnd, secondStart, secondEnd) {
  return new RuntimeError(3010, ngDevMode && `The CSS property "${prop}" that exists between the times of "${firstStart}ms" and "${firstEnd}ms" is also being animated in a parallel animation between the times of "${secondStart}ms" and "${secondEnd}ms"`);
}
function invalidKeyframes() {
  return new RuntimeError(3011, ngDevMode && `keyframes() must be placed inside of a call to animate()`);
}
function invalidOffset() {
  return new RuntimeError(3012, ngDevMode && `Please ensure that all keyframe offsets are between 0 and 1`);
}
function keyframeOffsetsOutOfOrder() {
  return new RuntimeError(3200, ngDevMode && `Please ensure that all keyframe offsets are in order`);
}
function keyframesMissingOffsets() {
  return new RuntimeError(3202, ngDevMode && `Not all style() steps within the declared keyframes() contain offsets`);
}
function invalidStagger() {
  return new RuntimeError(3013, ngDevMode && `stagger() can only be used inside of query()`);
}
function invalidQuery(selector) {
  return new RuntimeError(3014, ngDevMode && `\`query("${selector}")\` returned zero elements. (Use \`query("${selector}", { optional: true })\` if you wish to allow this.)`);
}
function invalidExpression(expr) {
  return new RuntimeError(3015, ngDevMode && `The provided transition expression "${expr}" is not supported`);
}
function invalidTransitionAlias(alias) {
  return new RuntimeError(3016, ngDevMode && `The transition alias value "${alias}" is not supported`);
}
function triggerBuildFailed(name, errors) {
  return new RuntimeError(3404, ngDevMode && `The animation trigger "${name}" has failed to build due to the following errors:
 - ${errors.map((err) => err.message).join("\n - ")}`);
}
function animationFailed(errors) {
  return new RuntimeError(3502, ngDevMode && `Unable to animate due to the following errors:${LINE_START}${errors.map((err) => err.message).join(LINE_START)}`);
}
function registerFailed(errors) {
  return new RuntimeError(3503, ngDevMode && `Unable to build the animation due to the following errors: ${errors.map((err) => err.message).join("\n")}`);
}
function missingOrDestroyedAnimation() {
  return new RuntimeError(3300, ngDevMode && "The requested animation doesn't exist or has already been destroyed");
}
function createAnimationFailed(errors) {
  return new RuntimeError(3504, ngDevMode && `Unable to create the animation due to the following errors:${errors.map((err) => err.message).join("\n")}`);
}
function missingPlayer(id) {
  return new RuntimeError(3301, ngDevMode && `Unable to find the timeline player referenced by ${id}`);
}
function missingTrigger(phase, name) {
  return new RuntimeError(3302, ngDevMode && `Unable to listen on the animation trigger event "${phase}" because the animation trigger "${name}" doesn't exist!`);
}
function missingEvent(name) {
  return new RuntimeError(3303, ngDevMode && `Unable to listen on the animation trigger "${name}" because the provided event is undefined!`);
}
function unsupportedTriggerEvent(phase, name) {
  return new RuntimeError(3400, ngDevMode && `The provided animation trigger event "${phase}" for the animation trigger "${name}" is not supported!`);
}
function unregisteredTrigger(name) {
  return new RuntimeError(3401, ngDevMode && `The provided animation trigger "${name}" has not been registered!`);
}
function triggerTransitionsFailed(errors) {
  return new RuntimeError(3402, ngDevMode && `Unable to process animations due to the following failed trigger transitions
 ${errors.map((err) => err.message).join("\n")}`);
}
function transitionFailed(name, errors) {
  return new RuntimeError(3505, ngDevMode && `@${name} has failed due to:
 ${errors.map((err) => err.message).join("\n- ")}`);
}
var ANIMATABLE_PROP_SET = /* @__PURE__ */ new Set(["-moz-outline-radius", "-moz-outline-radius-bottomleft", "-moz-outline-radius-bottomright", "-moz-outline-radius-topleft", "-moz-outline-radius-topright", "-ms-grid-columns", "-ms-grid-rows", "-webkit-line-clamp", "-webkit-text-fill-color", "-webkit-text-stroke", "-webkit-text-stroke-color", "accent-color", "all", "backdrop-filter", "background", "background-color", "background-position", "background-size", "block-size", "border", "border-block-end", "border-block-end-color", "border-block-end-width", "border-block-start", "border-block-start-color", "border-block-start-width", "border-bottom", "border-bottom-color", "border-bottom-left-radius", "border-bottom-right-radius", "border-bottom-width", "border-color", "border-end-end-radius", "border-end-start-radius", "border-image-outset", "border-image-slice", "border-image-width", "border-inline-end", "border-inline-end-color", "border-inline-end-width", "border-inline-start", "border-inline-start-color", "border-inline-start-width", "border-left", "border-left-color", "border-left-width", "border-radius", "border-right", "border-right-color", "border-right-width", "border-start-end-radius", "border-start-start-radius", "border-top", "border-top-color", "border-top-left-radius", "border-top-right-radius", "border-top-width", "border-width", "bottom", "box-shadow", "caret-color", "clip", "clip-path", "color", "column-count", "column-gap", "column-rule", "column-rule-color", "column-rule-width", "column-width", "columns", "filter", "flex", "flex-basis", "flex-grow", "flex-shrink", "font", "font-size", "font-size-adjust", "font-stretch", "font-variation-settings", "font-weight", "gap", "grid-column-gap", "grid-gap", "grid-row-gap", "grid-template-columns", "grid-template-rows", "height", "inline-size", "input-security", "inset", "inset-block", "inset-block-end", "inset-block-start", "inset-inline", "inset-inline-end", "inset-inline-start", "left", "letter-spacing", "line-clamp", "line-height", "margin", "margin-block-end", "margin-block-start", "margin-bottom", "margin-inline-end", "margin-inline-start", "margin-left", "margin-right", "margin-top", "mask", "mask-border", "mask-position", "mask-size", "max-block-size", "max-height", "max-inline-size", "max-lines", "max-width", "min-block-size", "min-height", "min-inline-size", "min-width", "object-position", "offset", "offset-anchor", "offset-distance", "offset-path", "offset-position", "offset-rotate", "opacity", "order", "outline", "outline-color", "outline-offset", "outline-width", "padding", "padding-block-end", "padding-block-start", "padding-bottom", "padding-inline-end", "padding-inline-start", "padding-left", "padding-right", "padding-top", "perspective", "perspective-origin", "right", "rotate", "row-gap", "scale", "scroll-margin", "scroll-margin-block", "scroll-margin-block-end", "scroll-margin-block-start", "scroll-margin-bottom", "scroll-margin-inline", "scroll-margin-inline-end", "scroll-margin-inline-start", "scroll-margin-left", "scroll-margin-right", "scroll-margin-top", "scroll-padding", "scroll-padding-block", "scroll-padding-block-end", "scroll-padding-block-start", "scroll-padding-bottom", "scroll-padding-inline", "scroll-padding-inline-end", "scroll-padding-inline-start", "scroll-padding-left", "scroll-padding-right", "scroll-padding-top", "scroll-snap-coordinate", "scroll-snap-destination", "scrollbar-color", "shape-image-threshold", "shape-margin", "shape-outside", "tab-size", "text-decoration", "text-decoration-color", "text-decoration-thickness", "text-emphasis", "text-emphasis-color", "text-indent", "text-shadow", "text-underline-offset", "top", "transform", "transform-origin", "translate", "vertical-align", "visibility", "width", "word-spacing", "z-index", "zoom"]);
function optimizeGroupPlayer(players) {
  switch (players.length) {
    case 0:
      return new NoopAnimationPlayer();
    case 1:
      return players[0];
    default:
      return new AnimationGroupPlayer(players);
  }
}
function normalizeKeyframes$1(normalizer, keyframes, preStyles = /* @__PURE__ */ new Map(), postStyles = /* @__PURE__ */ new Map()) {
  const errors = [];
  const normalizedKeyframes = [];
  let previousOffset = -1;
  let previousKeyframe = null;
  keyframes.forEach((kf) => {
    const offset = kf.get("offset");
    const isSameOffset = offset == previousOffset;
    const normalizedKeyframe = isSameOffset && previousKeyframe || /* @__PURE__ */ new Map();
    kf.forEach((val, prop) => {
      let normalizedProp = prop;
      let normalizedValue = val;
      if (prop !== "offset") {
        normalizedProp = normalizer.normalizePropertyName(normalizedProp, errors);
        switch (normalizedValue) {
          case \u0275PRE_STYLE:
            normalizedValue = preStyles.get(prop);
            break;
          case AUTO_STYLE:
            normalizedValue = postStyles.get(prop);
            break;
          default:
            normalizedValue = normalizer.normalizeStyleValue(prop, normalizedProp, normalizedValue, errors);
            break;
        }
      }
      normalizedKeyframe.set(normalizedProp, normalizedValue);
    });
    if (!isSameOffset) {
      normalizedKeyframes.push(normalizedKeyframe);
    }
    previousKeyframe = normalizedKeyframe;
    previousOffset = offset;
  });
  if (errors.length) {
    throw animationFailed(errors);
  }
  return normalizedKeyframes;
}
function listenOnPlayer(player, eventName, event, callback) {
  switch (eventName) {
    case "start":
      player.onStart(() => callback(event && copyAnimationEvent(event, "start", player)));
      break;
    case "done":
      player.onDone(() => callback(event && copyAnimationEvent(event, "done", player)));
      break;
    case "destroy":
      player.onDestroy(() => callback(event && copyAnimationEvent(event, "destroy", player)));
      break;
  }
}
function copyAnimationEvent(e, phaseName, player) {
  const totalTime = player.totalTime;
  const disabled = player.disabled ? true : false;
  const event = makeAnimationEvent(e.element, e.triggerName, e.fromState, e.toState, phaseName || e.phaseName, totalTime == void 0 ? e.totalTime : totalTime, disabled);
  const data = e["_data"];
  if (data != null) {
    event["_data"] = data;
  }
  return event;
}
function makeAnimationEvent(element, triggerName, fromState, toState, phaseName = "", totalTime = 0, disabled) {
  return {
    element,
    triggerName,
    fromState,
    toState,
    phaseName,
    totalTime,
    disabled: !!disabled
  };
}
function getOrSetDefaultValue(map2, key, defaultValue) {
  let value = map2.get(key);
  if (!value) {
    map2.set(key, value = defaultValue);
  }
  return value;
}
function parseTimelineCommand(command) {
  const separatorPos = command.indexOf(":");
  const id = command.substring(1, separatorPos);
  const action = command.slice(separatorPos + 1);
  return [id, action];
}
var documentElement = /* @__PURE__ */ (() => typeof document === "undefined" ? null : document.documentElement)();
function getParentElement(element) {
  const parent = element.parentNode || element.host || null;
  if (parent === documentElement) {
    return null;
  }
  return parent;
}
function containsVendorPrefix(prop) {
  return prop.substring(1, 6) == "ebkit";
}
var _CACHED_BODY = null;
var _IS_WEBKIT = false;
function validateStyleProperty(prop) {
  if (!_CACHED_BODY) {
    _CACHED_BODY = getBodyNode() || {};
    _IS_WEBKIT = _CACHED_BODY.style ? "WebkitAppearance" in _CACHED_BODY.style : false;
  }
  let result = true;
  if (_CACHED_BODY.style && !containsVendorPrefix(prop)) {
    result = prop in _CACHED_BODY.style;
    if (!result && _IS_WEBKIT) {
      const camelProp = "Webkit" + prop.charAt(0).toUpperCase() + prop.slice(1);
      result = camelProp in _CACHED_BODY.style;
    }
  }
  return result;
}
function validateWebAnimatableStyleProperty(prop) {
  return ANIMATABLE_PROP_SET.has(prop);
}
function getBodyNode() {
  if (typeof document != "undefined") {
    return document.body;
  }
  return null;
}
function containsElement(elm1, elm2) {
  while (elm2) {
    if (elm2 === elm1) {
      return true;
    }
    elm2 = getParentElement(elm2);
  }
  return false;
}
function invokeQuery(element, selector, multi) {
  if (multi) {
    return Array.from(element.querySelectorAll(selector));
  }
  const elem = element.querySelector(selector);
  return elem ? [elem] : [];
}
var NoopAnimationDriver = class _NoopAnimationDriver {
  /**
   * @returns Whether `prop` is a valid CSS property
   */
  validateStyleProperty(prop) {
    return validateStyleProperty(prop);
  }
  /**
   *
   * @returns Whether elm1 contains elm2.
   */
  containsElement(elm1, elm2) {
    return containsElement(elm1, elm2);
  }
  /**
   * @returns Rhe parent of the given element or `null` if the element is the `document`
   */
  getParentElement(element) {
    return getParentElement(element);
  }
  /**
   * @returns The result of the query selector on the element. The array will contain up to 1 item
   *     if `multi` is  `false`.
   */
  query(element, selector, multi) {
    return invokeQuery(element, selector, multi);
  }
  /**
   * @returns The `defaultValue` or empty string
   */
  computeStyle(element, prop, defaultValue) {
    return defaultValue || "";
  }
  /**
   * @returns An `NoopAnimationPlayer`
   */
  animate(element, keyframes, duration, delay, easing, previousPlayers = [], scrubberAccessRequested) {
    return new NoopAnimationPlayer(duration, delay);
  }
  static {
    this.\u0275fac = function NoopAnimationDriver_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NoopAnimationDriver)();
    };
  }
  static {
    this.\u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
      token: _NoopAnimationDriver,
      factory: _NoopAnimationDriver.\u0275fac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NoopAnimationDriver, [{
    type: Injectable
  }], null, null);
})();
var AnimationDriver = class {
  static {
    this.NOOP = new NoopAnimationDriver();
  }
};
var AnimationStyleNormalizer = class {
};
var ONE_SECOND = 1e3;
var SUBSTITUTION_EXPR_START = "{{";
var SUBSTITUTION_EXPR_END = "}}";
var ENTER_CLASSNAME = "ng-enter";
var LEAVE_CLASSNAME = "ng-leave";
var NG_TRIGGER_CLASSNAME = "ng-trigger";
var NG_TRIGGER_SELECTOR = ".ng-trigger";
var NG_ANIMATING_CLASSNAME = "ng-animating";
var NG_ANIMATING_SELECTOR = ".ng-animating";
function resolveTimingValue(value) {
  if (typeof value == "number") return value;
  const matches = value.match(/^(-?[\.\d]+)(m?s)/);
  if (!matches || matches.length < 2) return 0;
  return _convertTimeValueToMS(parseFloat(matches[1]), matches[2]);
}
function _convertTimeValueToMS(value, unit) {
  switch (unit) {
    case "s":
      return value * ONE_SECOND;
    default:
      return value;
  }
}
function resolveTiming(timings, errors, allowNegativeValues) {
  return timings.hasOwnProperty("duration") ? timings : parseTimeExpression(timings, errors, allowNegativeValues);
}
function parseTimeExpression(exp, errors, allowNegativeValues) {
  const regex = /^(-?[\.\d]+)(m?s)(?:\s+(-?[\.\d]+)(m?s))?(?:\s+([-a-z]+(?:\(.+?\))?))?$/i;
  let duration;
  let delay = 0;
  let easing = "";
  if (typeof exp === "string") {
    const matches = exp.match(regex);
    if (matches === null) {
      errors.push(invalidTimingValue(exp));
      return {
        duration: 0,
        delay: 0,
        easing: ""
      };
    }
    duration = _convertTimeValueToMS(parseFloat(matches[1]), matches[2]);
    const delayMatch = matches[3];
    if (delayMatch != null) {
      delay = _convertTimeValueToMS(parseFloat(delayMatch), matches[4]);
    }
    const easingVal = matches[5];
    if (easingVal) {
      easing = easingVal;
    }
  } else {
    duration = exp;
  }
  if (!allowNegativeValues) {
    let containsErrors = false;
    let startIndex = errors.length;
    if (duration < 0) {
      errors.push(negativeStepValue());
      containsErrors = true;
    }
    if (delay < 0) {
      errors.push(negativeDelayValue());
      containsErrors = true;
    }
    if (containsErrors) {
      errors.splice(startIndex, 0, invalidTimingValue(exp));
    }
  }
  return {
    duration,
    delay,
    easing
  };
}
function normalizeKeyframes(keyframes) {
  if (!keyframes.length) {
    return [];
  }
  if (keyframes[0] instanceof Map) {
    return keyframes;
  }
  return keyframes.map((kf) => new Map(Object.entries(kf)));
}
function setStyles(element, styles, formerStyles) {
  styles.forEach((val, prop) => {
    const camelProp = dashCaseToCamelCase(prop);
    if (formerStyles && !formerStyles.has(prop)) {
      formerStyles.set(prop, element.style[camelProp]);
    }
    element.style[camelProp] = val;
  });
}
function eraseStyles(element, styles) {
  styles.forEach((_, prop) => {
    const camelProp = dashCaseToCamelCase(prop);
    element.style[camelProp] = "";
  });
}
function normalizeAnimationEntry(steps) {
  if (Array.isArray(steps)) {
    if (steps.length == 1) return steps[0];
    return sequence(steps);
  }
  return steps;
}
function validateStyleParams(value, options, errors) {
  const params = options.params || {};
  const matches = extractStyleParams(value);
  if (matches.length) {
    matches.forEach((varName) => {
      if (!params.hasOwnProperty(varName)) {
        errors.push(invalidStyleParams(varName));
      }
    });
  }
}
var PARAM_REGEX = new RegExp(`${SUBSTITUTION_EXPR_START}\\s*(.+?)\\s*${SUBSTITUTION_EXPR_END}`, "g");
function extractStyleParams(value) {
  let params = [];
  if (typeof value === "string") {
    let match;
    while (match = PARAM_REGEX.exec(value)) {
      params.push(match[1]);
    }
    PARAM_REGEX.lastIndex = 0;
  }
  return params;
}
function interpolateParams(value, params, errors) {
  const original = `${value}`;
  const str = original.replace(PARAM_REGEX, (_, varName) => {
    let localVal = params[varName];
    if (localVal == null) {
      errors.push(invalidParamValue(varName));
      localVal = "";
    }
    return localVal.toString();
  });
  return str == original ? value : str;
}
var DASH_CASE_REGEXP = /-+([a-z0-9])/g;
function dashCaseToCamelCase(input) {
  return input.replace(DASH_CASE_REGEXP, (...m) => m[1].toUpperCase());
}
function camelCaseToDashCase(input) {
  return input.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase();
}
function allowPreviousPlayerStylesMerge(duration, delay) {
  return duration === 0 || delay === 0;
}
function balancePreviousStylesIntoKeyframes(element, keyframes, previousStyles) {
  if (previousStyles.size && keyframes.length) {
    let startingKeyframe = keyframes[0];
    let missingStyleProps = [];
    previousStyles.forEach((val, prop) => {
      if (!startingKeyframe.has(prop)) {
        missingStyleProps.push(prop);
      }
      startingKeyframe.set(prop, val);
    });
    if (missingStyleProps.length) {
      for (let i = 1; i < keyframes.length; i++) {
        let kf = keyframes[i];
        missingStyleProps.forEach((prop) => kf.set(prop, computeStyle(element, prop)));
      }
    }
  }
  return keyframes;
}
function visitDslNode(visitor, node, context) {
  switch (node.type) {
    case AnimationMetadataType.Trigger:
      return visitor.visitTrigger(node, context);
    case AnimationMetadataType.State:
      return visitor.visitState(node, context);
    case AnimationMetadataType.Transition:
      return visitor.visitTransition(node, context);
    case AnimationMetadataType.Sequence:
      return visitor.visitSequence(node, context);
    case AnimationMetadataType.Group:
      return visitor.visitGroup(node, context);
    case AnimationMetadataType.Animate:
      return visitor.visitAnimate(node, context);
    case AnimationMetadataType.Keyframes:
      return visitor.visitKeyframes(node, context);
    case AnimationMetadataType.Style:
      return visitor.visitStyle(node, context);
    case AnimationMetadataType.Reference:
      return visitor.visitReference(node, context);
    case AnimationMetadataType.AnimateChild:
      return visitor.visitAnimateChild(node, context);
    case AnimationMetadataType.AnimateRef:
      return visitor.visitAnimateRef(node, context);
    case AnimationMetadataType.Query:
      return visitor.visitQuery(node, context);
    case AnimationMetadataType.Stagger:
      return visitor.visitStagger(node, context);
    default:
      throw invalidNodeType(node.type);
  }
}
function computeStyle(element, prop) {
  return window.getComputedStyle(element)[prop];
}
var DIMENSIONAL_PROP_SET = /* @__PURE__ */ new Set(["width", "height", "minWidth", "minHeight", "maxWidth", "maxHeight", "left", "top", "bottom", "right", "fontSize", "outlineWidth", "outlineOffset", "paddingTop", "paddingLeft", "paddingBottom", "paddingRight", "marginTop", "marginLeft", "marginBottom", "marginRight", "borderRadius", "borderWidth", "borderTopWidth", "borderLeftWidth", "borderRightWidth", "borderBottomWidth", "textIndent", "perspective"]);
var WebAnimationsStyleNormalizer = class extends AnimationStyleNormalizer {
  normalizePropertyName(propertyName, errors) {
    return dashCaseToCamelCase(propertyName);
  }
  normalizeStyleValue(userProvidedProperty, normalizedProperty, value, errors) {
    let unit = "";
    const strVal = value.toString().trim();
    if (DIMENSIONAL_PROP_SET.has(normalizedProperty) && value !== 0 && value !== "0") {
      if (typeof value === "number") {
        unit = "px";
      } else {
        const valAndSuffixMatch = value.match(/^[+-]?[\d\.]+([a-z]*)$/);
        if (valAndSuffixMatch && valAndSuffixMatch[1].length == 0) {
          errors.push(invalidCssUnitValue(userProvidedProperty, value));
        }
      }
    }
    return strVal + unit;
  }
};
function createListOfWarnings(warnings) {
  const LINE_START2 = "\n - ";
  return `${LINE_START2}${warnings.filter(Boolean).map((warning) => warning).join(LINE_START2)}`;
}
function warnTriggerBuild(name, warnings) {
  (typeof ngDevMode === "undefined" || ngDevMode) && console.warn(`The animation trigger "${name}" has built with the following warnings:${createListOfWarnings(warnings)}`);
}
function warnRegister(warnings) {
  (typeof ngDevMode === "undefined" || ngDevMode) && console.warn(`Animation built with the following warnings:${createListOfWarnings(warnings)}`);
}
function pushUnrecognizedPropertiesWarning(warnings, props) {
  if (props.length) {
    warnings.push(`The following provided properties are not recognized: ${props.join(", ")}`);
  }
}
var ANY_STATE = "*";
function parseTransitionExpr(transitionValue, errors) {
  const expressions = [];
  if (typeof transitionValue == "string") {
    transitionValue.split(/\s*,\s*/).forEach((str) => parseInnerTransitionStr(str, expressions, errors));
  } else {
    expressions.push(transitionValue);
  }
  return expressions;
}
function parseInnerTransitionStr(eventStr, expressions, errors) {
  if (eventStr[0] == ":") {
    const result = parseAnimationAlias(eventStr, errors);
    if (typeof result == "function") {
      expressions.push(result);
      return;
    }
    eventStr = result;
  }
  const match = eventStr.match(/^(\*|[-\w]+)\s*(<?[=-]>)\s*(\*|[-\w]+)$/);
  if (match == null || match.length < 4) {
    errors.push(invalidExpression(eventStr));
    return expressions;
  }
  const fromState = match[1];
  const separator = match[2];
  const toState = match[3];
  expressions.push(makeLambdaFromStates(fromState, toState));
  const isFullAnyStateExpr = fromState == ANY_STATE && toState == ANY_STATE;
  if (separator[0] == "<" && !isFullAnyStateExpr) {
    expressions.push(makeLambdaFromStates(toState, fromState));
  }
  return;
}
function parseAnimationAlias(alias, errors) {
  switch (alias) {
    case ":enter":
      return "void => *";
    case ":leave":
      return "* => void";
    case ":increment":
      return (fromState, toState) => parseFloat(toState) > parseFloat(fromState);
    case ":decrement":
      return (fromState, toState) => parseFloat(toState) < parseFloat(fromState);
    default:
      errors.push(invalidTransitionAlias(alias));
      return "* => *";
  }
}
var TRUE_BOOLEAN_VALUES = /* @__PURE__ */ new Set(["true", "1"]);
var FALSE_BOOLEAN_VALUES = /* @__PURE__ */ new Set(["false", "0"]);
function makeLambdaFromStates(lhs, rhs) {
  const LHS_MATCH_BOOLEAN = TRUE_BOOLEAN_VALUES.has(lhs) || FALSE_BOOLEAN_VALUES.has(lhs);
  const RHS_MATCH_BOOLEAN = TRUE_BOOLEAN_VALUES.has(rhs) || FALSE_BOOLEAN_VALUES.has(rhs);
  return (fromState, toState) => {
    let lhsMatch = lhs == ANY_STATE || lhs == fromState;
    let rhsMatch = rhs == ANY_STATE || rhs == toState;
    if (!lhsMatch && LHS_MATCH_BOOLEAN && typeof fromState === "boolean") {
      lhsMatch = fromState ? TRUE_BOOLEAN_VALUES.has(lhs) : FALSE_BOOLEAN_VALUES.has(lhs);
    }
    if (!rhsMatch && RHS_MATCH_BOOLEAN && typeof toState === "boolean") {
      rhsMatch = toState ? TRUE_BOOLEAN_VALUES.has(rhs) : FALSE_BOOLEAN_VALUES.has(rhs);
    }
    return lhsMatch && rhsMatch;
  };
}
var SELF_TOKEN = ":self";
var SELF_TOKEN_REGEX = new RegExp(`s*${SELF_TOKEN}s*,?`, "g");
function buildAnimationAst(driver, metadata, errors, warnings) {
  return new AnimationAstBuilderVisitor(driver).build(metadata, errors, warnings);
}
var ROOT_SELECTOR = "";
var AnimationAstBuilderVisitor = class {
  constructor(_driver) {
    this._driver = _driver;
  }
  build(metadata, errors, warnings) {
    const context = new AnimationAstBuilderContext(errors);
    this._resetContextStyleTimingState(context);
    const ast = visitDslNode(this, normalizeAnimationEntry(metadata), context);
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      if (context.unsupportedCSSPropertiesFound.size) {
        pushUnrecognizedPropertiesWarning(warnings, [...context.unsupportedCSSPropertiesFound.keys()]);
      }
    }
    return ast;
  }
  _resetContextStyleTimingState(context) {
    context.currentQuerySelector = ROOT_SELECTOR;
    context.collectedStyles = /* @__PURE__ */ new Map();
    context.collectedStyles.set(ROOT_SELECTOR, /* @__PURE__ */ new Map());
    context.currentTime = 0;
  }
  visitTrigger(metadata, context) {
    let queryCount = context.queryCount = 0;
    let depCount = context.depCount = 0;
    const states = [];
    const transitions = [];
    if (metadata.name.charAt(0) == "@") {
      context.errors.push(invalidTrigger());
    }
    metadata.definitions.forEach((def) => {
      this._resetContextStyleTimingState(context);
      if (def.type == AnimationMetadataType.State) {
        const stateDef = def;
        const name = stateDef.name;
        name.toString().split(/\s*,\s*/).forEach((n) => {
          stateDef.name = n;
          states.push(this.visitState(stateDef, context));
        });
        stateDef.name = name;
      } else if (def.type == AnimationMetadataType.Transition) {
        const transition = this.visitTransition(def, context);
        queryCount += transition.queryCount;
        depCount += transition.depCount;
        transitions.push(transition);
      } else {
        context.errors.push(invalidDefinition());
      }
    });
    return {
      type: AnimationMetadataType.Trigger,
      name: metadata.name,
      states,
      transitions,
      queryCount,
      depCount,
      options: null
    };
  }
  visitState(metadata, context) {
    const styleAst = this.visitStyle(metadata.styles, context);
    const astParams = metadata.options && metadata.options.params || null;
    if (styleAst.containsDynamicStyles) {
      const missingSubs = /* @__PURE__ */ new Set();
      const params = astParams || {};
      styleAst.styles.forEach((style2) => {
        if (style2 instanceof Map) {
          style2.forEach((value) => {
            extractStyleParams(value).forEach((sub) => {
              if (!params.hasOwnProperty(sub)) {
                missingSubs.add(sub);
              }
            });
          });
        }
      });
      if (missingSubs.size) {
        context.errors.push(invalidState(metadata.name, [...missingSubs.values()]));
      }
    }
    return {
      type: AnimationMetadataType.State,
      name: metadata.name,
      style: styleAst,
      options: astParams ? {
        params: astParams
      } : null
    };
  }
  visitTransition(metadata, context) {
    context.queryCount = 0;
    context.depCount = 0;
    const animation = visitDslNode(this, normalizeAnimationEntry(metadata.animation), context);
    const matchers = parseTransitionExpr(metadata.expr, context.errors);
    return {
      type: AnimationMetadataType.Transition,
      matchers,
      animation,
      queryCount: context.queryCount,
      depCount: context.depCount,
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitSequence(metadata, context) {
    return {
      type: AnimationMetadataType.Sequence,
      steps: metadata.steps.map((s) => visitDslNode(this, s, context)),
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitGroup(metadata, context) {
    const currentTime = context.currentTime;
    let furthestTime = 0;
    const steps = metadata.steps.map((step) => {
      context.currentTime = currentTime;
      const innerAst = visitDslNode(this, step, context);
      furthestTime = Math.max(furthestTime, context.currentTime);
      return innerAst;
    });
    context.currentTime = furthestTime;
    return {
      type: AnimationMetadataType.Group,
      steps,
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitAnimate(metadata, context) {
    const timingAst = constructTimingAst(metadata.timings, context.errors);
    context.currentAnimateTimings = timingAst;
    let styleAst;
    let styleMetadata = metadata.styles ? metadata.styles : style({});
    if (styleMetadata.type == AnimationMetadataType.Keyframes) {
      styleAst = this.visitKeyframes(styleMetadata, context);
    } else {
      let styleMetadata2 = metadata.styles;
      let isEmpty = false;
      if (!styleMetadata2) {
        isEmpty = true;
        const newStyleData = {};
        if (timingAst.easing) {
          newStyleData["easing"] = timingAst.easing;
        }
        styleMetadata2 = style(newStyleData);
      }
      context.currentTime += timingAst.duration + timingAst.delay;
      const _styleAst = this.visitStyle(styleMetadata2, context);
      _styleAst.isEmptyStep = isEmpty;
      styleAst = _styleAst;
    }
    context.currentAnimateTimings = null;
    return {
      type: AnimationMetadataType.Animate,
      timings: timingAst,
      style: styleAst,
      options: null
    };
  }
  visitStyle(metadata, context) {
    const ast = this._makeStyleAst(metadata, context);
    this._validateStyleAst(ast, context);
    return ast;
  }
  _makeStyleAst(metadata, context) {
    const styles = [];
    const metadataStyles = Array.isArray(metadata.styles) ? metadata.styles : [metadata.styles];
    for (let styleTuple of metadataStyles) {
      if (typeof styleTuple === "string") {
        if (styleTuple === AUTO_STYLE) {
          styles.push(styleTuple);
        } else {
          context.errors.push(invalidStyleValue(styleTuple));
        }
      } else {
        styles.push(new Map(Object.entries(styleTuple)));
      }
    }
    let containsDynamicStyles = false;
    let collectedEasing = null;
    styles.forEach((styleData) => {
      if (styleData instanceof Map) {
        if (styleData.has("easing")) {
          collectedEasing = styleData.get("easing");
          styleData.delete("easing");
        }
        if (!containsDynamicStyles) {
          for (let value of styleData.values()) {
            if (value.toString().indexOf(SUBSTITUTION_EXPR_START) >= 0) {
              containsDynamicStyles = true;
              break;
            }
          }
        }
      }
    });
    return {
      type: AnimationMetadataType.Style,
      styles,
      easing: collectedEasing,
      offset: metadata.offset,
      containsDynamicStyles,
      options: null
    };
  }
  _validateStyleAst(ast, context) {
    const timings = context.currentAnimateTimings;
    let endTime = context.currentTime;
    let startTime = context.currentTime;
    if (timings && startTime > 0) {
      startTime -= timings.duration + timings.delay;
    }
    ast.styles.forEach((tuple) => {
      if (typeof tuple === "string") return;
      tuple.forEach((value, prop) => {
        if (typeof ngDevMode === "undefined" || ngDevMode) {
          if (!this._driver.validateStyleProperty(prop)) {
            tuple.delete(prop);
            context.unsupportedCSSPropertiesFound.add(prop);
            return;
          }
        }
        const collectedStyles = context.collectedStyles.get(context.currentQuerySelector);
        const collectedEntry = collectedStyles.get(prop);
        let updateCollectedStyle = true;
        if (collectedEntry) {
          if (startTime != endTime && startTime >= collectedEntry.startTime && endTime <= collectedEntry.endTime) {
            context.errors.push(invalidParallelAnimation(prop, collectedEntry.startTime, collectedEntry.endTime, startTime, endTime));
            updateCollectedStyle = false;
          }
          startTime = collectedEntry.startTime;
        }
        if (updateCollectedStyle) {
          collectedStyles.set(prop, {
            startTime,
            endTime
          });
        }
        if (context.options) {
          validateStyleParams(value, context.options, context.errors);
        }
      });
    });
  }
  visitKeyframes(metadata, context) {
    const ast = {
      type: AnimationMetadataType.Keyframes,
      styles: [],
      options: null
    };
    if (!context.currentAnimateTimings) {
      context.errors.push(invalidKeyframes());
      return ast;
    }
    const MAX_KEYFRAME_OFFSET = 1;
    let totalKeyframesWithOffsets = 0;
    const offsets = [];
    let offsetsOutOfOrder = false;
    let keyframesOutOfRange = false;
    let previousOffset = 0;
    const keyframes = metadata.steps.map((styles) => {
      const style2 = this._makeStyleAst(styles, context);
      let offsetVal = style2.offset != null ? style2.offset : consumeOffset(style2.styles);
      let offset = 0;
      if (offsetVal != null) {
        totalKeyframesWithOffsets++;
        offset = style2.offset = offsetVal;
      }
      keyframesOutOfRange = keyframesOutOfRange || offset < 0 || offset > 1;
      offsetsOutOfOrder = offsetsOutOfOrder || offset < previousOffset;
      previousOffset = offset;
      offsets.push(offset);
      return style2;
    });
    if (keyframesOutOfRange) {
      context.errors.push(invalidOffset());
    }
    if (offsetsOutOfOrder) {
      context.errors.push(keyframeOffsetsOutOfOrder());
    }
    const length = metadata.steps.length;
    let generatedOffset = 0;
    if (totalKeyframesWithOffsets > 0 && totalKeyframesWithOffsets < length) {
      context.errors.push(keyframesMissingOffsets());
    } else if (totalKeyframesWithOffsets == 0) {
      generatedOffset = MAX_KEYFRAME_OFFSET / (length - 1);
    }
    const limit = length - 1;
    const currentTime = context.currentTime;
    const currentAnimateTimings = context.currentAnimateTimings;
    const animateDuration = currentAnimateTimings.duration;
    keyframes.forEach((kf, i) => {
      const offset = generatedOffset > 0 ? i == limit ? 1 : generatedOffset * i : offsets[i];
      const durationUpToThisFrame = offset * animateDuration;
      context.currentTime = currentTime + currentAnimateTimings.delay + durationUpToThisFrame;
      currentAnimateTimings.duration = durationUpToThisFrame;
      this._validateStyleAst(kf, context);
      kf.offset = offset;
      ast.styles.push(kf);
    });
    return ast;
  }
  visitReference(metadata, context) {
    return {
      type: AnimationMetadataType.Reference,
      animation: visitDslNode(this, normalizeAnimationEntry(metadata.animation), context),
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitAnimateChild(metadata, context) {
    context.depCount++;
    return {
      type: AnimationMetadataType.AnimateChild,
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitAnimateRef(metadata, context) {
    return {
      type: AnimationMetadataType.AnimateRef,
      animation: this.visitReference(metadata.animation, context),
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitQuery(metadata, context) {
    const parentSelector = context.currentQuerySelector;
    const options = metadata.options || {};
    context.queryCount++;
    context.currentQuery = metadata;
    const [selector, includeSelf] = normalizeSelector(metadata.selector);
    context.currentQuerySelector = parentSelector.length ? parentSelector + " " + selector : selector;
    getOrSetDefaultValue(context.collectedStyles, context.currentQuerySelector, /* @__PURE__ */ new Map());
    const animation = visitDslNode(this, normalizeAnimationEntry(metadata.animation), context);
    context.currentQuery = null;
    context.currentQuerySelector = parentSelector;
    return {
      type: AnimationMetadataType.Query,
      selector,
      limit: options.limit || 0,
      optional: !!options.optional,
      includeSelf,
      animation,
      originalSelector: metadata.selector,
      options: normalizeAnimationOptions(metadata.options)
    };
  }
  visitStagger(metadata, context) {
    if (!context.currentQuery) {
      context.errors.push(invalidStagger());
    }
    const timings = metadata.timings === "full" ? {
      duration: 0,
      delay: 0,
      easing: "full"
    } : resolveTiming(metadata.timings, context.errors, true);
    return {
      type: AnimationMetadataType.Stagger,
      animation: visitDslNode(this, normalizeAnimationEntry(metadata.animation), context),
      timings,
      options: null
    };
  }
};
function normalizeSelector(selector) {
  const hasAmpersand = selector.split(/\s*,\s*/).find((token) => token == SELF_TOKEN) ? true : false;
  if (hasAmpersand) {
    selector = selector.replace(SELF_TOKEN_REGEX, "");
  }
  selector = selector.replace(/@\*/g, NG_TRIGGER_SELECTOR).replace(/@\w+/g, (match) => NG_TRIGGER_SELECTOR + "-" + match.slice(1)).replace(/:animating/g, NG_ANIMATING_SELECTOR);
  return [selector, hasAmpersand];
}
function normalizeParams(obj) {
  return obj ? __spreadValues({}, obj) : null;
}
var AnimationAstBuilderContext = class {
  constructor(errors) {
    this.errors = errors;
    this.queryCount = 0;
    this.depCount = 0;
    this.currentTransition = null;
    this.currentQuery = null;
    this.currentQuerySelector = null;
    this.currentAnimateTimings = null;
    this.currentTime = 0;
    this.collectedStyles = /* @__PURE__ */ new Map();
    this.options = null;
    this.unsupportedCSSPropertiesFound = /* @__PURE__ */ new Set();
  }
};
function consumeOffset(styles) {
  if (typeof styles == "string") return null;
  let offset = null;
  if (Array.isArray(styles)) {
    styles.forEach((styleTuple) => {
      if (styleTuple instanceof Map && styleTuple.has("offset")) {
        const obj = styleTuple;
        offset = parseFloat(obj.get("offset"));
        obj.delete("offset");
      }
    });
  } else if (styles instanceof Map && styles.has("offset")) {
    const obj = styles;
    offset = parseFloat(obj.get("offset"));
    obj.delete("offset");
  }
  return offset;
}
function constructTimingAst(value, errors) {
  if (value.hasOwnProperty("duration")) {
    return value;
  }
  if (typeof value == "number") {
    const duration = resolveTiming(value, errors).duration;
    return makeTimingAst(duration, 0, "");
  }
  const strValue = value;
  const isDynamic = strValue.split(/\s+/).some((v) => v.charAt(0) == "{" && v.charAt(1) == "{");
  if (isDynamic) {
    const ast = makeTimingAst(0, 0, "");
    ast.dynamic = true;
    ast.strValue = strValue;
    return ast;
  }
  const timings = resolveTiming(strValue, errors);
  return makeTimingAst(timings.duration, timings.delay, timings.easing);
}
function normalizeAnimationOptions(options) {
  if (options) {
    options = __spreadValues({}, options);
    if (options["params"]) {
      options["params"] = normalizeParams(options["params"]);
    }
  } else {
    options = {};
  }
  return options;
}
function makeTimingAst(duration, delay, easing) {
  return {
    duration,
    delay,
    easing
  };
}
function createTimelineInstruction(element, keyframes, preStyleProps, postStyleProps, duration, delay, easing = null, subTimeline = false) {
  return {
    type: 1,
    element,
    keyframes,
    preStyleProps,
    postStyleProps,
    duration,
    delay,
    totalTime: duration + delay,
    easing,
    subTimeline
  };
}
var ElementInstructionMap = class {
  constructor() {
    this._map = /* @__PURE__ */ new Map();
  }
  get(element) {
    return this._map.get(element) || [];
  }
  append(element, instructions) {
    let existingInstructions = this._map.get(element);
    if (!existingInstructions) {
      this._map.set(element, existingInstructions = []);
    }
    existingInstructions.push(...instructions);
  }
  has(element) {
    return this._map.has(element);
  }
  clear() {
    this._map.clear();
  }
};
var ONE_FRAME_IN_MILLISECONDS = 1;
var ENTER_TOKEN = ":enter";
var ENTER_TOKEN_REGEX = new RegExp(ENTER_TOKEN, "g");
var LEAVE_TOKEN = ":leave";
var LEAVE_TOKEN_REGEX = new RegExp(LEAVE_TOKEN, "g");
function buildAnimationTimelines(driver, rootElement, ast, enterClassName, leaveClassName, startingStyles = /* @__PURE__ */ new Map(), finalStyles = /* @__PURE__ */ new Map(), options, subInstructions, errors = []) {
  return new AnimationTimelineBuilderVisitor().buildKeyframes(driver, rootElement, ast, enterClassName, leaveClassName, startingStyles, finalStyles, options, subInstructions, errors);
}
var AnimationTimelineBuilderVisitor = class {
  buildKeyframes(driver, rootElement, ast, enterClassName, leaveClassName, startingStyles, finalStyles, options, subInstructions, errors = []) {
    subInstructions = subInstructions || new ElementInstructionMap();
    const context = new AnimationTimelineContext(driver, rootElement, subInstructions, enterClassName, leaveClassName, errors, []);
    context.options = options;
    const delay = options.delay ? resolveTimingValue(options.delay) : 0;
    context.currentTimeline.delayNextStep(delay);
    context.currentTimeline.setStyles([startingStyles], null, context.errors, options);
    visitDslNode(this, ast, context);
    const timelines = context.timelines.filter((timeline) => timeline.containsAnimation());
    if (timelines.length && finalStyles.size) {
      let lastRootTimeline;
      for (let i = timelines.length - 1; i >= 0; i--) {
        const timeline = timelines[i];
        if (timeline.element === rootElement) {
          lastRootTimeline = timeline;
          break;
        }
      }
      if (lastRootTimeline && !lastRootTimeline.allowOnlyTimelineStyles()) {
        lastRootTimeline.setStyles([finalStyles], null, context.errors, options);
      }
    }
    return timelines.length ? timelines.map((timeline) => timeline.buildKeyframes()) : [createTimelineInstruction(rootElement, [], [], [], 0, delay, "", false)];
  }
  visitTrigger(ast, context) {
  }
  visitState(ast, context) {
  }
  visitTransition(ast, context) {
  }
  visitAnimateChild(ast, context) {
    const elementInstructions = context.subInstructions.get(context.element);
    if (elementInstructions) {
      const innerContext = context.createSubContext(ast.options);
      const startTime = context.currentTimeline.currentTime;
      const endTime = this._visitSubInstructions(elementInstructions, innerContext, innerContext.options);
      if (startTime != endTime) {
        context.transformIntoNewTimeline(endTime);
      }
    }
    context.previousNode = ast;
  }
  visitAnimateRef(ast, context) {
    const innerContext = context.createSubContext(ast.options);
    innerContext.transformIntoNewTimeline();
    this._applyAnimationRefDelays([ast.options, ast.animation.options], context, innerContext);
    this.visitReference(ast.animation, innerContext);
    context.transformIntoNewTimeline(innerContext.currentTimeline.currentTime);
    context.previousNode = ast;
  }
  _applyAnimationRefDelays(animationsRefsOptions, context, innerContext) {
    for (const animationRefOptions of animationsRefsOptions) {
      const animationDelay = animationRefOptions?.delay;
      if (animationDelay) {
        const animationDelayValue = typeof animationDelay === "number" ? animationDelay : resolveTimingValue(interpolateParams(animationDelay, animationRefOptions?.params ?? {}, context.errors));
        innerContext.delayNextStep(animationDelayValue);
      }
    }
  }
  _visitSubInstructions(instructions, context, options) {
    const startTime = context.currentTimeline.currentTime;
    let furthestTime = startTime;
    const duration = options.duration != null ? resolveTimingValue(options.duration) : null;
    const delay = options.delay != null ? resolveTimingValue(options.delay) : null;
    if (duration !== 0) {
      instructions.forEach((instruction) => {
        const instructionTimings = context.appendInstructionToTimeline(instruction, duration, delay);
        furthestTime = Math.max(furthestTime, instructionTimings.duration + instructionTimings.delay);
      });
    }
    return furthestTime;
  }
  visitReference(ast, context) {
    context.updateOptions(ast.options, true);
    visitDslNode(this, ast.animation, context);
    context.previousNode = ast;
  }
  visitSequence(ast, context) {
    const subContextCount = context.subContextCount;
    let ctx = context;
    const options = ast.options;
    if (options && (options.params || options.delay)) {
      ctx = context.createSubContext(options);
      ctx.transformIntoNewTimeline();
      if (options.delay != null) {
        if (ctx.previousNode.type == AnimationMetadataType.Style) {
          ctx.currentTimeline.snapshotCurrentStyles();
          ctx.previousNode = DEFAULT_NOOP_PREVIOUS_NODE;
        }
        const delay = resolveTimingValue(options.delay);
        ctx.delayNextStep(delay);
      }
    }
    if (ast.steps.length) {
      ast.steps.forEach((s) => visitDslNode(this, s, ctx));
      ctx.currentTimeline.applyStylesToKeyframe();
      if (ctx.subContextCount > subContextCount) {
        ctx.transformIntoNewTimeline();
      }
    }
    context.previousNode = ast;
  }
  visitGroup(ast, context) {
    const innerTimelines = [];
    let furthestTime = context.currentTimeline.currentTime;
    const delay = ast.options && ast.options.delay ? resolveTimingValue(ast.options.delay) : 0;
    ast.steps.forEach((s) => {
      const innerContext = context.createSubContext(ast.options);
      if (delay) {
        innerContext.delayNextStep(delay);
      }
      visitDslNode(this, s, innerContext);
      furthestTime = Math.max(furthestTime, innerContext.currentTimeline.currentTime);
      innerTimelines.push(innerContext.currentTimeline);
    });
    innerTimelines.forEach((timeline) => context.currentTimeline.mergeTimelineCollectedStyles(timeline));
    context.transformIntoNewTimeline(furthestTime);
    context.previousNode = ast;
  }
  _visitTiming(ast, context) {
    if (ast.dynamic) {
      const strValue = ast.strValue;
      const timingValue = context.params ? interpolateParams(strValue, context.params, context.errors) : strValue;
      return resolveTiming(timingValue, context.errors);
    } else {
      return {
        duration: ast.duration,
        delay: ast.delay,
        easing: ast.easing
      };
    }
  }
  visitAnimate(ast, context) {
    const timings = context.currentAnimateTimings = this._visitTiming(ast.timings, context);
    const timeline = context.currentTimeline;
    if (timings.delay) {
      context.incrementTime(timings.delay);
      timeline.snapshotCurrentStyles();
    }
    const style2 = ast.style;
    if (style2.type == AnimationMetadataType.Keyframes) {
      this.visitKeyframes(style2, context);
    } else {
      context.incrementTime(timings.duration);
      this.visitStyle(style2, context);
      timeline.applyStylesToKeyframe();
    }
    context.currentAnimateTimings = null;
    context.previousNode = ast;
  }
  visitStyle(ast, context) {
    const timeline = context.currentTimeline;
    const timings = context.currentAnimateTimings;
    if (!timings && timeline.hasCurrentStyleProperties()) {
      timeline.forwardFrame();
    }
    const easing = timings && timings.easing || ast.easing;
    if (ast.isEmptyStep) {
      timeline.applyEmptyStep(easing);
    } else {
      timeline.setStyles(ast.styles, easing, context.errors, context.options);
    }
    context.previousNode = ast;
  }
  visitKeyframes(ast, context) {
    const currentAnimateTimings = context.currentAnimateTimings;
    const startTime = context.currentTimeline.duration;
    const duration = currentAnimateTimings.duration;
    const innerContext = context.createSubContext();
    const innerTimeline = innerContext.currentTimeline;
    innerTimeline.easing = currentAnimateTimings.easing;
    ast.styles.forEach((step) => {
      const offset = step.offset || 0;
      innerTimeline.forwardTime(offset * duration);
      innerTimeline.setStyles(step.styles, step.easing, context.errors, context.options);
      innerTimeline.applyStylesToKeyframe();
    });
    context.currentTimeline.mergeTimelineCollectedStyles(innerTimeline);
    context.transformIntoNewTimeline(startTime + duration);
    context.previousNode = ast;
  }
  visitQuery(ast, context) {
    const startTime = context.currentTimeline.currentTime;
    const options = ast.options || {};
    const delay = options.delay ? resolveTimingValue(options.delay) : 0;
    if (delay && (context.previousNode.type === AnimationMetadataType.Style || startTime == 0 && context.currentTimeline.hasCurrentStyleProperties())) {
      context.currentTimeline.snapshotCurrentStyles();
      context.previousNode = DEFAULT_NOOP_PREVIOUS_NODE;
    }
    let furthestTime = startTime;
    const elms = context.invokeQuery(ast.selector, ast.originalSelector, ast.limit, ast.includeSelf, options.optional ? true : false, context.errors);
    context.currentQueryTotal = elms.length;
    let sameElementTimeline = null;
    elms.forEach((element, i) => {
      context.currentQueryIndex = i;
      const innerContext = context.createSubContext(ast.options, element);
      if (delay) {
        innerContext.delayNextStep(delay);
      }
      if (element === context.element) {
        sameElementTimeline = innerContext.currentTimeline;
      }
      visitDslNode(this, ast.animation, innerContext);
      innerContext.currentTimeline.applyStylesToKeyframe();
      const endTime = innerContext.currentTimeline.currentTime;
      furthestTime = Math.max(furthestTime, endTime);
    });
    context.currentQueryIndex = 0;
    context.currentQueryTotal = 0;
    context.transformIntoNewTimeline(furthestTime);
    if (sameElementTimeline) {
      context.currentTimeline.mergeTimelineCollectedStyles(sameElementTimeline);
      context.currentTimeline.snapshotCurrentStyles();
    }
    context.previousNode = ast;
  }
  visitStagger(ast, context) {
    const parentContext = context.parentContext;
    const tl = context.currentTimeline;
    const timings = ast.timings;
    const duration = Math.abs(timings.duration);
    const maxTime = duration * (context.currentQueryTotal - 1);
    let delay = duration * context.currentQueryIndex;
    let staggerTransformer = timings.duration < 0 ? "reverse" : timings.easing;
    switch (staggerTransformer) {
      case "reverse":
        delay = maxTime - delay;
        break;
      case "full":
        delay = parentContext.currentStaggerTime;
        break;
    }
    const timeline = context.currentTimeline;
    if (delay) {
      timeline.delayNextStep(delay);
    }
    const startingTime = timeline.currentTime;
    visitDslNode(this, ast.animation, context);
    context.previousNode = ast;
    parentContext.currentStaggerTime = tl.currentTime - startingTime + (tl.startTime - parentContext.currentTimeline.startTime);
  }
};
var DEFAULT_NOOP_PREVIOUS_NODE = {};
var AnimationTimelineContext = class _AnimationTimelineContext {
  constructor(_driver, element, subInstructions, _enterClassName, _leaveClassName, errors, timelines, initialTimeline) {
    this._driver = _driver;
    this.element = element;
    this.subInstructions = subInstructions;
    this._enterClassName = _enterClassName;
    this._leaveClassName = _leaveClassName;
    this.errors = errors;
    this.timelines = timelines;
    this.parentContext = null;
    this.currentAnimateTimings = null;
    this.previousNode = DEFAULT_NOOP_PREVIOUS_NODE;
    this.subContextCount = 0;
    this.options = {};
    this.currentQueryIndex = 0;
    this.currentQueryTotal = 0;
    this.currentStaggerTime = 0;
    this.currentTimeline = initialTimeline || new TimelineBuilder(this._driver, element, 0);
    timelines.push(this.currentTimeline);
  }
  get params() {
    return this.options.params;
  }
  updateOptions(options, skipIfExists) {
    if (!options) return;
    const newOptions = options;
    let optionsToUpdate = this.options;
    if (newOptions.duration != null) {
      optionsToUpdate.duration = resolveTimingValue(newOptions.duration);
    }
    if (newOptions.delay != null) {
      optionsToUpdate.delay = resolveTimingValue(newOptions.delay);
    }
    const newParams = newOptions.params;
    if (newParams) {
      let paramsToUpdate = optionsToUpdate.params;
      if (!paramsToUpdate) {
        paramsToUpdate = this.options.params = {};
      }
      Object.keys(newParams).forEach((name) => {
        if (!skipIfExists || !paramsToUpdate.hasOwnProperty(name)) {
          paramsToUpdate[name] = interpolateParams(newParams[name], paramsToUpdate, this.errors);
        }
      });
    }
  }
  _copyOptions() {
    const options = {};
    if (this.options) {
      const oldParams = this.options.params;
      if (oldParams) {
        const params = options["params"] = {};
        Object.keys(oldParams).forEach((name) => {
          params[name] = oldParams[name];
        });
      }
    }
    return options;
  }
  createSubContext(options = null, element, newTime) {
    const target = element || this.element;
    const context = new _AnimationTimelineContext(this._driver, target, this.subInstructions, this._enterClassName, this._leaveClassName, this.errors, this.timelines, this.currentTimeline.fork(target, newTime || 0));
    context.previousNode = this.previousNode;
    context.currentAnimateTimings = this.currentAnimateTimings;
    context.options = this._copyOptions();
    context.updateOptions(options);
    context.currentQueryIndex = this.currentQueryIndex;
    context.currentQueryTotal = this.currentQueryTotal;
    context.parentContext = this;
    this.subContextCount++;
    return context;
  }
  transformIntoNewTimeline(newTime) {
    this.previousNode = DEFAULT_NOOP_PREVIOUS_NODE;
    this.currentTimeline = this.currentTimeline.fork(this.element, newTime);
    this.timelines.push(this.currentTimeline);
    return this.currentTimeline;
  }
  appendInstructionToTimeline(instruction, duration, delay) {
    const updatedTimings = {
      duration: duration != null ? duration : instruction.duration,
      delay: this.currentTimeline.currentTime + (delay != null ? delay : 0) + instruction.delay,
      easing: ""
    };
    const builder = new SubTimelineBuilder(this._driver, instruction.element, instruction.keyframes, instruction.preStyleProps, instruction.postStyleProps, updatedTimings, instruction.stretchStartingKeyframe);
    this.timelines.push(builder);
    return updatedTimings;
  }
  incrementTime(time) {
    this.currentTimeline.forwardTime(this.currentTimeline.duration + time);
  }
  delayNextStep(delay) {
    if (delay > 0) {
      this.currentTimeline.delayNextStep(delay);
    }
  }
  invokeQuery(selector, originalSelector, limit, includeSelf, optional, errors) {
    let results = [];
    if (includeSelf) {
      results.push(this.element);
    }
    if (selector.length > 0) {
      selector = selector.replace(ENTER_TOKEN_REGEX, "." + this._enterClassName);
      selector = selector.replace(LEAVE_TOKEN_REGEX, "." + this._leaveClassName);
      const multi = limit != 1;
      let elements = this._driver.query(this.element, selector, multi);
      if (limit !== 0) {
        elements = limit < 0 ? elements.slice(elements.length + limit, elements.length) : elements.slice(0, limit);
      }
      results.push(...elements);
    }
    if (!optional && results.length == 0) {
      errors.push(invalidQuery(originalSelector));
    }
    return results;
  }
};
var TimelineBuilder = class _TimelineBuilder {
  constructor(_driver, element, startTime, _elementTimelineStylesLookup) {
    this._driver = _driver;
    this.element = element;
    this.startTime = startTime;
    this._elementTimelineStylesLookup = _elementTimelineStylesLookup;
    this.duration = 0;
    this.easing = null;
    this._previousKeyframe = /* @__PURE__ */ new Map();
    this._currentKeyframe = /* @__PURE__ */ new Map();
    this._keyframes = /* @__PURE__ */ new Map();
    this._styleSummary = /* @__PURE__ */ new Map();
    this._localTimelineStyles = /* @__PURE__ */ new Map();
    this._pendingStyles = /* @__PURE__ */ new Map();
    this._backFill = /* @__PURE__ */ new Map();
    this._currentEmptyStepKeyframe = null;
    if (!this._elementTimelineStylesLookup) {
      this._elementTimelineStylesLookup = /* @__PURE__ */ new Map();
    }
    this._globalTimelineStyles = this._elementTimelineStylesLookup.get(element);
    if (!this._globalTimelineStyles) {
      this._globalTimelineStyles = this._localTimelineStyles;
      this._elementTimelineStylesLookup.set(element, this._localTimelineStyles);
    }
    this._loadKeyframe();
  }
  containsAnimation() {
    switch (this._keyframes.size) {
      case 0:
        return false;
      case 1:
        return this.hasCurrentStyleProperties();
      default:
        return true;
    }
  }
  hasCurrentStyleProperties() {
    return this._currentKeyframe.size > 0;
  }
  get currentTime() {
    return this.startTime + this.duration;
  }
  delayNextStep(delay) {
    const hasPreStyleStep = this._keyframes.size === 1 && this._pendingStyles.size;
    if (this.duration || hasPreStyleStep) {
      this.forwardTime(this.currentTime + delay);
      if (hasPreStyleStep) {
        this.snapshotCurrentStyles();
      }
    } else {
      this.startTime += delay;
    }
  }
  fork(element, currentTime) {
    this.applyStylesToKeyframe();
    return new _TimelineBuilder(this._driver, element, currentTime || this.currentTime, this._elementTimelineStylesLookup);
  }
  _loadKeyframe() {
    if (this._currentKeyframe) {
      this._previousKeyframe = this._currentKeyframe;
    }
    this._currentKeyframe = this._keyframes.get(this.duration);
    if (!this._currentKeyframe) {
      this._currentKeyframe = /* @__PURE__ */ new Map();
      this._keyframes.set(this.duration, this._currentKeyframe);
    }
  }
  forwardFrame() {
    this.duration += ONE_FRAME_IN_MILLISECONDS;
    this._loadKeyframe();
  }
  forwardTime(time) {
    this.applyStylesToKeyframe();
    this.duration = time;
    this._loadKeyframe();
  }
  _updateStyle(prop, value) {
    this._localTimelineStyles.set(prop, value);
    this._globalTimelineStyles.set(prop, value);
    this._styleSummary.set(prop, {
      time: this.currentTime,
      value
    });
  }
  allowOnlyTimelineStyles() {
    return this._currentEmptyStepKeyframe !== this._currentKeyframe;
  }
  applyEmptyStep(easing) {
    if (easing) {
      this._previousKeyframe.set("easing", easing);
    }
    for (let [prop, value] of this._globalTimelineStyles) {
      this._backFill.set(prop, value || AUTO_STYLE);
      this._currentKeyframe.set(prop, AUTO_STYLE);
    }
    this._currentEmptyStepKeyframe = this._currentKeyframe;
  }
  setStyles(input, easing, errors, options) {
    if (easing) {
      this._previousKeyframe.set("easing", easing);
    }
    const params = options && options.params || {};
    const styles = flattenStyles(input, this._globalTimelineStyles);
    for (let [prop, value] of styles) {
      const val = interpolateParams(value, params, errors);
      this._pendingStyles.set(prop, val);
      if (!this._localTimelineStyles.has(prop)) {
        this._backFill.set(prop, this._globalTimelineStyles.get(prop) ?? AUTO_STYLE);
      }
      this._updateStyle(prop, val);
    }
  }
  applyStylesToKeyframe() {
    if (this._pendingStyles.size == 0) return;
    this._pendingStyles.forEach((val, prop) => {
      this._currentKeyframe.set(prop, val);
    });
    this._pendingStyles.clear();
    this._localTimelineStyles.forEach((val, prop) => {
      if (!this._currentKeyframe.has(prop)) {
        this._currentKeyframe.set(prop, val);
      }
    });
  }
  snapshotCurrentStyles() {
    for (let [prop, val] of this._localTimelineStyles) {
      this._pendingStyles.set(prop, val);
      this._updateStyle(prop, val);
    }
  }
  getFinalKeyframe() {
    return this._keyframes.get(this.duration);
  }
  get properties() {
    const properties = [];
    for (let prop in this._currentKeyframe) {
      properties.push(prop);
    }
    return properties;
  }
  mergeTimelineCollectedStyles(timeline) {
    timeline._styleSummary.forEach((details1, prop) => {
      const details0 = this._styleSummary.get(prop);
      if (!details0 || details1.time > details0.time) {
        this._updateStyle(prop, details1.value);
      }
    });
  }
  buildKeyframes() {
    this.applyStylesToKeyframe();
    const preStyleProps = /* @__PURE__ */ new Set();
    const postStyleProps = /* @__PURE__ */ new Set();
    const isEmpty = this._keyframes.size === 1 && this.duration === 0;
    let finalKeyframes = [];
    this._keyframes.forEach((keyframe, time) => {
      const finalKeyframe = new Map([...this._backFill, ...keyframe]);
      finalKeyframe.forEach((value, prop) => {
        if (value === \u0275PRE_STYLE) {
          preStyleProps.add(prop);
        } else if (value === AUTO_STYLE) {
          postStyleProps.add(prop);
        }
      });
      if (!isEmpty) {
        finalKeyframe.set("offset", time / this.duration);
      }
      finalKeyframes.push(finalKeyframe);
    });
    const preProps = [...preStyleProps.values()];
    const postProps = [...postStyleProps.values()];
    if (isEmpty) {
      const kf0 = finalKeyframes[0];
      const kf1 = new Map(kf0);
      kf0.set("offset", 0);
      kf1.set("offset", 1);
      finalKeyframes = [kf0, kf1];
    }
    return createTimelineInstruction(this.element, finalKeyframes, preProps, postProps, this.duration, this.startTime, this.easing, false);
  }
};
var SubTimelineBuilder = class extends TimelineBuilder {
  constructor(driver, element, keyframes, preStyleProps, postStyleProps, timings, _stretchStartingKeyframe = false) {
    super(driver, element, timings.delay);
    this.keyframes = keyframes;
    this.preStyleProps = preStyleProps;
    this.postStyleProps = postStyleProps;
    this._stretchStartingKeyframe = _stretchStartingKeyframe;
    this.timings = {
      duration: timings.duration,
      delay: timings.delay,
      easing: timings.easing
    };
  }
  containsAnimation() {
    return this.keyframes.length > 1;
  }
  buildKeyframes() {
    let keyframes = this.keyframes;
    let {
      delay,
      duration,
      easing
    } = this.timings;
    if (this._stretchStartingKeyframe && delay) {
      const newKeyframes = [];
      const totalTime = duration + delay;
      const startingGap = delay / totalTime;
      const newFirstKeyframe = new Map(keyframes[0]);
      newFirstKeyframe.set("offset", 0);
      newKeyframes.push(newFirstKeyframe);
      const oldFirstKeyframe = new Map(keyframes[0]);
      oldFirstKeyframe.set("offset", roundOffset(startingGap));
      newKeyframes.push(oldFirstKeyframe);
      const limit = keyframes.length - 1;
      for (let i = 1; i <= limit; i++) {
        let kf = new Map(keyframes[i]);
        const oldOffset = kf.get("offset");
        const timeAtKeyframe = delay + oldOffset * duration;
        kf.set("offset", roundOffset(timeAtKeyframe / totalTime));
        newKeyframes.push(kf);
      }
      duration = totalTime;
      delay = 0;
      easing = "";
      keyframes = newKeyframes;
    }
    return createTimelineInstruction(this.element, keyframes, this.preStyleProps, this.postStyleProps, duration, delay, easing, true);
  }
};
function roundOffset(offset, decimalPoints = 3) {
  const mult = Math.pow(10, decimalPoints - 1);
  return Math.round(offset * mult) / mult;
}
function flattenStyles(input, allStyles) {
  const styles = /* @__PURE__ */ new Map();
  let allProperties;
  input.forEach((token) => {
    if (token === "*") {
      allProperties ??= allStyles.keys();
      for (let prop of allProperties) {
        styles.set(prop, AUTO_STYLE);
      }
    } else {
      for (let [prop, val] of token) {
        styles.set(prop, val);
      }
    }
  });
  return styles;
}
function createTransitionInstruction(element, triggerName, fromState, toState, isRemovalTransition, fromStyles, toStyles, timelines, queriedElements, preStyleProps, postStyleProps, totalTime, errors) {
  return {
    type: 0,
    element,
    triggerName,
    isRemovalTransition,
    fromState,
    fromStyles,
    toState,
    toStyles,
    timelines,
    queriedElements,
    preStyleProps,
    postStyleProps,
    totalTime,
    errors
  };
}
var EMPTY_OBJECT = {};
var AnimationTransitionFactory = class {
  constructor(_triggerName, ast, _stateStyles) {
    this._triggerName = _triggerName;
    this.ast = ast;
    this._stateStyles = _stateStyles;
  }
  match(currentState, nextState, element, params) {
    return oneOrMoreTransitionsMatch(this.ast.matchers, currentState, nextState, element, params);
  }
  buildStyles(stateName, params, errors) {
    let styler = this._stateStyles.get("*");
    if (stateName !== void 0) {
      styler = this._stateStyles.get(stateName?.toString()) || styler;
    }
    return styler ? styler.buildStyles(params, errors) : /* @__PURE__ */ new Map();
  }
  build(driver, element, currentState, nextState, enterClassName, leaveClassName, currentOptions, nextOptions, subInstructions, skipAstBuild) {
    const errors = [];
    const transitionAnimationParams = this.ast.options && this.ast.options.params || EMPTY_OBJECT;
    const currentAnimationParams = currentOptions && currentOptions.params || EMPTY_OBJECT;
    const currentStateStyles = this.buildStyles(currentState, currentAnimationParams, errors);
    const nextAnimationParams = nextOptions && nextOptions.params || EMPTY_OBJECT;
    const nextStateStyles = this.buildStyles(nextState, nextAnimationParams, errors);
    const queriedElements = /* @__PURE__ */ new Set();
    const preStyleMap = /* @__PURE__ */ new Map();
    const postStyleMap = /* @__PURE__ */ new Map();
    const isRemoval = nextState === "void";
    const animationOptions = {
      params: applyParamDefaults(nextAnimationParams, transitionAnimationParams),
      delay: this.ast.options?.delay
    };
    const timelines = skipAstBuild ? [] : buildAnimationTimelines(driver, element, this.ast.animation, enterClassName, leaveClassName, currentStateStyles, nextStateStyles, animationOptions, subInstructions, errors);
    let totalTime = 0;
    timelines.forEach((tl) => {
      totalTime = Math.max(tl.duration + tl.delay, totalTime);
    });
    if (errors.length) {
      return createTransitionInstruction(element, this._triggerName, currentState, nextState, isRemoval, currentStateStyles, nextStateStyles, [], [], preStyleMap, postStyleMap, totalTime, errors);
    }
    timelines.forEach((tl) => {
      const elm = tl.element;
      const preProps = getOrSetDefaultValue(preStyleMap, elm, /* @__PURE__ */ new Set());
      tl.preStyleProps.forEach((prop) => preProps.add(prop));
      const postProps = getOrSetDefaultValue(postStyleMap, elm, /* @__PURE__ */ new Set());
      tl.postStyleProps.forEach((prop) => postProps.add(prop));
      if (elm !== element) {
        queriedElements.add(elm);
      }
    });
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      checkNonAnimatableInTimelines(timelines, this._triggerName, driver);
    }
    return createTransitionInstruction(element, this._triggerName, currentState, nextState, isRemoval, currentStateStyles, nextStateStyles, timelines, [...queriedElements.values()], preStyleMap, postStyleMap, totalTime);
  }
};
function checkNonAnimatableInTimelines(timelines, triggerName, driver) {
  if (!driver.validateAnimatableStyleProperty) {
    return;
  }
  const allowedNonAnimatableProps = /* @__PURE__ */ new Set([
    // 'easing' is a utility/synthetic prop we use to represent
    // easing functions, it represents a property of the animation
    // which is not animatable but different values can be used
    // in different steps
    "easing"
  ]);
  const invalidNonAnimatableProps = /* @__PURE__ */ new Set();
  timelines.forEach(({
    keyframes
  }) => {
    const nonAnimatablePropsInitialValues = /* @__PURE__ */ new Map();
    keyframes.forEach((keyframe) => {
      const entriesToCheck = Array.from(keyframe.entries()).filter(([prop]) => !allowedNonAnimatableProps.has(prop));
      for (const [prop, value] of entriesToCheck) {
        if (!driver.validateAnimatableStyleProperty(prop)) {
          if (nonAnimatablePropsInitialValues.has(prop) && !invalidNonAnimatableProps.has(prop)) {
            const propInitialValue = nonAnimatablePropsInitialValues.get(prop);
            if (propInitialValue !== value) {
              invalidNonAnimatableProps.add(prop);
            }
          } else {
            nonAnimatablePropsInitialValues.set(prop, value);
          }
        }
      }
    });
  });
  if (invalidNonAnimatableProps.size > 0) {
    console.warn(`Warning: The animation trigger "${triggerName}" is attempting to animate the following not animatable properties: ` + Array.from(invalidNonAnimatableProps).join(", ") + "\n(to check the list of all animatable properties visit https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_animated_properties)");
  }
}
function oneOrMoreTransitionsMatch(matchFns, currentState, nextState, element, params) {
  return matchFns.some((fn) => fn(currentState, nextState, element, params));
}
function applyParamDefaults(userParams, defaults) {
  const result = __spreadValues({}, defaults);
  Object.entries(userParams).forEach(([key, value]) => {
    if (value != null) {
      result[key] = value;
    }
  });
  return result;
}
var AnimationStateStyles = class {
  constructor(styles, defaultParams, normalizer) {
    this.styles = styles;
    this.defaultParams = defaultParams;
    this.normalizer = normalizer;
  }
  buildStyles(params, errors) {
    const finalStyles = /* @__PURE__ */ new Map();
    const combinedParams = applyParamDefaults(params, this.defaultParams);
    this.styles.styles.forEach((value) => {
      if (typeof value !== "string") {
        value.forEach((val, prop) => {
          if (val) {
            val = interpolateParams(val, combinedParams, errors);
          }
          const normalizedProp = this.normalizer.normalizePropertyName(prop, errors);
          val = this.normalizer.normalizeStyleValue(prop, normalizedProp, val, errors);
          finalStyles.set(prop, val);
        });
      }
    });
    return finalStyles;
  }
};
function buildTrigger(name, ast, normalizer) {
  return new AnimationTrigger(name, ast, normalizer);
}
var AnimationTrigger = class {
  constructor(name, ast, _normalizer) {
    this.name = name;
    this.ast = ast;
    this._normalizer = _normalizer;
    this.transitionFactories = [];
    this.states = /* @__PURE__ */ new Map();
    ast.states.forEach((ast2) => {
      const defaultParams = ast2.options && ast2.options.params || {};
      this.states.set(ast2.name, new AnimationStateStyles(ast2.style, defaultParams, _normalizer));
    });
    balanceProperties(this.states, "true", "1");
    balanceProperties(this.states, "false", "0");
    ast.transitions.forEach((ast2) => {
      this.transitionFactories.push(new AnimationTransitionFactory(name, ast2, this.states));
    });
    this.fallbackTransition = createFallbackTransition(name, this.states, this._normalizer);
  }
  get containsQueries() {
    return this.ast.queryCount > 0;
  }
  matchTransition(currentState, nextState, element, params) {
    const entry = this.transitionFactories.find((f) => f.match(currentState, nextState, element, params));
    return entry || null;
  }
  matchStyles(currentState, params, errors) {
    return this.fallbackTransition.buildStyles(currentState, params, errors);
  }
};
function createFallbackTransition(triggerName, states, normalizer) {
  const matchers = [(fromState, toState) => true];
  const animation = {
    type: AnimationMetadataType.Sequence,
    steps: [],
    options: null
  };
  const transition = {
    type: AnimationMetadataType.Transition,
    animation,
    matchers,
    options: null,
    queryCount: 0,
    depCount: 0
  };
  return new AnimationTransitionFactory(triggerName, transition, states);
}
function balanceProperties(stateMap, key1, key2) {
  if (stateMap.has(key1)) {
    if (!stateMap.has(key2)) {
      stateMap.set(key2, stateMap.get(key1));
    }
  } else if (stateMap.has(key2)) {
    stateMap.set(key1, stateMap.get(key2));
  }
}
var EMPTY_INSTRUCTION_MAP = new ElementInstructionMap();
var TimelineAnimationEngine = class {
  constructor(bodyNode, _driver, _normalizer) {
    this.bodyNode = bodyNode;
    this._driver = _driver;
    this._normalizer = _normalizer;
    this._animations = /* @__PURE__ */ new Map();
    this._playersById = /* @__PURE__ */ new Map();
    this.players = [];
  }
  register(id, metadata) {
    const errors = [];
    const warnings = [];
    const ast = buildAnimationAst(this._driver, metadata, errors, warnings);
    if (errors.length) {
      throw registerFailed(errors);
    } else {
      if (warnings.length) {
        warnRegister(warnings);
      }
      this._animations.set(id, ast);
    }
  }
  _buildPlayer(i, preStyles, postStyles) {
    const element = i.element;
    const keyframes = normalizeKeyframes$1(this._normalizer, i.keyframes, preStyles, postStyles);
    return this._driver.animate(element, keyframes, i.duration, i.delay, i.easing, [], true);
  }
  create(id, element, options = {}) {
    const errors = [];
    const ast = this._animations.get(id);
    let instructions;
    const autoStylesMap = /* @__PURE__ */ new Map();
    if (ast) {
      instructions = buildAnimationTimelines(this._driver, element, ast, ENTER_CLASSNAME, LEAVE_CLASSNAME, /* @__PURE__ */ new Map(), /* @__PURE__ */ new Map(), options, EMPTY_INSTRUCTION_MAP, errors);
      instructions.forEach((inst) => {
        const styles = getOrSetDefaultValue(autoStylesMap, inst.element, /* @__PURE__ */ new Map());
        inst.postStyleProps.forEach((prop) => styles.set(prop, null));
      });
    } else {
      errors.push(missingOrDestroyedAnimation());
      instructions = [];
    }
    if (errors.length) {
      throw createAnimationFailed(errors);
    }
    autoStylesMap.forEach((styles, element2) => {
      styles.forEach((_, prop) => {
        styles.set(prop, this._driver.computeStyle(element2, prop, AUTO_STYLE));
      });
    });
    const players = instructions.map((i) => {
      const styles = autoStylesMap.get(i.element);
      return this._buildPlayer(i, /* @__PURE__ */ new Map(), styles);
    });
    const player = optimizeGroupPlayer(players);
    this._playersById.set(id, player);
    player.onDestroy(() => this.destroy(id));
    this.players.push(player);
    return player;
  }
  destroy(id) {
    const player = this._getPlayer(id);
    player.destroy();
    this._playersById.delete(id);
    const index = this.players.indexOf(player);
    if (index >= 0) {
      this.players.splice(index, 1);
    }
  }
  _getPlayer(id) {
    const player = this._playersById.get(id);
    if (!player) {
      throw missingPlayer(id);
    }
    return player;
  }
  listen(id, element, eventName, callback) {
    const baseEvent = makeAnimationEvent(element, "", "", "");
    listenOnPlayer(this._getPlayer(id), eventName, baseEvent, callback);
    return () => {
    };
  }
  command(id, element, command, args) {
    if (command == "register") {
      this.register(id, args[0]);
      return;
    }
    if (command == "create") {
      const options = args[0] || {};
      this.create(id, element, options);
      return;
    }
    const player = this._getPlayer(id);
    switch (command) {
      case "play":
        player.play();
        break;
      case "pause":
        player.pause();
        break;
      case "reset":
        player.reset();
        break;
      case "restart":
        player.restart();
        break;
      case "finish":
        player.finish();
        break;
      case "init":
        player.init();
        break;
      case "setPosition":
        player.setPosition(parseFloat(args[0]));
        break;
      case "destroy":
        this.destroy(id);
        break;
    }
  }
};
var QUEUED_CLASSNAME = "ng-animate-queued";
var QUEUED_SELECTOR = ".ng-animate-queued";
var DISABLED_CLASSNAME = "ng-animate-disabled";
var DISABLED_SELECTOR = ".ng-animate-disabled";
var STAR_CLASSNAME = "ng-star-inserted";
var STAR_SELECTOR = ".ng-star-inserted";
var EMPTY_PLAYER_ARRAY = [];
var NULL_REMOVAL_STATE = {
  namespaceId: "",
  setForRemoval: false,
  setForMove: false,
  hasAnimation: false,
  removedBeforeQueried: false
};
var NULL_REMOVED_QUERIED_STATE = {
  namespaceId: "",
  setForMove: false,
  setForRemoval: false,
  hasAnimation: false,
  removedBeforeQueried: true
};
var REMOVAL_FLAG = "__ng_removed";
var StateValue = class {
  get params() {
    return this.options.params;
  }
  constructor(input, namespaceId = "") {
    this.namespaceId = namespaceId;
    const isObj = input && input.hasOwnProperty("value");
    const value = isObj ? input["value"] : input;
    this.value = normalizeTriggerValue(value);
    if (isObj) {
      const _a = input, {
        value: value2
      } = _a, options = __objRest(_a, [
        "value"
      ]);
      this.options = options;
    } else {
      this.options = {};
    }
    if (!this.options.params) {
      this.options.params = {};
    }
  }
  absorbOptions(options) {
    const newParams = options.params;
    if (newParams) {
      const oldParams = this.options.params;
      Object.keys(newParams).forEach((prop) => {
        if (oldParams[prop] == null) {
          oldParams[prop] = newParams[prop];
        }
      });
    }
  }
};
var VOID_VALUE = "void";
var DEFAULT_STATE_VALUE = new StateValue(VOID_VALUE);
var AnimationTransitionNamespace = class {
  constructor(id, hostElement, _engine) {
    this.id = id;
    this.hostElement = hostElement;
    this._engine = _engine;
    this.players = [];
    this._triggers = /* @__PURE__ */ new Map();
    this._queue = [];
    this._elementListeners = /* @__PURE__ */ new Map();
    this._hostClassName = "ng-tns-" + id;
    addClass(hostElement, this._hostClassName);
  }
  listen(element, name, phase, callback) {
    if (!this._triggers.has(name)) {
      throw missingTrigger(phase, name);
    }
    if (phase == null || phase.length == 0) {
      throw missingEvent(name);
    }
    if (!isTriggerEventValid(phase)) {
      throw unsupportedTriggerEvent(phase, name);
    }
    const listeners = getOrSetDefaultValue(this._elementListeners, element, []);
    const data = {
      name,
      phase,
      callback
    };
    listeners.push(data);
    const triggersWithStates = getOrSetDefaultValue(this._engine.statesByElement, element, /* @__PURE__ */ new Map());
    if (!triggersWithStates.has(name)) {
      addClass(element, NG_TRIGGER_CLASSNAME);
      addClass(element, NG_TRIGGER_CLASSNAME + "-" + name);
      triggersWithStates.set(name, DEFAULT_STATE_VALUE);
    }
    return () => {
      this._engine.afterFlush(() => {
        const index = listeners.indexOf(data);
        if (index >= 0) {
          listeners.splice(index, 1);
        }
        if (!this._triggers.has(name)) {
          triggersWithStates.delete(name);
        }
      });
    };
  }
  register(name, ast) {
    if (this._triggers.has(name)) {
      return false;
    } else {
      this._triggers.set(name, ast);
      return true;
    }
  }
  _getTrigger(name) {
    const trigger = this._triggers.get(name);
    if (!trigger) {
      throw unregisteredTrigger(name);
    }
    return trigger;
  }
  trigger(element, triggerName, value, defaultToFallback = true) {
    const trigger = this._getTrigger(triggerName);
    const player = new TransitionAnimationPlayer(this.id, triggerName, element);
    let triggersWithStates = this._engine.statesByElement.get(element);
    if (!triggersWithStates) {
      addClass(element, NG_TRIGGER_CLASSNAME);
      addClass(element, NG_TRIGGER_CLASSNAME + "-" + triggerName);
      this._engine.statesByElement.set(element, triggersWithStates = /* @__PURE__ */ new Map());
    }
    let fromState = triggersWithStates.get(triggerName);
    const toState = new StateValue(value, this.id);
    const isObj = value && value.hasOwnProperty("value");
    if (!isObj && fromState) {
      toState.absorbOptions(fromState.options);
    }
    triggersWithStates.set(triggerName, toState);
    if (!fromState) {
      fromState = DEFAULT_STATE_VALUE;
    }
    const isRemoval = toState.value === VOID_VALUE;
    if (!isRemoval && fromState.value === toState.value) {
      if (!objEquals(fromState.params, toState.params)) {
        const errors = [];
        const fromStyles = trigger.matchStyles(fromState.value, fromState.params, errors);
        const toStyles = trigger.matchStyles(toState.value, toState.params, errors);
        if (errors.length) {
          this._engine.reportError(errors);
        } else {
          this._engine.afterFlush(() => {
            eraseStyles(element, fromStyles);
            setStyles(element, toStyles);
          });
        }
      }
      return;
    }
    const playersOnElement = getOrSetDefaultValue(this._engine.playersByElement, element, []);
    playersOnElement.forEach((player2) => {
      if (player2.namespaceId == this.id && player2.triggerName == triggerName && player2.queued) {
        player2.destroy();
      }
    });
    let transition = trigger.matchTransition(fromState.value, toState.value, element, toState.params);
    let isFallbackTransition = false;
    if (!transition) {
      if (!defaultToFallback) return;
      transition = trigger.fallbackTransition;
      isFallbackTransition = true;
    }
    this._engine.totalQueuedPlayers++;
    this._queue.push({
      element,
      triggerName,
      transition,
      fromState,
      toState,
      player,
      isFallbackTransition
    });
    if (!isFallbackTransition) {
      addClass(element, QUEUED_CLASSNAME);
      player.onStart(() => {
        removeClass(element, QUEUED_CLASSNAME);
      });
    }
    player.onDone(() => {
      let index = this.players.indexOf(player);
      if (index >= 0) {
        this.players.splice(index, 1);
      }
      const players = this._engine.playersByElement.get(element);
      if (players) {
        let index2 = players.indexOf(player);
        if (index2 >= 0) {
          players.splice(index2, 1);
        }
      }
    });
    this.players.push(player);
    playersOnElement.push(player);
    return player;
  }
  deregister(name) {
    this._triggers.delete(name);
    this._engine.statesByElement.forEach((stateMap) => stateMap.delete(name));
    this._elementListeners.forEach((listeners, element) => {
      this._elementListeners.set(element, listeners.filter((entry) => {
        return entry.name != name;
      }));
    });
  }
  clearElementCache(element) {
    this._engine.statesByElement.delete(element);
    this._elementListeners.delete(element);
    const elementPlayers = this._engine.playersByElement.get(element);
    if (elementPlayers) {
      elementPlayers.forEach((player) => player.destroy());
      this._engine.playersByElement.delete(element);
    }
  }
  _signalRemovalForInnerTriggers(rootElement, context) {
    const elements = this._engine.driver.query(rootElement, NG_TRIGGER_SELECTOR, true);
    elements.forEach((elm) => {
      if (elm[REMOVAL_FLAG]) return;
      const namespaces = this._engine.fetchNamespacesByElement(elm);
      if (namespaces.size) {
        namespaces.forEach((ns) => ns.triggerLeaveAnimation(elm, context, false, true));
      } else {
        this.clearElementCache(elm);
      }
    });
    this._engine.afterFlushAnimationsDone(() => elements.forEach((elm) => this.clearElementCache(elm)));
  }
  triggerLeaveAnimation(element, context, destroyAfterComplete, defaultToFallback) {
    const triggerStates = this._engine.statesByElement.get(element);
    const previousTriggersValues = /* @__PURE__ */ new Map();
    if (triggerStates) {
      const players = [];
      triggerStates.forEach((state, triggerName) => {
        previousTriggersValues.set(triggerName, state.value);
        if (this._triggers.has(triggerName)) {
          const player = this.trigger(element, triggerName, VOID_VALUE, defaultToFallback);
          if (player) {
            players.push(player);
          }
        }
      });
      if (players.length) {
        this._engine.markElementAsRemoved(this.id, element, true, context, previousTriggersValues);
        if (destroyAfterComplete) {
          optimizeGroupPlayer(players).onDone(() => this._engine.processLeaveNode(element));
        }
        return true;
      }
    }
    return false;
  }
  prepareLeaveAnimationListeners(element) {
    const listeners = this._elementListeners.get(element);
    const elementStates = this._engine.statesByElement.get(element);
    if (listeners && elementStates) {
      const visitedTriggers = /* @__PURE__ */ new Set();
      listeners.forEach((listener) => {
        const triggerName = listener.name;
        if (visitedTriggers.has(triggerName)) return;
        visitedTriggers.add(triggerName);
        const trigger = this._triggers.get(triggerName);
        const transition = trigger.fallbackTransition;
        const fromState = elementStates.get(triggerName) || DEFAULT_STATE_VALUE;
        const toState = new StateValue(VOID_VALUE);
        const player = new TransitionAnimationPlayer(this.id, triggerName, element);
        this._engine.totalQueuedPlayers++;
        this._queue.push({
          element,
          triggerName,
          transition,
          fromState,
          toState,
          player,
          isFallbackTransition: true
        });
      });
    }
  }
  removeNode(element, context) {
    const engine = this._engine;
    if (element.childElementCount) {
      this._signalRemovalForInnerTriggers(element, context);
    }
    if (this.triggerLeaveAnimation(element, context, true)) return;
    let containsPotentialParentTransition = false;
    if (engine.totalAnimations) {
      const currentPlayers = engine.players.length ? engine.playersByQueriedElement.get(element) : [];
      if (currentPlayers && currentPlayers.length) {
        containsPotentialParentTransition = true;
      } else {
        let parent = element;
        while (parent = parent.parentNode) {
          const triggers = engine.statesByElement.get(parent);
          if (triggers) {
            containsPotentialParentTransition = true;
            break;
          }
        }
      }
    }
    this.prepareLeaveAnimationListeners(element);
    if (containsPotentialParentTransition) {
      engine.markElementAsRemoved(this.id, element, false, context);
    } else {
      const removalFlag = element[REMOVAL_FLAG];
      if (!removalFlag || removalFlag === NULL_REMOVAL_STATE) {
        engine.afterFlush(() => this.clearElementCache(element));
        engine.destroyInnerAnimations(element);
        engine._onRemovalComplete(element, context);
      }
    }
  }
  insertNode(element, parent) {
    addClass(element, this._hostClassName);
  }
  drainQueuedTransitions(microtaskId) {
    const instructions = [];
    this._queue.forEach((entry) => {
      const player = entry.player;
      if (player.destroyed) return;
      const element = entry.element;
      const listeners = this._elementListeners.get(element);
      if (listeners) {
        listeners.forEach((listener) => {
          if (listener.name == entry.triggerName) {
            const baseEvent = makeAnimationEvent(element, entry.triggerName, entry.fromState.value, entry.toState.value);
            baseEvent["_data"] = microtaskId;
            listenOnPlayer(entry.player, listener.phase, baseEvent, listener.callback);
          }
        });
      }
      if (player.markedForDestroy) {
        this._engine.afterFlush(() => {
          player.destroy();
        });
      } else {
        instructions.push(entry);
      }
    });
    this._queue = [];
    return instructions.sort((a, b) => {
      const d0 = a.transition.ast.depCount;
      const d1 = b.transition.ast.depCount;
      if (d0 == 0 || d1 == 0) {
        return d0 - d1;
      }
      return this._engine.driver.containsElement(a.element, b.element) ? 1 : -1;
    });
  }
  destroy(context) {
    this.players.forEach((p) => p.destroy());
    this._signalRemovalForInnerTriggers(this.hostElement, context);
  }
};
var TransitionAnimationEngine = class {
  /** @internal */
  _onRemovalComplete(element, context) {
    this.onRemovalComplete(element, context);
  }
  constructor(bodyNode, driver, _normalizer) {
    this.bodyNode = bodyNode;
    this.driver = driver;
    this._normalizer = _normalizer;
    this.players = [];
    this.newHostElements = /* @__PURE__ */ new Map();
    this.playersByElement = /* @__PURE__ */ new Map();
    this.playersByQueriedElement = /* @__PURE__ */ new Map();
    this.statesByElement = /* @__PURE__ */ new Map();
    this.disabledNodes = /* @__PURE__ */ new Set();
    this.totalAnimations = 0;
    this.totalQueuedPlayers = 0;
    this._namespaceLookup = {};
    this._namespaceList = [];
    this._flushFns = [];
    this._whenQuietFns = [];
    this.namespacesByHostElement = /* @__PURE__ */ new Map();
    this.collectedEnterElements = [];
    this.collectedLeaveElements = [];
    this.onRemovalComplete = (element, context) => {
    };
  }
  get queuedPlayers() {
    const players = [];
    this._namespaceList.forEach((ns) => {
      ns.players.forEach((player) => {
        if (player.queued) {
          players.push(player);
        }
      });
    });
    return players;
  }
  createNamespace(namespaceId, hostElement) {
    const ns = new AnimationTransitionNamespace(namespaceId, hostElement, this);
    if (this.bodyNode && this.driver.containsElement(this.bodyNode, hostElement)) {
      this._balanceNamespaceList(ns, hostElement);
    } else {
      this.newHostElements.set(hostElement, ns);
      this.collectEnterElement(hostElement);
    }
    return this._namespaceLookup[namespaceId] = ns;
  }
  _balanceNamespaceList(ns, hostElement) {
    const namespaceList = this._namespaceList;
    const namespacesByHostElement = this.namespacesByHostElement;
    const limit = namespaceList.length - 1;
    if (limit >= 0) {
      let found = false;
      let ancestor = this.driver.getParentElement(hostElement);
      while (ancestor) {
        const ancestorNs = namespacesByHostElement.get(ancestor);
        if (ancestorNs) {
          const index = namespaceList.indexOf(ancestorNs);
          namespaceList.splice(index + 1, 0, ns);
          found = true;
          break;
        }
        ancestor = this.driver.getParentElement(ancestor);
      }
      if (!found) {
        namespaceList.unshift(ns);
      }
    } else {
      namespaceList.push(ns);
    }
    namespacesByHostElement.set(hostElement, ns);
    return ns;
  }
  register(namespaceId, hostElement) {
    let ns = this._namespaceLookup[namespaceId];
    if (!ns) {
      ns = this.createNamespace(namespaceId, hostElement);
    }
    return ns;
  }
  registerTrigger(namespaceId, name, trigger) {
    let ns = this._namespaceLookup[namespaceId];
    if (ns && ns.register(name, trigger)) {
      this.totalAnimations++;
    }
  }
  destroy(namespaceId, context) {
    if (!namespaceId) return;
    this.afterFlush(() => {
    });
    this.afterFlushAnimationsDone(() => {
      const ns = this._fetchNamespace(namespaceId);
      this.namespacesByHostElement.delete(ns.hostElement);
      const index = this._namespaceList.indexOf(ns);
      if (index >= 0) {
        this._namespaceList.splice(index, 1);
      }
      ns.destroy(context);
      delete this._namespaceLookup[namespaceId];
    });
  }
  _fetchNamespace(id) {
    return this._namespaceLookup[id];
  }
  fetchNamespacesByElement(element) {
    const namespaces = /* @__PURE__ */ new Set();
    const elementStates = this.statesByElement.get(element);
    if (elementStates) {
      for (let stateValue of elementStates.values()) {
        if (stateValue.namespaceId) {
          const ns = this._fetchNamespace(stateValue.namespaceId);
          if (ns) {
            namespaces.add(ns);
          }
        }
      }
    }
    return namespaces;
  }
  trigger(namespaceId, element, name, value) {
    if (isElementNode(element)) {
      const ns = this._fetchNamespace(namespaceId);
      if (ns) {
        ns.trigger(element, name, value);
        return true;
      }
    }
    return false;
  }
  insertNode(namespaceId, element, parent, insertBefore) {
    if (!isElementNode(element)) return;
    const details = element[REMOVAL_FLAG];
    if (details && details.setForRemoval) {
      details.setForRemoval = false;
      details.setForMove = true;
      const index = this.collectedLeaveElements.indexOf(element);
      if (index >= 0) {
        this.collectedLeaveElements.splice(index, 1);
      }
    }
    if (namespaceId) {
      const ns = this._fetchNamespace(namespaceId);
      if (ns) {
        ns.insertNode(element, parent);
      }
    }
    if (insertBefore) {
      this.collectEnterElement(element);
    }
  }
  collectEnterElement(element) {
    this.collectedEnterElements.push(element);
  }
  markElementAsDisabled(element, value) {
    if (value) {
      if (!this.disabledNodes.has(element)) {
        this.disabledNodes.add(element);
        addClass(element, DISABLED_CLASSNAME);
      }
    } else if (this.disabledNodes.has(element)) {
      this.disabledNodes.delete(element);
      removeClass(element, DISABLED_CLASSNAME);
    }
  }
  removeNode(namespaceId, element, context) {
    if (isElementNode(element)) {
      const ns = namespaceId ? this._fetchNamespace(namespaceId) : null;
      if (ns) {
        ns.removeNode(element, context);
      } else {
        this.markElementAsRemoved(namespaceId, element, false, context);
      }
      const hostNS = this.namespacesByHostElement.get(element);
      if (hostNS && hostNS.id !== namespaceId) {
        hostNS.removeNode(element, context);
      }
    } else {
      this._onRemovalComplete(element, context);
    }
  }
  markElementAsRemoved(namespaceId, element, hasAnimation, context, previousTriggersValues) {
    this.collectedLeaveElements.push(element);
    element[REMOVAL_FLAG] = {
      namespaceId,
      setForRemoval: context,
      hasAnimation,
      removedBeforeQueried: false,
      previousTriggersValues
    };
  }
  listen(namespaceId, element, name, phase, callback) {
    if (isElementNode(element)) {
      return this._fetchNamespace(namespaceId).listen(element, name, phase, callback);
    }
    return () => {
    };
  }
  _buildInstruction(entry, subTimelines, enterClassName, leaveClassName, skipBuildAst) {
    return entry.transition.build(this.driver, entry.element, entry.fromState.value, entry.toState.value, enterClassName, leaveClassName, entry.fromState.options, entry.toState.options, subTimelines, skipBuildAst);
  }
  destroyInnerAnimations(containerElement) {
    let elements = this.driver.query(containerElement, NG_TRIGGER_SELECTOR, true);
    elements.forEach((element) => this.destroyActiveAnimationsForElement(element));
    if (this.playersByQueriedElement.size == 0) return;
    elements = this.driver.query(containerElement, NG_ANIMATING_SELECTOR, true);
    elements.forEach((element) => this.finishActiveQueriedAnimationOnElement(element));
  }
  destroyActiveAnimationsForElement(element) {
    const players = this.playersByElement.get(element);
    if (players) {
      players.forEach((player) => {
        if (player.queued) {
          player.markedForDestroy = true;
        } else {
          player.destroy();
        }
      });
    }
  }
  finishActiveQueriedAnimationOnElement(element) {
    const players = this.playersByQueriedElement.get(element);
    if (players) {
      players.forEach((player) => player.finish());
    }
  }
  whenRenderingDone() {
    return new Promise((resolve) => {
      if (this.players.length) {
        return optimizeGroupPlayer(this.players).onDone(() => resolve());
      } else {
        resolve();
      }
    });
  }
  processLeaveNode(element) {
    const details = element[REMOVAL_FLAG];
    if (details && details.setForRemoval) {
      element[REMOVAL_FLAG] = NULL_REMOVAL_STATE;
      if (details.namespaceId) {
        this.destroyInnerAnimations(element);
        const ns = this._fetchNamespace(details.namespaceId);
        if (ns) {
          ns.clearElementCache(element);
        }
      }
      this._onRemovalComplete(element, details.setForRemoval);
    }
    if (element.classList?.contains(DISABLED_CLASSNAME)) {
      this.markElementAsDisabled(element, false);
    }
    this.driver.query(element, DISABLED_SELECTOR, true).forEach((node) => {
      this.markElementAsDisabled(node, false);
    });
  }
  flush(microtaskId = -1) {
    let players = [];
    if (this.newHostElements.size) {
      this.newHostElements.forEach((ns, element) => this._balanceNamespaceList(ns, element));
      this.newHostElements.clear();
    }
    if (this.totalAnimations && this.collectedEnterElements.length) {
      for (let i = 0; i < this.collectedEnterElements.length; i++) {
        const elm = this.collectedEnterElements[i];
        addClass(elm, STAR_CLASSNAME);
      }
    }
    if (this._namespaceList.length && (this.totalQueuedPlayers || this.collectedLeaveElements.length)) {
      const cleanupFns = [];
      try {
        players = this._flushAnimations(cleanupFns, microtaskId);
      } finally {
        for (let i = 0; i < cleanupFns.length; i++) {
          cleanupFns[i]();
        }
      }
    } else {
      for (let i = 0; i < this.collectedLeaveElements.length; i++) {
        const element = this.collectedLeaveElements[i];
        this.processLeaveNode(element);
      }
    }
    this.totalQueuedPlayers = 0;
    this.collectedEnterElements.length = 0;
    this.collectedLeaveElements.length = 0;
    this._flushFns.forEach((fn) => fn());
    this._flushFns = [];
    if (this._whenQuietFns.length) {
      const quietFns = this._whenQuietFns;
      this._whenQuietFns = [];
      if (players.length) {
        optimizeGroupPlayer(players).onDone(() => {
          quietFns.forEach((fn) => fn());
        });
      } else {
        quietFns.forEach((fn) => fn());
      }
    }
  }
  reportError(errors) {
    throw triggerTransitionsFailed(errors);
  }
  _flushAnimations(cleanupFns, microtaskId) {
    const subTimelines = new ElementInstructionMap();
    const skippedPlayers = [];
    const skippedPlayersMap = /* @__PURE__ */ new Map();
    const queuedInstructions = [];
    const queriedElements = /* @__PURE__ */ new Map();
    const allPreStyleElements = /* @__PURE__ */ new Map();
    const allPostStyleElements = /* @__PURE__ */ new Map();
    const disabledElementsSet = /* @__PURE__ */ new Set();
    this.disabledNodes.forEach((node) => {
      disabledElementsSet.add(node);
      const nodesThatAreDisabled = this.driver.query(node, QUEUED_SELECTOR, true);
      for (let i2 = 0; i2 < nodesThatAreDisabled.length; i2++) {
        disabledElementsSet.add(nodesThatAreDisabled[i2]);
      }
    });
    const bodyNode = this.bodyNode;
    const allTriggerElements = Array.from(this.statesByElement.keys());
    const enterNodeMap = buildRootMap(allTriggerElements, this.collectedEnterElements);
    const enterNodeMapIds = /* @__PURE__ */ new Map();
    let i = 0;
    enterNodeMap.forEach((nodes, root) => {
      const className = ENTER_CLASSNAME + i++;
      enterNodeMapIds.set(root, className);
      nodes.forEach((node) => addClass(node, className));
    });
    const allLeaveNodes = [];
    const mergedLeaveNodes = /* @__PURE__ */ new Set();
    const leaveNodesWithoutAnimations = /* @__PURE__ */ new Set();
    for (let i2 = 0; i2 < this.collectedLeaveElements.length; i2++) {
      const element = this.collectedLeaveElements[i2];
      const details = element[REMOVAL_FLAG];
      if (details && details.setForRemoval) {
        allLeaveNodes.push(element);
        mergedLeaveNodes.add(element);
        if (details.hasAnimation) {
          this.driver.query(element, STAR_SELECTOR, true).forEach((elm) => mergedLeaveNodes.add(elm));
        } else {
          leaveNodesWithoutAnimations.add(element);
        }
      }
    }
    const leaveNodeMapIds = /* @__PURE__ */ new Map();
    const leaveNodeMap = buildRootMap(allTriggerElements, Array.from(mergedLeaveNodes));
    leaveNodeMap.forEach((nodes, root) => {
      const className = LEAVE_CLASSNAME + i++;
      leaveNodeMapIds.set(root, className);
      nodes.forEach((node) => addClass(node, className));
    });
    cleanupFns.push(() => {
      enterNodeMap.forEach((nodes, root) => {
        const className = enterNodeMapIds.get(root);
        nodes.forEach((node) => removeClass(node, className));
      });
      leaveNodeMap.forEach((nodes, root) => {
        const className = leaveNodeMapIds.get(root);
        nodes.forEach((node) => removeClass(node, className));
      });
      allLeaveNodes.forEach((element) => {
        this.processLeaveNode(element);
      });
    });
    const allPlayers = [];
    const erroneousTransitions = [];
    for (let i2 = this._namespaceList.length - 1; i2 >= 0; i2--) {
      const ns = this._namespaceList[i2];
      ns.drainQueuedTransitions(microtaskId).forEach((entry) => {
        const player = entry.player;
        const element = entry.element;
        allPlayers.push(player);
        if (this.collectedEnterElements.length) {
          const details = element[REMOVAL_FLAG];
          if (details && details.setForMove) {
            if (details.previousTriggersValues && details.previousTriggersValues.has(entry.triggerName)) {
              const previousValue = details.previousTriggersValues.get(entry.triggerName);
              const triggersWithStates = this.statesByElement.get(entry.element);
              if (triggersWithStates && triggersWithStates.has(entry.triggerName)) {
                const state = triggersWithStates.get(entry.triggerName);
                state.value = previousValue;
                triggersWithStates.set(entry.triggerName, state);
              }
            }
            player.destroy();
            return;
          }
        }
        const nodeIsOrphaned = !bodyNode || !this.driver.containsElement(bodyNode, element);
        const leaveClassName = leaveNodeMapIds.get(element);
        const enterClassName = enterNodeMapIds.get(element);
        const instruction = this._buildInstruction(entry, subTimelines, enterClassName, leaveClassName, nodeIsOrphaned);
        if (instruction.errors && instruction.errors.length) {
          erroneousTransitions.push(instruction);
          return;
        }
        if (nodeIsOrphaned) {
          player.onStart(() => eraseStyles(element, instruction.fromStyles));
          player.onDestroy(() => setStyles(element, instruction.toStyles));
          skippedPlayers.push(player);
          return;
        }
        if (entry.isFallbackTransition) {
          player.onStart(() => eraseStyles(element, instruction.fromStyles));
          player.onDestroy(() => setStyles(element, instruction.toStyles));
          skippedPlayers.push(player);
          return;
        }
        const timelines = [];
        instruction.timelines.forEach((tl) => {
          tl.stretchStartingKeyframe = true;
          if (!this.disabledNodes.has(tl.element)) {
            timelines.push(tl);
          }
        });
        instruction.timelines = timelines;
        subTimelines.append(element, instruction.timelines);
        const tuple = {
          instruction,
          player,
          element
        };
        queuedInstructions.push(tuple);
        instruction.queriedElements.forEach((element2) => getOrSetDefaultValue(queriedElements, element2, []).push(player));
        instruction.preStyleProps.forEach((stringMap, element2) => {
          if (stringMap.size) {
            let setVal = allPreStyleElements.get(element2);
            if (!setVal) {
              allPreStyleElements.set(element2, setVal = /* @__PURE__ */ new Set());
            }
            stringMap.forEach((_, prop) => setVal.add(prop));
          }
        });
        instruction.postStyleProps.forEach((stringMap, element2) => {
          let setVal = allPostStyleElements.get(element2);
          if (!setVal) {
            allPostStyleElements.set(element2, setVal = /* @__PURE__ */ new Set());
          }
          stringMap.forEach((_, prop) => setVal.add(prop));
        });
      });
    }
    if (erroneousTransitions.length) {
      const errors = [];
      erroneousTransitions.forEach((instruction) => {
        errors.push(transitionFailed(instruction.triggerName, instruction.errors));
      });
      allPlayers.forEach((player) => player.destroy());
      this.reportError(errors);
    }
    const allPreviousPlayersMap = /* @__PURE__ */ new Map();
    const animationElementMap = /* @__PURE__ */ new Map();
    queuedInstructions.forEach((entry) => {
      const element = entry.element;
      if (subTimelines.has(element)) {
        animationElementMap.set(element, element);
        this._beforeAnimationBuild(entry.player.namespaceId, entry.instruction, allPreviousPlayersMap);
      }
    });
    skippedPlayers.forEach((player) => {
      const element = player.element;
      const previousPlayers = this._getPreviousPlayers(element, false, player.namespaceId, player.triggerName, null);
      previousPlayers.forEach((prevPlayer) => {
        getOrSetDefaultValue(allPreviousPlayersMap, element, []).push(prevPlayer);
        prevPlayer.destroy();
      });
    });
    const replaceNodes = allLeaveNodes.filter((node) => {
      return replacePostStylesAsPre(node, allPreStyleElements, allPostStyleElements);
    });
    const postStylesMap = /* @__PURE__ */ new Map();
    const allLeaveQueriedNodes = cloakAndComputeStyles(postStylesMap, this.driver, leaveNodesWithoutAnimations, allPostStyleElements, AUTO_STYLE);
    allLeaveQueriedNodes.forEach((node) => {
      if (replacePostStylesAsPre(node, allPreStyleElements, allPostStyleElements)) {
        replaceNodes.push(node);
      }
    });
    const preStylesMap = /* @__PURE__ */ new Map();
    enterNodeMap.forEach((nodes, root) => {
      cloakAndComputeStyles(preStylesMap, this.driver, new Set(nodes), allPreStyleElements, \u0275PRE_STYLE);
    });
    replaceNodes.forEach((node) => {
      const post = postStylesMap.get(node);
      const pre = preStylesMap.get(node);
      postStylesMap.set(node, new Map([...post?.entries() ?? [], ...pre?.entries() ?? []]));
    });
    const rootPlayers = [];
    const subPlayers = [];
    const NO_PARENT_ANIMATION_ELEMENT_DETECTED = {};
    queuedInstructions.forEach((entry) => {
      const {
        element,
        player,
        instruction
      } = entry;
      if (subTimelines.has(element)) {
        if (disabledElementsSet.has(element)) {
          player.onDestroy(() => setStyles(element, instruction.toStyles));
          player.disabled = true;
          player.overrideTotalTime(instruction.totalTime);
          skippedPlayers.push(player);
          return;
        }
        let parentWithAnimation = NO_PARENT_ANIMATION_ELEMENT_DETECTED;
        if (animationElementMap.size > 1) {
          let elm = element;
          const parentsToAdd = [];
          while (elm = elm.parentNode) {
            const detectedParent = animationElementMap.get(elm);
            if (detectedParent) {
              parentWithAnimation = detectedParent;
              break;
            }
            parentsToAdd.push(elm);
          }
          parentsToAdd.forEach((parent) => animationElementMap.set(parent, parentWithAnimation));
        }
        const innerPlayer = this._buildAnimation(player.namespaceId, instruction, allPreviousPlayersMap, skippedPlayersMap, preStylesMap, postStylesMap);
        player.setRealPlayer(innerPlayer);
        if (parentWithAnimation === NO_PARENT_ANIMATION_ELEMENT_DETECTED) {
          rootPlayers.push(player);
        } else {
          const parentPlayers = this.playersByElement.get(parentWithAnimation);
          if (parentPlayers && parentPlayers.length) {
            player.parentPlayer = optimizeGroupPlayer(parentPlayers);
          }
          skippedPlayers.push(player);
        }
      } else {
        eraseStyles(element, instruction.fromStyles);
        player.onDestroy(() => setStyles(element, instruction.toStyles));
        subPlayers.push(player);
        if (disabledElementsSet.has(element)) {
          skippedPlayers.push(player);
        }
      }
    });
    subPlayers.forEach((player) => {
      const playersForElement = skippedPlayersMap.get(player.element);
      if (playersForElement && playersForElement.length) {
        const innerPlayer = optimizeGroupPlayer(playersForElement);
        player.setRealPlayer(innerPlayer);
      }
    });
    skippedPlayers.forEach((player) => {
      if (player.parentPlayer) {
        player.syncPlayerEvents(player.parentPlayer);
      } else {
        player.destroy();
      }
    });
    for (let i2 = 0; i2 < allLeaveNodes.length; i2++) {
      const element = allLeaveNodes[i2];
      const details = element[REMOVAL_FLAG];
      removeClass(element, LEAVE_CLASSNAME);
      if (details && details.hasAnimation) continue;
      let players = [];
      if (queriedElements.size) {
        let queriedPlayerResults = queriedElements.get(element);
        if (queriedPlayerResults && queriedPlayerResults.length) {
          players.push(...queriedPlayerResults);
        }
        let queriedInnerElements = this.driver.query(element, NG_ANIMATING_SELECTOR, true);
        for (let j = 0; j < queriedInnerElements.length; j++) {
          let queriedPlayers = queriedElements.get(queriedInnerElements[j]);
          if (queriedPlayers && queriedPlayers.length) {
            players.push(...queriedPlayers);
          }
        }
      }
      const activePlayers = players.filter((p) => !p.destroyed);
      if (activePlayers.length) {
        removeNodesAfterAnimationDone(this, element, activePlayers);
      } else {
        this.processLeaveNode(element);
      }
    }
    allLeaveNodes.length = 0;
    rootPlayers.forEach((player) => {
      this.players.push(player);
      player.onDone(() => {
        player.destroy();
        const index = this.players.indexOf(player);
        this.players.splice(index, 1);
      });
      player.play();
    });
    return rootPlayers;
  }
  afterFlush(callback) {
    this._flushFns.push(callback);
  }
  afterFlushAnimationsDone(callback) {
    this._whenQuietFns.push(callback);
  }
  _getPreviousPlayers(element, isQueriedElement, namespaceId, triggerName, toStateValue) {
    let players = [];
    if (isQueriedElement) {
      const queriedElementPlayers = this.playersByQueriedElement.get(element);
      if (queriedElementPlayers) {
        players = queriedElementPlayers;
      }
    } else {
      const elementPlayers = this.playersByElement.get(element);
      if (elementPlayers) {
        const isRemovalAnimation = !toStateValue || toStateValue == VOID_VALUE;
        elementPlayers.forEach((player) => {
          if (player.queued) return;
          if (!isRemovalAnimation && player.triggerName != triggerName) return;
          players.push(player);
        });
      }
    }
    if (namespaceId || triggerName) {
      players = players.filter((player) => {
        if (namespaceId && namespaceId != player.namespaceId) return false;
        if (triggerName && triggerName != player.triggerName) return false;
        return true;
      });
    }
    return players;
  }
  _beforeAnimationBuild(namespaceId, instruction, allPreviousPlayersMap) {
    const triggerName = instruction.triggerName;
    const rootElement = instruction.element;
    const targetNameSpaceId = instruction.isRemovalTransition ? void 0 : namespaceId;
    const targetTriggerName = instruction.isRemovalTransition ? void 0 : triggerName;
    for (const timelineInstruction of instruction.timelines) {
      const element = timelineInstruction.element;
      const isQueriedElement = element !== rootElement;
      const players = getOrSetDefaultValue(allPreviousPlayersMap, element, []);
      const previousPlayers = this._getPreviousPlayers(element, isQueriedElement, targetNameSpaceId, targetTriggerName, instruction.toState);
      previousPlayers.forEach((player) => {
        const realPlayer = player.getRealPlayer();
        if (realPlayer.beforeDestroy) {
          realPlayer.beforeDestroy();
        }
        player.destroy();
        players.push(player);
      });
    }
    eraseStyles(rootElement, instruction.fromStyles);
  }
  _buildAnimation(namespaceId, instruction, allPreviousPlayersMap, skippedPlayersMap, preStylesMap, postStylesMap) {
    const triggerName = instruction.triggerName;
    const rootElement = instruction.element;
    const allQueriedPlayers = [];
    const allConsumedElements = /* @__PURE__ */ new Set();
    const allSubElements = /* @__PURE__ */ new Set();
    const allNewPlayers = instruction.timelines.map((timelineInstruction) => {
      const element = timelineInstruction.element;
      allConsumedElements.add(element);
      const details = element[REMOVAL_FLAG];
      if (details && details.removedBeforeQueried) return new NoopAnimationPlayer(timelineInstruction.duration, timelineInstruction.delay);
      const isQueriedElement = element !== rootElement;
      const previousPlayers = flattenGroupPlayers((allPreviousPlayersMap.get(element) || EMPTY_PLAYER_ARRAY).map((p) => p.getRealPlayer())).filter((p) => {
        const pp = p;
        return pp.element ? pp.element === element : false;
      });
      const preStyles = preStylesMap.get(element);
      const postStyles = postStylesMap.get(element);
      const keyframes = normalizeKeyframes$1(this._normalizer, timelineInstruction.keyframes, preStyles, postStyles);
      const player2 = this._buildPlayer(timelineInstruction, keyframes, previousPlayers);
      if (timelineInstruction.subTimeline && skippedPlayersMap) {
        allSubElements.add(element);
      }
      if (isQueriedElement) {
        const wrappedPlayer = new TransitionAnimationPlayer(namespaceId, triggerName, element);
        wrappedPlayer.setRealPlayer(player2);
        allQueriedPlayers.push(wrappedPlayer);
      }
      return player2;
    });
    allQueriedPlayers.forEach((player2) => {
      getOrSetDefaultValue(this.playersByQueriedElement, player2.element, []).push(player2);
      player2.onDone(() => deleteOrUnsetInMap(this.playersByQueriedElement, player2.element, player2));
    });
    allConsumedElements.forEach((element) => addClass(element, NG_ANIMATING_CLASSNAME));
    const player = optimizeGroupPlayer(allNewPlayers);
    player.onDestroy(() => {
      allConsumedElements.forEach((element) => removeClass(element, NG_ANIMATING_CLASSNAME));
      setStyles(rootElement, instruction.toStyles);
    });
    allSubElements.forEach((element) => {
      getOrSetDefaultValue(skippedPlayersMap, element, []).push(player);
    });
    return player;
  }
  _buildPlayer(instruction, keyframes, previousPlayers) {
    if (keyframes.length > 0) {
      return this.driver.animate(instruction.element, keyframes, instruction.duration, instruction.delay, instruction.easing, previousPlayers);
    }
    return new NoopAnimationPlayer(instruction.duration, instruction.delay);
  }
};
var TransitionAnimationPlayer = class {
  constructor(namespaceId, triggerName, element) {
    this.namespaceId = namespaceId;
    this.triggerName = triggerName;
    this.element = element;
    this._player = new NoopAnimationPlayer();
    this._containsRealPlayer = false;
    this._queuedCallbacks = /* @__PURE__ */ new Map();
    this.destroyed = false;
    this.parentPlayer = null;
    this.markedForDestroy = false;
    this.disabled = false;
    this.queued = true;
    this.totalTime = 0;
  }
  setRealPlayer(player) {
    if (this._containsRealPlayer) return;
    this._player = player;
    this._queuedCallbacks.forEach((callbacks, phase) => {
      callbacks.forEach((callback) => listenOnPlayer(player, phase, void 0, callback));
    });
    this._queuedCallbacks.clear();
    this._containsRealPlayer = true;
    this.overrideTotalTime(player.totalTime);
    this.queued = false;
  }
  getRealPlayer() {
    return this._player;
  }
  overrideTotalTime(totalTime) {
    this.totalTime = totalTime;
  }
  syncPlayerEvents(player) {
    const p = this._player;
    if (p.triggerCallback) {
      player.onStart(() => p.triggerCallback("start"));
    }
    player.onDone(() => this.finish());
    player.onDestroy(() => this.destroy());
  }
  _queueEvent(name, callback) {
    getOrSetDefaultValue(this._queuedCallbacks, name, []).push(callback);
  }
  onDone(fn) {
    if (this.queued) {
      this._queueEvent("done", fn);
    }
    this._player.onDone(fn);
  }
  onStart(fn) {
    if (this.queued) {
      this._queueEvent("start", fn);
    }
    this._player.onStart(fn);
  }
  onDestroy(fn) {
    if (this.queued) {
      this._queueEvent("destroy", fn);
    }
    this._player.onDestroy(fn);
  }
  init() {
    this._player.init();
  }
  hasStarted() {
    return this.queued ? false : this._player.hasStarted();
  }
  play() {
    !this.queued && this._player.play();
  }
  pause() {
    !this.queued && this._player.pause();
  }
  restart() {
    !this.queued && this._player.restart();
  }
  finish() {
    this._player.finish();
  }
  destroy() {
    this.destroyed = true;
    this._player.destroy();
  }
  reset() {
    !this.queued && this._player.reset();
  }
  setPosition(p) {
    if (!this.queued) {
      this._player.setPosition(p);
    }
  }
  getPosition() {
    return this.queued ? 0 : this._player.getPosition();
  }
  /** @internal */
  triggerCallback(phaseName) {
    const p = this._player;
    if (p.triggerCallback) {
      p.triggerCallback(phaseName);
    }
  }
};
function deleteOrUnsetInMap(map2, key, value) {
  let currentValues = map2.get(key);
  if (currentValues) {
    if (currentValues.length) {
      const index = currentValues.indexOf(value);
      currentValues.splice(index, 1);
    }
    if (currentValues.length == 0) {
      map2.delete(key);
    }
  }
  return currentValues;
}
function normalizeTriggerValue(value) {
  return value != null ? value : null;
}
function isElementNode(node) {
  return node && node["nodeType"] === 1;
}
function isTriggerEventValid(eventName) {
  return eventName == "start" || eventName == "done";
}
function cloakElement(element, value) {
  const oldValue = element.style.display;
  element.style.display = value != null ? value : "none";
  return oldValue;
}
function cloakAndComputeStyles(valuesMap, driver, elements, elementPropsMap, defaultStyle) {
  const cloakVals = [];
  elements.forEach((element) => cloakVals.push(cloakElement(element)));
  const failedElements = [];
  elementPropsMap.forEach((props, element) => {
    const styles = /* @__PURE__ */ new Map();
    props.forEach((prop) => {
      const value = driver.computeStyle(element, prop, defaultStyle);
      styles.set(prop, value);
      if (!value || value.length == 0) {
        element[REMOVAL_FLAG] = NULL_REMOVED_QUERIED_STATE;
        failedElements.push(element);
      }
    });
    valuesMap.set(element, styles);
  });
  let i = 0;
  elements.forEach((element) => cloakElement(element, cloakVals[i++]));
  return failedElements;
}
function buildRootMap(roots, nodes) {
  const rootMap = /* @__PURE__ */ new Map();
  roots.forEach((root) => rootMap.set(root, []));
  if (nodes.length == 0) return rootMap;
  const NULL_NODE = 1;
  const nodeSet = new Set(nodes);
  const localRootMap = /* @__PURE__ */ new Map();
  function getRoot(node) {
    if (!node) return NULL_NODE;
    let root = localRootMap.get(node);
    if (root) return root;
    const parent = node.parentNode;
    if (rootMap.has(parent)) {
      root = parent;
    } else if (nodeSet.has(parent)) {
      root = NULL_NODE;
    } else {
      root = getRoot(parent);
    }
    localRootMap.set(node, root);
    return root;
  }
  nodes.forEach((node) => {
    const root = getRoot(node);
    if (root !== NULL_NODE) {
      rootMap.get(root).push(node);
    }
  });
  return rootMap;
}
function addClass(element, className) {
  element.classList?.add(className);
}
function removeClass(element, className) {
  element.classList?.remove(className);
}
function removeNodesAfterAnimationDone(engine, element, players) {
  optimizeGroupPlayer(players).onDone(() => engine.processLeaveNode(element));
}
function flattenGroupPlayers(players) {
  const finalPlayers = [];
  _flattenGroupPlayersRecur(players, finalPlayers);
  return finalPlayers;
}
function _flattenGroupPlayersRecur(players, finalPlayers) {
  for (let i = 0; i < players.length; i++) {
    const player = players[i];
    if (player instanceof AnimationGroupPlayer) {
      _flattenGroupPlayersRecur(player.players, finalPlayers);
    } else {
      finalPlayers.push(player);
    }
  }
}
function objEquals(a, b) {
  const k1 = Object.keys(a);
  const k2 = Object.keys(b);
  if (k1.length != k2.length) return false;
  for (let i = 0; i < k1.length; i++) {
    const prop = k1[i];
    if (!b.hasOwnProperty(prop) || a[prop] !== b[prop]) return false;
  }
  return true;
}
function replacePostStylesAsPre(element, allPreStyleElements, allPostStyleElements) {
  const postEntry = allPostStyleElements.get(element);
  if (!postEntry) return false;
  let preEntry = allPreStyleElements.get(element);
  if (preEntry) {
    postEntry.forEach((data) => preEntry.add(data));
  } else {
    allPreStyleElements.set(element, postEntry);
  }
  allPostStyleElements.delete(element);
  return true;
}
var AnimationEngine = class {
  constructor(doc, _driver, _normalizer) {
    this._driver = _driver;
    this._normalizer = _normalizer;
    this._triggerCache = {};
    this.onRemovalComplete = (element, context) => {
    };
    this._transitionEngine = new TransitionAnimationEngine(doc.body, _driver, _normalizer);
    this._timelineEngine = new TimelineAnimationEngine(doc.body, _driver, _normalizer);
    this._transitionEngine.onRemovalComplete = (element, context) => this.onRemovalComplete(element, context);
  }
  registerTrigger(componentId, namespaceId, hostElement, name, metadata) {
    const cacheKey = componentId + "-" + name;
    let trigger = this._triggerCache[cacheKey];
    if (!trigger) {
      const errors = [];
      const warnings = [];
      const ast = buildAnimationAst(this._driver, metadata, errors, warnings);
      if (errors.length) {
        throw triggerBuildFailed(name, errors);
      }
      if (warnings.length) {
        warnTriggerBuild(name, warnings);
      }
      trigger = buildTrigger(name, ast, this._normalizer);
      this._triggerCache[cacheKey] = trigger;
    }
    this._transitionEngine.registerTrigger(namespaceId, name, trigger);
  }
  register(namespaceId, hostElement) {
    this._transitionEngine.register(namespaceId, hostElement);
  }
  destroy(namespaceId, context) {
    this._transitionEngine.destroy(namespaceId, context);
  }
  onInsert(namespaceId, element, parent, insertBefore) {
    this._transitionEngine.insertNode(namespaceId, element, parent, insertBefore);
  }
  onRemove(namespaceId, element, context) {
    this._transitionEngine.removeNode(namespaceId, element, context);
  }
  disableAnimations(element, disable) {
    this._transitionEngine.markElementAsDisabled(element, disable);
  }
  process(namespaceId, element, property, value) {
    if (property.charAt(0) == "@") {
      const [id, action] = parseTimelineCommand(property);
      const args = value;
      this._timelineEngine.command(id, element, action, args);
    } else {
      this._transitionEngine.trigger(namespaceId, element, property, value);
    }
  }
  listen(namespaceId, element, eventName, eventPhase, callback) {
    if (eventName.charAt(0) == "@") {
      const [id, action] = parseTimelineCommand(eventName);
      return this._timelineEngine.listen(id, element, action, callback);
    }
    return this._transitionEngine.listen(namespaceId, element, eventName, eventPhase, callback);
  }
  flush(microtaskId = -1) {
    this._transitionEngine.flush(microtaskId);
  }
  get players() {
    return [...this._transitionEngine.players, ...this._timelineEngine.players];
  }
  whenRenderingDone() {
    return this._transitionEngine.whenRenderingDone();
  }
  afterFlushAnimationsDone(cb) {
    this._transitionEngine.afterFlushAnimationsDone(cb);
  }
};
function packageNonAnimatableStyles(element, styles) {
  let startStyles = null;
  let endStyles = null;
  if (Array.isArray(styles) && styles.length) {
    startStyles = filterNonAnimatableStyles(styles[0]);
    if (styles.length > 1) {
      endStyles = filterNonAnimatableStyles(styles[styles.length - 1]);
    }
  } else if (styles instanceof Map) {
    startStyles = filterNonAnimatableStyles(styles);
  }
  return startStyles || endStyles ? new SpecialCasedStyles(element, startStyles, endStyles) : null;
}
var SpecialCasedStyles = class _SpecialCasedStyles {
  static {
    this.initialStylesByElement = /* @__PURE__ */ new WeakMap();
  }
  constructor(_element, _startStyles, _endStyles) {
    this._element = _element;
    this._startStyles = _startStyles;
    this._endStyles = _endStyles;
    this._state = 0;
    let initialStyles = _SpecialCasedStyles.initialStylesByElement.get(_element);
    if (!initialStyles) {
      _SpecialCasedStyles.initialStylesByElement.set(_element, initialStyles = /* @__PURE__ */ new Map());
    }
    this._initialStyles = initialStyles;
  }
  start() {
    if (this._state < 1) {
      if (this._startStyles) {
        setStyles(this._element, this._startStyles, this._initialStyles);
      }
      this._state = 1;
    }
  }
  finish() {
    this.start();
    if (this._state < 2) {
      setStyles(this._element, this._initialStyles);
      if (this._endStyles) {
        setStyles(this._element, this._endStyles);
        this._endStyles = null;
      }
      this._state = 1;
    }
  }
  destroy() {
    this.finish();
    if (this._state < 3) {
      _SpecialCasedStyles.initialStylesByElement.delete(this._element);
      if (this._startStyles) {
        eraseStyles(this._element, this._startStyles);
        this._endStyles = null;
      }
      if (this._endStyles) {
        eraseStyles(this._element, this._endStyles);
        this._endStyles = null;
      }
      setStyles(this._element, this._initialStyles);
      this._state = 3;
    }
  }
};
function filterNonAnimatableStyles(styles) {
  let result = null;
  styles.forEach((val, prop) => {
    if (isNonAnimatableStyle(prop)) {
      result = result || /* @__PURE__ */ new Map();
      result.set(prop, val);
    }
  });
  return result;
}
function isNonAnimatableStyle(prop) {
  return prop === "display" || prop === "position";
}
var WebAnimationsPlayer = class {
  constructor(element, keyframes, options, _specialStyles) {
    this.element = element;
    this.keyframes = keyframes;
    this.options = options;
    this._specialStyles = _specialStyles;
    this._onDoneFns = [];
    this._onStartFns = [];
    this._onDestroyFns = [];
    this._initialized = false;
    this._finished = false;
    this._started = false;
    this._destroyed = false;
    this._originalOnDoneFns = [];
    this._originalOnStartFns = [];
    this.time = 0;
    this.parentPlayer = null;
    this.currentSnapshot = /* @__PURE__ */ new Map();
    this._duration = options["duration"];
    this._delay = options["delay"] || 0;
    this.time = this._duration + this._delay;
  }
  _onFinish() {
    if (!this._finished) {
      this._finished = true;
      this._onDoneFns.forEach((fn) => fn());
      this._onDoneFns = [];
    }
  }
  init() {
    this._buildPlayer();
    this._preparePlayerBeforeStart();
  }
  _buildPlayer() {
    if (this._initialized) return;
    this._initialized = true;
    const keyframes = this.keyframes;
    this.domPlayer = this._triggerWebAnimation(this.element, keyframes, this.options);
    this._finalKeyframe = keyframes.length ? keyframes[keyframes.length - 1] : /* @__PURE__ */ new Map();
    const onFinish = () => this._onFinish();
    this.domPlayer.addEventListener("finish", onFinish);
    this.onDestroy(() => {
      this.domPlayer.removeEventListener("finish", onFinish);
    });
  }
  _preparePlayerBeforeStart() {
    if (this._delay) {
      this._resetDomPlayerState();
    } else {
      this.domPlayer.pause();
    }
  }
  _convertKeyframesToObject(keyframes) {
    const kfs = [];
    keyframes.forEach((frame) => {
      kfs.push(Object.fromEntries(frame));
    });
    return kfs;
  }
  /** @internal */
  _triggerWebAnimation(element, keyframes, options) {
    return element.animate(this._convertKeyframesToObject(keyframes), options);
  }
  onStart(fn) {
    this._originalOnStartFns.push(fn);
    this._onStartFns.push(fn);
  }
  onDone(fn) {
    this._originalOnDoneFns.push(fn);
    this._onDoneFns.push(fn);
  }
  onDestroy(fn) {
    this._onDestroyFns.push(fn);
  }
  play() {
    this._buildPlayer();
    if (!this.hasStarted()) {
      this._onStartFns.forEach((fn) => fn());
      this._onStartFns = [];
      this._started = true;
      if (this._specialStyles) {
        this._specialStyles.start();
      }
    }
    this.domPlayer.play();
  }
  pause() {
    this.init();
    this.domPlayer.pause();
  }
  finish() {
    this.init();
    if (this._specialStyles) {
      this._specialStyles.finish();
    }
    this._onFinish();
    this.domPlayer.finish();
  }
  reset() {
    this._resetDomPlayerState();
    this._destroyed = false;
    this._finished = false;
    this._started = false;
    this._onStartFns = this._originalOnStartFns;
    this._onDoneFns = this._originalOnDoneFns;
  }
  _resetDomPlayerState() {
    if (this.domPlayer) {
      this.domPlayer.cancel();
    }
  }
  restart() {
    this.reset();
    this.play();
  }
  hasStarted() {
    return this._started;
  }
  destroy() {
    if (!this._destroyed) {
      this._destroyed = true;
      this._resetDomPlayerState();
      this._onFinish();
      if (this._specialStyles) {
        this._specialStyles.destroy();
      }
      this._onDestroyFns.forEach((fn) => fn());
      this._onDestroyFns = [];
    }
  }
  setPosition(p) {
    if (this.domPlayer === void 0) {
      this.init();
    }
    this.domPlayer.currentTime = p * this.time;
  }
  getPosition() {
    return +(this.domPlayer.currentTime ?? 0) / this.time;
  }
  get totalTime() {
    return this._delay + this._duration;
  }
  beforeDestroy() {
    const styles = /* @__PURE__ */ new Map();
    if (this.hasStarted()) {
      const finalKeyframe = this._finalKeyframe;
      finalKeyframe.forEach((val, prop) => {
        if (prop !== "offset") {
          styles.set(prop, this._finished ? val : computeStyle(this.element, prop));
        }
      });
    }
    this.currentSnapshot = styles;
  }
  /** @internal */
  triggerCallback(phaseName) {
    const methods = phaseName === "start" ? this._onStartFns : this._onDoneFns;
    methods.forEach((fn) => fn());
    methods.length = 0;
  }
};
var WebAnimationsDriver = class {
  validateStyleProperty(prop) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      return validateStyleProperty(prop);
    }
    return true;
  }
  validateAnimatableStyleProperty(prop) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      const cssProp = camelCaseToDashCase(prop);
      return validateWebAnimatableStyleProperty(cssProp);
    }
    return true;
  }
  containsElement(elm1, elm2) {
    return containsElement(elm1, elm2);
  }
  getParentElement(element) {
    return getParentElement(element);
  }
  query(element, selector, multi) {
    return invokeQuery(element, selector, multi);
  }
  computeStyle(element, prop, defaultValue) {
    return computeStyle(element, prop);
  }
  animate(element, keyframes, duration, delay, easing, previousPlayers = []) {
    const fill = delay == 0 ? "both" : "forwards";
    const playerOptions = {
      duration,
      delay,
      fill
    };
    if (easing) {
      playerOptions["easing"] = easing;
    }
    const previousStyles = /* @__PURE__ */ new Map();
    const previousWebAnimationPlayers = previousPlayers.filter((player) => player instanceof WebAnimationsPlayer);
    if (allowPreviousPlayerStylesMerge(duration, delay)) {
      previousWebAnimationPlayers.forEach((player) => {
        player.currentSnapshot.forEach((val, prop) => previousStyles.set(prop, val));
      });
    }
    let _keyframes = normalizeKeyframes(keyframes).map((styles) => new Map(styles));
    _keyframes = balancePreviousStylesIntoKeyframes(element, _keyframes, previousStyles);
    const specialStyles = packageNonAnimatableStyles(element, _keyframes);
    return new WebAnimationsPlayer(element, _keyframes, playerOptions, specialStyles);
  }
};
var ANIMATION_PREFIX = "@";
var DISABLE_ANIMATIONS_FLAG = "@.disabled";
var BaseAnimationRenderer = class {
  constructor(namespaceId, delegate, engine, _onDestroy) {
    this.namespaceId = namespaceId;
    this.delegate = delegate;
    this.engine = engine;
    this._onDestroy = _onDestroy;
    this.\u0275type = 0;
  }
  get data() {
    return this.delegate.data;
  }
  destroyNode(node) {
    this.delegate.destroyNode?.(node);
  }
  destroy() {
    this.engine.destroy(this.namespaceId, this.delegate);
    this.engine.afterFlushAnimationsDone(() => {
      queueMicrotask(() => {
        this.delegate.destroy();
      });
    });
    this._onDestroy?.();
  }
  createElement(name, namespace) {
    return this.delegate.createElement(name, namespace);
  }
  createComment(value) {
    return this.delegate.createComment(value);
  }
  createText(value) {
    return this.delegate.createText(value);
  }
  appendChild(parent, newChild) {
    this.delegate.appendChild(parent, newChild);
    this.engine.onInsert(this.namespaceId, newChild, parent, false);
  }
  insertBefore(parent, newChild, refChild, isMove = true) {
    this.delegate.insertBefore(parent, newChild, refChild);
    this.engine.onInsert(this.namespaceId, newChild, parent, isMove);
  }
  removeChild(parent, oldChild, isHostElement) {
    if (this.parentNode(oldChild)) {
      this.engine.onRemove(this.namespaceId, oldChild, this.delegate);
    }
  }
  selectRootElement(selectorOrNode, preserveContent) {
    return this.delegate.selectRootElement(selectorOrNode, preserveContent);
  }
  parentNode(node) {
    return this.delegate.parentNode(node);
  }
  nextSibling(node) {
    return this.delegate.nextSibling(node);
  }
  setAttribute(el, name, value, namespace) {
    this.delegate.setAttribute(el, name, value, namespace);
  }
  removeAttribute(el, name, namespace) {
    this.delegate.removeAttribute(el, name, namespace);
  }
  addClass(el, name) {
    this.delegate.addClass(el, name);
  }
  removeClass(el, name) {
    this.delegate.removeClass(el, name);
  }
  setStyle(el, style2, value, flags) {
    this.delegate.setStyle(el, style2, value, flags);
  }
  removeStyle(el, style2, flags) {
    this.delegate.removeStyle(el, style2, flags);
  }
  setProperty(el, name, value) {
    if (name.charAt(0) == ANIMATION_PREFIX && name == DISABLE_ANIMATIONS_FLAG) {
      this.disableAnimations(el, !!value);
    } else {
      this.delegate.setProperty(el, name, value);
    }
  }
  setValue(node, value) {
    this.delegate.setValue(node, value);
  }
  listen(target, eventName, callback) {
    return this.delegate.listen(target, eventName, callback);
  }
  disableAnimations(element, value) {
    this.engine.disableAnimations(element, value);
  }
};
var AnimationRenderer = class extends BaseAnimationRenderer {
  constructor(factory, namespaceId, delegate, engine, onDestroy) {
    super(namespaceId, delegate, engine, onDestroy);
    this.factory = factory;
    this.namespaceId = namespaceId;
  }
  setProperty(el, name, value) {
    if (name.charAt(0) == ANIMATION_PREFIX) {
      if (name.charAt(1) == "." && name == DISABLE_ANIMATIONS_FLAG) {
        value = value === void 0 ? true : !!value;
        this.disableAnimations(el, value);
      } else {
        this.engine.process(this.namespaceId, el, name.slice(1), value);
      }
    } else {
      this.delegate.setProperty(el, name, value);
    }
  }
  listen(target, eventName, callback) {
    if (eventName.charAt(0) == ANIMATION_PREFIX) {
      const element = resolveElementFromTarget(target);
      let name = eventName.slice(1);
      let phase = "";
      if (name.charAt(0) != ANIMATION_PREFIX) {
        [name, phase] = parseTriggerCallbackName(name);
      }
      return this.engine.listen(this.namespaceId, element, name, phase, (event) => {
        const countId = event["_data"] || -1;
        this.factory.scheduleListenerCallback(countId, callback, event);
      });
    }
    return this.delegate.listen(target, eventName, callback);
  }
};
function resolveElementFromTarget(target) {
  switch (target) {
    case "body":
      return document.body;
    case "document":
      return document;
    case "window":
      return window;
    default:
      return target;
  }
}
function parseTriggerCallbackName(triggerName) {
  const dotIndex = triggerName.indexOf(".");
  const trigger = triggerName.substring(0, dotIndex);
  const phase = triggerName.slice(dotIndex + 1);
  return [trigger, phase];
}
var AnimationRendererFactory = class {
  constructor(delegate, engine, _zone) {
    this.delegate = delegate;
    this.engine = engine;
    this._zone = _zone;
    this._currentId = 0;
    this._microtaskId = 1;
    this._animationCallbacksBuffer = [];
    this._rendererCache = /* @__PURE__ */ new Map();
    this._cdRecurDepth = 0;
    engine.onRemovalComplete = (element, delegate2) => {
      delegate2?.removeChild(null, element);
    };
  }
  createRenderer(hostElement, type) {
    const EMPTY_NAMESPACE_ID = "";
    const delegate = this.delegate.createRenderer(hostElement, type);
    if (!hostElement || !type?.data?.["animation"]) {
      const cache = this._rendererCache;
      let renderer = cache.get(delegate);
      if (!renderer) {
        const onRendererDestroy = () => cache.delete(delegate);
        renderer = new BaseAnimationRenderer(EMPTY_NAMESPACE_ID, delegate, this.engine, onRendererDestroy);
        cache.set(delegate, renderer);
      }
      return renderer;
    }
    const componentId = type.id;
    const namespaceId = type.id + "-" + this._currentId;
    this._currentId++;
    this.engine.register(namespaceId, hostElement);
    const registerTrigger = (trigger) => {
      if (Array.isArray(trigger)) {
        trigger.forEach(registerTrigger);
      } else {
        this.engine.registerTrigger(componentId, namespaceId, hostElement, trigger.name, trigger);
      }
    };
    const animationTriggers = type.data["animation"];
    animationTriggers.forEach(registerTrigger);
    return new AnimationRenderer(this, namespaceId, delegate, this.engine);
  }
  begin() {
    this._cdRecurDepth++;
    if (this.delegate.begin) {
      this.delegate.begin();
    }
  }
  _scheduleCountTask() {
    queueMicrotask(() => {
      this._microtaskId++;
    });
  }
  /** @internal */
  scheduleListenerCallback(count, fn, data) {
    if (count >= 0 && count < this._microtaskId) {
      this._zone.run(() => fn(data));
      return;
    }
    const animationCallbacksBuffer = this._animationCallbacksBuffer;
    if (animationCallbacksBuffer.length == 0) {
      queueMicrotask(() => {
        this._zone.run(() => {
          animationCallbacksBuffer.forEach((tuple) => {
            const [fn2, data2] = tuple;
            fn2(data2);
          });
          this._animationCallbacksBuffer = [];
        });
      });
    }
    animationCallbacksBuffer.push([fn, data]);
  }
  end() {
    this._cdRecurDepth--;
    if (this._cdRecurDepth == 0) {
      this._zone.runOutsideAngular(() => {
        this._scheduleCountTask();
        this.engine.flush(this._microtaskId);
      });
    }
    if (this.delegate.end) {
      this.delegate.end();
    }
  }
  whenRenderingDone() {
    return this.engine.whenRenderingDone();
  }
};

// node_modules/@angular/platform-browser/fesm2022/animations.mjs
var InjectableAnimationEngine = class _InjectableAnimationEngine extends AnimationEngine {
  // The `ApplicationRef` is injected here explicitly to force the dependency ordering.
  // Since the `ApplicationRef` should be created earlier before the `AnimationEngine`, they
  // both have `ngOnDestroy` hooks and `flush()` must be called after all views are destroyed.
  constructor(doc, driver, normalizer) {
    super(doc, driver, normalizer);
  }
  ngOnDestroy() {
    this.flush();
  }
  static {
    this.\u0275fac = function InjectableAnimationEngine_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _InjectableAnimationEngine)(\u0275\u0275inject(DOCUMENT), \u0275\u0275inject(AnimationDriver), \u0275\u0275inject(AnimationStyleNormalizer));
    };
  }
  static {
    this.\u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
      token: _InjectableAnimationEngine,
      factory: _InjectableAnimationEngine.\u0275fac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(InjectableAnimationEngine, [{
    type: Injectable
  }], () => [{
    type: Document,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: AnimationDriver
  }, {
    type: AnimationStyleNormalizer
  }], null);
})();
function instantiateDefaultStyleNormalizer() {
  return new WebAnimationsStyleNormalizer();
}
function instantiateRendererFactory(renderer, engine, zone) {
  return new AnimationRendererFactory(renderer, engine, zone);
}
var SHARED_ANIMATION_PROVIDERS = [{
  provide: AnimationStyleNormalizer,
  useFactory: instantiateDefaultStyleNormalizer
}, {
  provide: AnimationEngine,
  useClass: InjectableAnimationEngine
}, {
  provide: RendererFactory2,
  useFactory: instantiateRendererFactory,
  deps: [DomRendererFactory2, AnimationEngine, NgZone]
}];
var BROWSER_ANIMATIONS_PROVIDERS = [{
  provide: AnimationDriver,
  useFactory: () => new WebAnimationsDriver()
}, {
  provide: ANIMATION_MODULE_TYPE,
  useValue: "BrowserAnimations"
}, ...SHARED_ANIMATION_PROVIDERS];
var BROWSER_NOOP_ANIMATIONS_PROVIDERS = [{
  provide: AnimationDriver,
  useClass: NoopAnimationDriver
}, {
  provide: ANIMATION_MODULE_TYPE,
  useValue: "NoopAnimations"
}, ...SHARED_ANIMATION_PROVIDERS];
var BrowserAnimationsModule = class _BrowserAnimationsModule {
  /**
   * Configures the module based on the specified object.
   *
   * @param config Object used to configure the behavior of the `BrowserAnimationsModule`.
   * @see {@link BrowserAnimationsModuleConfig}
   *
   * @usageNotes
   * When registering the `BrowserAnimationsModule`, you can use the `withConfig`
   * function as follows:
   * ```
   * @NgModule({
   *   imports: [BrowserAnimationsModule.withConfig(config)]
   * })
   * class MyNgModule {}
   * ```
   */
  static withConfig(config) {
    return {
      ngModule: _BrowserAnimationsModule,
      providers: config.disableAnimations ? BROWSER_NOOP_ANIMATIONS_PROVIDERS : BROWSER_ANIMATIONS_PROVIDERS
    };
  }
  static {
    this.\u0275fac = function BrowserAnimationsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BrowserAnimationsModule)();
    };
  }
  static {
    this.\u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
      type: _BrowserAnimationsModule
    });
  }
  static {
    this.\u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({
      providers: BROWSER_ANIMATIONS_PROVIDERS,
      imports: [BrowserModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BrowserAnimationsModule, [{
    type: NgModule,
    args: [{
      exports: [BrowserModule],
      providers: BROWSER_ANIMATIONS_PROVIDERS
    }]
  }], null, null);
})();
var NoopAnimationsModule = class _NoopAnimationsModule {
  static {
    this.\u0275fac = function NoopAnimationsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NoopAnimationsModule)();
    };
  }
  static {
    this.\u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
      type: _NoopAnimationsModule
    });
  }
  static {
    this.\u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({
      providers: BROWSER_NOOP_ANIMATIONS_PROVIDERS,
      imports: [BrowserModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NoopAnimationsModule, [{
    type: NgModule,
    args: [{
      exports: [BrowserModule],
      providers: BROWSER_NOOP_ANIMATIONS_PROVIDERS
    }]
  }], null, null);
})();

// src/app/app-routing.module.ts
var routes = [
  {
    path: "",
    canActivate: [AuthGuard],
    runGuardsAndResolvers: "always",
    children: [
      {
        path: "settings",
        loadChildren: () => import("./settings-routing.module-BVAGWILL.js").then((m) => m.routes)
      },
      {
        path: "collections",
        loadChildren: () => import("./collections-routing.module-MQT26SZW.js").then((m) => m.routes)
      },
      {
        path: "lists",
        loadChildren: () => import("./reading-list-routing.module-2NWAKNWS.js").then((m) => m.routes)
      },
      {
        path: "announcements",
        loadChildren: () => import("./announcements-routing.module-GVIPS742.js").then((m) => m.routes)
      },
      {
        path: "bookmarks",
        loadChildren: () => import("./bookmark-routing.module-V5XNSOAD.js").then((m) => m.routes)
      },
      {
        path: "all-series",
        loadChildren: () => import("./all-series-routing.module-2TNHIZX7.js").then((m) => m.routes)
      },
      {
        path: "all-filters",
        loadChildren: () => import("./all-filters-routing.module-PGIDQ4QT.js").then((m) => m.routes)
      },
      {
        path: "want-to-read",
        loadChildren: () => import("./want-to-read-routing.module-COTLZBQW.js").then((m) => m.routes)
      },
      {
        path: "home",
        loadChildren: () => import("./dashboard-routing.module-6IM4QLXC.js").then((m) => m.routes)
      },
      {
        path: "person",
        loadChildren: () => import("./person-detail-routing.module-45Y4MLFF.js").then((m) => m.routes)
      },
      {
        path: "browse/authors",
        loadChildren: () => import("./browse-authors-routing.module-N56W42OD.js").then((m) => m.routes)
      },
      {
        path: "library",
        runGuardsAndResolvers: "always",
        canActivate: [AuthGuard, LibraryAccessGuard],
        children: [
          {
            path: ":libraryId",
            pathMatch: "full",
            loadChildren: () => import("./library-detail-routing.module-UYB6FUMH.js").then((m) => m.routes)
          },
          {
            path: ":libraryId/series/:seriesId",
            pathMatch: "full",
            loadComponent: () => import("./series-detail.component-HPEW4B3F.js").then((c) => c.SeriesDetailComponent)
          },
          {
            path: ":libraryId/series/:seriesId/chapter/:chapterId",
            pathMatch: "full",
            loadComponent: () => import("./chapter-detail.component-WNNH24DZ.js").then((c) => c.ChapterDetailComponent)
          },
          {
            path: ":libraryId/series/:seriesId/volume/:volumeId",
            pathMatch: "full",
            loadComponent: () => import("./volume-detail.component-YFEFYMXC.js").then((c) => c.VolumeDetailComponent)
          },
          {
            path: ":libraryId/series/:seriesId/manga",
            loadChildren: () => import("./manga-reader.router.module-HVR3YQJD.js").then((m) => m.routes)
          },
          {
            path: ":libraryId/series/:seriesId/book",
            loadChildren: () => import("./book-reader.router.module-MXNPFDVD.js").then((m) => m.routes)
          },
          {
            path: ":libraryId/series/:seriesId/pdf",
            loadChildren: () => import("./pdf-reader.router.module-F3KUWDF5.js").then((m) => m.routes)
          }
        ]
      },
      { path: "", pathMatch: "full", redirectTo: "home" }
    ]
  },
  {
    path: "registration",
    loadChildren: () => import("./registration.router.module-CWBG5KC5.js").then((m) => m.routes)
  },
  {
    path: "login",
    loadChildren: () => import("./registration.router.module-CWBG5KC5.js").then((m) => m.routes)
  },
  { path: "libraries", pathMatch: "full", redirectTo: "home" },
  { path: "**", pathMatch: "prefix", redirectTo: "home" },
  { path: "**", pathMatch: "full", redirectTo: "home" },
  { path: "", pathMatch: "full", redirectTo: "home" }
];
var AppRoutingModule = class _AppRoutingModule {
  static {
    this.\u0275fac = function AppRoutingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AppRoutingModule)();
    };
  }
  static {
    this.\u0275mod = \u0275\u0275defineNgModule({ type: _AppRoutingModule });
  }
  static {
    this.\u0275inj = \u0275\u0275defineInjector({ imports: [RouterModule.forRoot(routes, { scrollPositionRestoration: "enabled", preloadingStrategy: PreloadAllModules }), RouterModule] });
  }
};

// src/app/_interceptors/jwt.interceptor.ts
var JwtInterceptor = class _JwtInterceptor {
  constructor(accountService) {
    this.accountService = accountService;
  }
  intercept(request, next) {
    return this.accountService.currentUser$.pipe(take(1), switchMap((user) => {
      if (user) {
        request = request.clone({
          setHeaders: {
            Authorization: `Bearer ${user.token}`
          }
        });
      }
      return next.handle(request);
    }));
  }
  static {
    this.\u0275fac = function JwtInterceptor_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JwtInterceptor)(\u0275\u0275inject(AccountService));
    };
  }
  static {
    this.\u0275prov = \u0275\u0275defineInjectable({ token: _JwtInterceptor, factory: _JwtInterceptor.\u0275fac });
  }
};

// src/app/_interceptors/error.interceptor.ts
var ErrorInterceptor = class _ErrorInterceptor {
  constructor(router, toastr, accountService, translocoService) {
    this.router = router;
    this.toastr = toastr;
    this.accountService = accountService;
    this.translocoService = translocoService;
  }
  intercept(request, next) {
    return next.handle(request).pipe(catchError((error) => {
      if (error === void 0 || error === null) {
        return throwError(error);
      }
      switch (error.status) {
        case 400:
          this.handleValidationError(error);
          break;
        case 401:
          this.handleAuthError(error);
          break;
        case 404:
          this.handleNotFound(error);
          break;
        case 500:
          this.handleServerException(error);
          break;
        default:
          const genericError = translate("errors.generic");
          if (this.toastr.previousToastMessage !== "Something unexpected went wrong." && this.toastr.previousToastMessage !== genericError) {
            this.toast(genericError);
          }
          break;
      }
      return throwError(error);
    }));
  }
  handleValidationError(error) {
    if (Array.isArray(error.error)) {
      const modalStateErrors = [];
      if (error.error.length > 0 && error.error[0].hasOwnProperty("message")) {
        if (error.error[0].details === null) {
          error.error.forEach((issue) => {
            modalStateErrors.push(issue.message);
          });
        } else {
          error.error.forEach((issue) => {
            modalStateErrors.push(issue.details);
          });
        }
      } else {
        error.error.forEach((issue) => {
          modalStateErrors.push(issue.description);
        });
      }
      throw modalStateErrors.flat();
    } else if (error.error.errors) {
      const modalStateErrors = [];
      for (const key in error.error.errors) {
        if (error.error.errors[key]) {
          modalStateErrors.push(error.error.errors[key]);
        }
      }
      throw modalStateErrors.flat();
    } else {
      console.error("error:", error);
      if (error.statusText === "Bad Request") {
        if (error.error instanceof Blob) {
          this.toast("errors.download", error.status);
          return;
        }
        this.toast(error.error, this.translocoService.translate("errors.error-code", { num: error.status }));
      } else {
        this.toast(error.statusText === "OK" ? error.error : error.statusText, this.translocoService.translate("errors.error-code", { num: error.status }));
      }
    }
  }
  handleNotFound(error) {
    this.toast("errors.not-found");
  }
  handleServerException(error) {
    const err = error.error;
    if (err.hasOwnProperty("message") && err.message.trim() !== "") {
      if (err.message != "User is not authenticated" && error.message !== "errors.user-not-auth") {
        console.error("500 error: ", error);
      }
      this.toast(err.message);
      return;
    }
    if (error.hasOwnProperty("message") && error.message.trim() !== "") {
      if (error.message !== "User is not authenticated" && error.message !== "errors.user-not-auth") {
        console.error("500 error: ", error);
      }
      return;
    }
    this.toast("errors.unknown-crit");
    console.error("500 error:", error);
  }
  handleAuthError(error) {
    if (location.href.includes("/registration/confirm-email?token=")) {
      return;
    }
    this.accountService.logout();
  }
  toast(message, title) {
    if (message.startsWith("errors.")) {
      this.toastr.error(this.translocoService.translate(message), title);
    } else {
      this.toastr.error(message, title);
    }
  }
  static {
    this.\u0275fac = function ErrorInterceptor_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ErrorInterceptor)(\u0275\u0275inject(Router), \u0275\u0275inject(ToastrService), \u0275\u0275inject(AccountService), \u0275\u0275inject(TranslocoService));
    };
  }
  static {
    this.\u0275prov = \u0275\u0275defineInjectable({ token: _ErrorInterceptor, factory: _ErrorInterceptor.\u0275fac });
  }
};

// i18n-cache-busting.json
var i18n_cache_busting_default = {};

// src/httpLoader.ts
var HttpLoader = class _HttpLoader {
  constructor(http) {
    this.http = http;
  }
  getTranslation(langPath) {
    const tokens = langPath.split("/");
    const langCode = tokens[tokens.length - 1];
    const url = `assets/langs/${langCode}.json?v=${i18n_cache_busting_default[langCode]}`;
    console.log("loading locale: ", url);
    return this.http.get(url);
  }
  static {
    this.\u0275fac = function HttpLoader_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HttpLoader)(\u0275\u0275inject(HttpClient));
    };
  }
  static {
    this.\u0275prov = \u0275\u0275defineInjectable({ token: _HttpLoader, factory: _HttpLoader.\u0275fac, providedIn: "root" });
  }
};

// node_modules/@jsverse/transloco-persist-lang/fesm2022/jsverse-transloco-persist-lang.mjs
var TRANSLOCO_PERSIST_LANG_STORAGE = new InjectionToken("TRANSLOCO_PERSIST_LANG_STORAGE");
var TRANSLOCO_PERSIST_LANG_CONFIG = new InjectionToken("TRANSLOCO_PERSIST_LANG_CONFIG");
var TranslocoPersistLangService = class _TranslocoPersistLangService {
  service = inject(TranslocoService);
  storage = inject(TRANSLOCO_PERSIST_LANG_STORAGE);
  config = inject(TRANSLOCO_PERSIST_LANG_CONFIG);
  subscription = null;
  storageKey = this.config.storageKey || "translocoLang";
  constructor() {
    if (isBrowser()) {
      this.init();
    }
  }
  getCachedLang() {
    return isBrowser() ? this.storage.getItem(this.storageKey) : null;
  }
  clear() {
    isBrowser() && this.storage.removeItem(this.storageKey);
  }
  updateStorageOnLangChange() {
    return this.service.langChanges$.pipe(skip(1)).subscribe((lang) => {
      this.save(lang);
    });
  }
  init() {
    this.setActiveLang();
    this.subscription?.unsubscribe();
    this.subscription = this.updateStorageOnLangChange();
  }
  setActiveLang() {
    const cachedLang = this.storage.getItem(this.storageKey);
    const defaultLang = this.service.config.defaultLang;
    let activeLang = cachedLang || defaultLang;
    if (isFunction(this.config.getLangFn)) {
      const browserLang = getBrowserLang();
      const cultureLang = getBrowserCultureLang();
      activeLang = this.config.getLangFn({
        browserLang,
        defaultLang,
        cultureLang,
        cachedLang
      });
    }
    if (activeLang) {
      this.service.setActiveLang(activeLang);
    }
  }
  save(lang) {
    if (!this.service.config.prodMode) {
      console.log(`%c \u{1F37B} Saving ${lang} to storage`, "background: #fff; color: #2196F3;");
    }
    this.storage.setItem(this.storageKey, lang);
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
    this.subscription = null;
  }
  static \u0275fac = function TranslocoPersistLangService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TranslocoPersistLangService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _TranslocoPersistLangService,
    factory: _TranslocoPersistLangService.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TranslocoPersistLangService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
function noop() {
}
function provideTranslocoPersistLang(_a) {
  var _b = _a, {
    storage
  } = _b, config = __objRest(_b, [
    "storage"
  ]);
  return makeEnvironmentProviders([
    {
      provide: TRANSLOCO_PERSIST_LANG_CONFIG,
      useValue: config ?? {}
    },
    // Initialize the service
    {
      provide: APP_INITIALIZER,
      useFactory: () => noop,
      multi: true,
      deps: [TranslocoPersistLangService]
    },
    __spreadValues({
      provide: TRANSLOCO_PERSIST_LANG_STORAGE
    }, storage)
  ]);
}

// node_modules/@jsverse/transloco-persist-translations/fesm2022/jsverse-transloco-persist-translations.mjs
var dayInMilliseconds = 86400;
var defaultConfig = {
  ttl: dayInMilliseconds,
  storageKey: "@transloco/translations"
};
var TRANSLOCO_PERSIST_TRANSLATIONS_LOADER = new InjectionToken("TRANSLOCO_PERSIST_TRANSLATIONS_LOADER");
var TRANSLOCO_PERSIST_TRANSLATIONS_STORAGE = new InjectionToken("The storage to use for the persistance behavior");
var TRANSLOCO_PERSIST_TRANSLATIONS_STORAGE_CONFIG = new InjectionToken("Configuration for the storage behavior");
function isPromise(v) {
  return v && isFunction(v.then);
}
function observify(asyncOrValue) {
  if (isPromise(asyncOrValue) || isObservable(asyncOrValue)) {
    return from(asyncOrValue);
  }
  return of(asyncOrValue);
}
function getTimestampKey(key) {
  return `${key}/timestamp`;
}
var TranslocoPersistTranslations = class _TranslocoPersistTranslations {
  loader = inject(TRANSLOCO_PERSIST_TRANSLATIONS_LOADER);
  storage = inject(TRANSLOCO_PERSIST_TRANSLATIONS_STORAGE);
  config = inject(TRANSLOCO_PERSIST_TRANSLATIONS_STORAGE_CONFIG);
  subscription = this.clearCurrentStorage().subscribe();
  cache = null;
  getTranslation(lang) {
    const storageKey = this.config.storageKey;
    return this.getCached(storageKey).pipe(switchMap((translations) => {
      if (translations?.[lang]) {
        return of(translations[lang]);
      }
      return from(this.loader.getTranslation(lang)).pipe(tap((translation) => this.setCache(storageKey, lang, translation)));
    }));
  }
  clearCache() {
    this.clearTimestamp(this.config.storageKey);
    this.clearTranslations();
    this.cache = null;
  }
  getCached(key) {
    return this.cache ? of(this.cache) : observify(this.storage.getItem(key)).pipe(map((item) => {
      return item ? this.decode(key, item) : null;
    }), tap((item) => {
      this.cache = item;
    }), take(1));
  }
  setCache(key, lang, translation) {
    const translations = this.cache || {};
    translations[lang] = translation;
    this.setTimestamp(key);
    this.storage.setItem(key, this.encode(translations));
    this.cache = translations;
  }
  encode(value) {
    return JSON.stringify(value);
  }
  decode(key, item) {
    if (isObject(item)) {
      return item;
    } else if (isString(item)) {
      try {
        return JSON.parse(item);
      } catch (e) {
        console.error(`storage key: ${key} is not serializable`);
        return null;
      }
    }
    return null;
  }
  setTimestamp(key) {
    this.storage.setItem(getTimestampKey(key), Date.now());
  }
  getTimestamp(key) {
    return observify(this.storage.getItem(getTimestampKey(key))).pipe(map(parseInt));
  }
  clearTimestamp(key) {
    this.storage.removeItem(getTimestampKey(key));
  }
  clearTranslations() {
    this.storage.removeItem(this.config.storageKey);
  }
  clearCurrentStorage() {
    const storageKey = this.config.storageKey;
    return this.getTimestamp(storageKey).pipe(filter(Boolean), tap((time) => {
      const isExpired = Date.now() - time >= this.config.ttl;
      if (isExpired) {
        this.storage.removeItem(storageKey);
      }
    }));
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
    this.subscription = null;
  }
  static \u0275fac = function TranslocoPersistTranslations_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TranslocoPersistTranslations)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _TranslocoPersistTranslations,
    factory: _TranslocoPersistTranslations.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TranslocoPersistTranslations, [{
    type: Injectable
  }], null, null);
})();
function provideTranslocoPersistTranslations(_a) {
  var _b = _a, {
    storage,
    loader
  } = _b, storageConfig = __objRest(_b, [
    "storage",
    "loader"
  ]);
  return makeEnvironmentProviders([{
    provide: TRANSLOCO_LOADER,
    useClass: TranslocoPersistTranslations
  }, provideTranslocoPersistTranslationsConfig(storageConfig), {
    provide: TRANSLOCO_PERSIST_TRANSLATIONS_LOADER,
    useClass: loader
  }, __spreadValues({
    provide: TRANSLOCO_PERSIST_TRANSLATIONS_STORAGE
  }, storage)]);
}
function provideTranslocoPersistTranslationsConfig(config) {
  return {
    provide: TRANSLOCO_PERSIST_TRANSLATIONS_STORAGE_CONFIG,
    useValue: __spreadValues(__spreadValues({}, defaultConfig), config)
  };
}

// src/main.ts
var disableAnimations = !("animate" in document.documentElement);
function preloadUser(userService, transloco) {
  return function() {
    return userService.currentUser$.pipe(distinctUntilChanged(), switchMap((user) => {
      if (user && user.preferences.locale) {
        transloco.setActiveLang(user.preferences.locale);
        return transloco.load(user.preferences.locale);
      }
      const localStorageLocale = localStorage.getItem(AccountService.localeKey) || "en";
      transloco.setActiveLang(localStorageLocale);
      return transloco.load(localStorageLocale);
    })).subscribe();
  };
}
var preLoad = {
  provide: APP_INITIALIZER,
  multi: true,
  useFactory: preloadUser,
  deps: [AccountService, TranslocoService]
};
function transformLanguageCodes(arr) {
  const transformedArray = [];
  arr.forEach((code) => {
    transformedArray.push(code);
    if (code.includes("-")) {
      const transformedCode = code.replace("-", "_");
      transformedArray.push(transformedCode);
    }
  });
  return transformedArray;
}
var languageCodes = [
  "af",
  "af_ZA",
  "ar",
  "ar_AE",
  "ar_BH",
  "ar_DZ",
  "ar_EG",
  "ar_IQ",
  "ar_JO",
  "ar_KW",
  "ar_LB",
  "ar_LY",
  "ar_MA",
  "ar_OM",
  "ar_QA",
  "ar_SA",
  "ar_SY",
  "ar_TN",
  "ar_YE",
  "az",
  "az_AZ",
  "az_AZ",
  "be",
  "be_BY",
  "bg",
  "bg_BG",
  "bs_BA",
  "ca",
  "ca_ES",
  "cs",
  "cs_CZ",
  "cy",
  "cy_GB",
  "da",
  "da_DK",
  "de",
  "de_AT",
  "de_CH",
  "de_DE",
  "de_LI",
  "de_LU",
  "dv",
  "dv_MV",
  "el",
  "el_GR",
  "en",
  "en_AU",
  "en_BZ",
  "en_CA",
  "en_CB",
  "en_GB",
  "en_IE",
  "en_JM",
  "en_NZ",
  "en_PH",
  "en_TT",
  "en_US",
  "en_ZA",
  "en_ZW",
  "eo",
  "es",
  "es_AR",
  "es_BO",
  "es_CL",
  "es_CO",
  "es_CR",
  "es_DO",
  "es_EC",
  "es_ES",
  "es_ES",
  "es_GT",
  "es_HN",
  "es_MX",
  "es_NI",
  "es_PA",
  "es_PE",
  "es_PR",
  "es_PY",
  "es_SV",
  "es_UY",
  "es_VE",
  "et",
  "et_EE",
  "eu",
  "eu_ES",
  "fa",
  "fa_IR",
  "fi",
  "fi_FI",
  "fo",
  "fo_FO",
  "fr",
  "fr_BE",
  "fr_CA",
  "fr_CH",
  "fr_FR",
  "fr_LU",
  "fr_MC",
  "gl",
  "gl_ES",
  "gu",
  "gu_IN",
  "he",
  "he_IL",
  "hi",
  "hi_IN",
  "hr",
  "hr_BA",
  "hr_HR",
  "hu",
  "hu_HU",
  "hy",
  "hy_AM",
  "id",
  "id_ID",
  "is",
  "is_IS",
  "it",
  "it_CH",
  "it_IT",
  "ja",
  "ja_JP",
  "ka",
  "ka_GE",
  "kk",
  "kk_KZ",
  "kn",
  "kn_IN",
  "ko",
  "ko_KR",
  "kok",
  "kok_IN",
  "ky",
  "ky_KG",
  "lt",
  "lt_LT",
  "lv",
  "lv_LV",
  "mi",
  "mi_NZ",
  "mk",
  "mk_MK",
  "mn",
  "mn_MN",
  "mr",
  "mr_IN",
  "ms",
  "ms_BN",
  "ms_MY",
  "mt",
  "mt_MT",
  "nb",
  "nb_NO",
  "nl",
  "nl_BE",
  "nl_NL",
  "nn_NO",
  "ns",
  "ns_ZA",
  "pa",
  "pa_IN",
  "pl",
  "pl_PL",
  "ps",
  "ps_AR",
  "pt",
  "pt_BR",
  "pt_PT",
  "qu",
  "qu_BO",
  "qu_EC",
  "qu_PE",
  "ro",
  "ro_RO",
  "ru",
  "ru_RU",
  "sa",
  "sa_IN",
  "se",
  "se_FI",
  "se_FI",
  "se_FI",
  "se_NO",
  "se_NO",
  "se_NO",
  "se_SE",
  "se_SE",
  "se_SE",
  "sk",
  "sk_SK",
  "sl",
  "sl_SI",
  "sq",
  "sq_AL",
  "sr_BA",
  "sr_BA",
  "sr_SP",
  "sr_SP",
  "sv",
  "sv_FI",
  "sv_SE",
  "sw",
  "sw_KE",
  "syr",
  "syr_SY",
  "ta",
  "ta_IN",
  "te",
  "te_IN",
  "th",
  "th_TH",
  "tl",
  "tl_PH",
  "tn",
  "tn_ZA",
  "tr",
  "tr_TR",
  "tt",
  "tt_RU",
  "ts",
  "uk",
  "uk_UA",
  "ur",
  "ur_PK",
  "uz",
  "uz_UZ",
  "uz_UZ",
  "vi",
  "vi_VN",
  "xh",
  "xh_ZA",
  "zh",
  "zh_CN",
  "zh_HK",
  "zh_MO",
  "zh_SG",
  "zh_TW",
  "zu",
  "zu_ZA",
  "zh_Hans",
  "zh_Hant",
  "nb_NO",
  "ga"
];
var translocoOptions = {
  config: {
    reRenderOnLangChange: true,
    availableLangs: transformLanguageCodes(languageCodes),
    prodMode: environment.production,
    defaultLang: "en",
    fallbackLang: "en",
    missingHandler: {
      useFallbackTranslation: true,
      allowEmpty: false,
      logMissingKey: true
    },
    failedRetries: 2
  }
};
function getBaseHref(platformLocation) {
  return platformLocation.getBaseHrefFromDOM();
}
bootstrapApplication(AppComponent, {
  providers: [
    importProvidersFrom(BrowserModule, AppRoutingModule, BrowserAnimationsModule.withConfig({ disableAnimations }), LazyLoadImageModule, ToastrModule.forRoot({
      positionClass: "toast-bottom-right",
      preventDuplicates: true,
      timeOut: 6e3,
      countDuplicates: true,
      autoDismiss: true
    }), NgCircleProgressModule.forRoot()),
    provideTransloco(translocoOptions),
    provideTranslocoLocale({
      defaultLocale: "en"
    }),
    provideTranslocoPersistTranslations({
      loader: HttpLoader,
      storage: { useValue: localStorage },
      ttl: 604800
    }),
    provideTranslocoPersistLang({
      storage: {
        useValue: localStorage
      }
    }),
    { provide: HTTP_INTERCEPTORS, useClass: ErrorInterceptor, multi: true },
    { provide: HTTP_INTERCEPTORS, useClass: JwtInterceptor, multi: true },
    preLoad,
    Title,
    { provide: SAVER, useFactory: getSaver },
    {
      provide: APP_BASE_HREF,
      useFactory: getBaseHref,
      deps: [PlatformLocation]
    },
    provideHttpClient(withInterceptorsFromDi())
  ]
}).catch((err) => console.error(err));
export {
  preLoad,
  preloadUser
};
/*! Bundled license information:

@angular/animations/fesm2022/browser.mjs:
  (**
   * @license Angular v18.2.9
   * (c) 2010-2024 Google LLC. https://angular.io/
   * License: MIT
   *)

@angular/platform-browser/fesm2022/animations.mjs:
  (**
   * @license Angular v18.2.9
   * (c) 2010-2024 Google LLC. https://angular.io/
   * License: MIT
   *)
*/
//# sourceMappingURL=main.js.map
